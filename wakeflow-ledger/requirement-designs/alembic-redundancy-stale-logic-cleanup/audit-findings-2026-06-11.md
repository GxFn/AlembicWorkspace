# Alembic Redundancy & Stale Logic Audit Findings

Date: 2026-06-11
Scope: Alembic, AlembicCore, AlembicAgent, AlembicDashboard, AlembicPlugin
Method: six parallel read-only scan agents (one per repository plus one
cross-repository comparison), launched from the AlembicWorkspace controller
session. This document freezes the evidence base for the
`alembic-redundancy-stale-logic-cleanup` demand sequence. Findings marked
medium/low confidence must be re-verified by RC0 before any deletion.

## 1. Dependency Direction (verified clean)

- All five `package.json` cross-references match the declared direction
  (Core at the bottom; Agent/Alembic/Plugin consume Core via
  `file:../AlembicCore`; Alembic consumes Agent; Dashboard standalone).
- No reverse dependencies, no cross-repository relative-path imports
  (`../../Alembic*`) in any product source tree.
- Confidence: high. No cleanup action required.

## 2. Alembic (main repository)

| # | Finding | Evidence | Confidence |
| --- | --- | --- | --- |
| A1 | `AGENTS.md` source map lists deleted `lib/agent/` and `lib/external/` | `Alembic/AGENTS.md:125-142`; Wave 4/5 extraction completed per `config/agent-extraction-boundary.json` | High |
| A2 | `AGENTS.md` says Dashboard lives in `dashboard/`; that directory holds only built output (`dist/`, ~4.9MB, built 2026-06-04); source of truth is the AlembicDashboard repository | `Alembic/AGENTS.md:117`, `Alembic/dashboard/` | High |
| A3 | `scratch/` holds 9 stage decision documents (AFAPI-* reports, `*-decision-register-route-probe*.json`) that per AGENTS.md belong in the workspace ledger. RC0 correction: they are NOT git-tracked (`.gitignore:58` ignores `scratch/`; `git ls-files scratch/` is empty) — archive-to-ledger only, no git removal exists | `Alembic/scratch/` | Verified by RC0 |
| A4 | `@deprecated` search result helper still defined/exported | `Alembic/lib/resident/tool-handlers/search.ts:391` ("Use `slimSearchResult` from `SearchTypes.ts`") | Medium |
| A5 | `test/unit/ExitController.test.ts` has 73 skip-related matches. RC0 verdict: clean — 0 disabled tests; all 116 matches across 11 unit files are business identifiers (`xit` inside `exit`/`shouldExit(`, `skipped*` counters); vitest configs carry no global filters. No test changes allowed | `Alembic/test/unit/ExitController.test.ts` (plus 10 other unit files) | Verified clean by RC0 |
| A6 | Top-level `skills/` directory is empty. RC0 correction: it is a configured runtime mount point (`config/default.json:42,64`; `lib/bootstrap.ts:215` hook scan; SetupService scaffolding) — keep, do not delete | `Alembic/skills/` | Verified by RC0 — keep-with-reason |
| A7 | `.release/alembic-ai/package.json` snapshot sync. RC0 resolution: `.release/` is gitignored (`.gitignore:16`, 0 tracked files) and regenerated by `scripts/prepare-publish-staging.mjs`; stale local copy is harmless, no sync issue | `Alembic/.release/alembic-ai/package.json` | Resolved by RC0 — no action |
| A8 | `vendor/AlembicDashboard` is a full repository copy (~307MB incl. node_modules); `vendor/AlembicCore` also present | `Alembic/vendor/` | High (disk fact); disposition needs decision |

Boundary gates (`config/core-import-boundary.json`,
`config/agent-extraction-boundary.json`) currently pass: 0 local agent files,
0 duplicate tool-v2 files, 0 product `#agent` call sites.

## 3. AlembicCore

| # | Finding | Evidence | Confidence |
| --- | --- | --- | --- |
| C1 | Interactive CLI prompt inside headless Core: `promptDiscovererChoice()` uses readline, TTY detection, console output. RC0 addition: ZERO consumers in all five repos (exported via `core/discovery/index.ts:50` and stable facade `project-intelligence.ts:65` but never called) — verified-dead interactive surface; delete in Core (keep load/save preference), nothing to relocate | `AlembicCore/src/core/discovery/DiscovererPreference.ts:163-211` | High; consumers verified by RC0 |
| C2 | Non-deterministic pruning trigger `Math.random() < 0.01` | `AlembicCore/src/repository/token/TokenUsageStore.ts:154` | High |
| C3 | Migration sequence gap: files are 001, 004-010; 002/003 absent, reason unrecorded | `AlembicCore/src/infrastructure/database/migrations/` | Medium — RC0 checks git history |
| C4 | 98 transitional public exports awaiting planned Wave 3B convergence (470 export paths total, 18 stable) | `AlembicCore/config/public-api-boundary.json` | High (planned, not dead) |
| C5 | `HnswIndex.ts` `#randomLevel()` randomness is algorithm-inherent | `AlembicCore/src/infrastructure/vector/HnswIndex.ts:188-192` | Keep; explicitly out of scope |

Codex/MCP isolation, reverse-dependency, delivery, and tool-system boundary
tests all pass; 76 test files active with no skip markers.

## 4. AlembicAgent

| # | Finding | Evidence | Confidence |
| --- | --- | --- | --- |
| G1 | Probe script import `../dist/external/ai/index.js`. RC0 nuance: the path still exists locally as a STALE build leftover (mtime 2026-05-29) while `src/external/` is gone, so the probe breaks on any clean rebuild; correct path is `../dist/ai/index.js` (`createProvider` from `AiFactory.ts:39` re-exported by `src/ai/index.ts`). `tmp/` and `dist/` are gitignored; the probe is an untracked local utility | `AlembicAgent/tmp/probe-relay.mjs:24` | Verified by RC0 |
| G2 | V1 (`src/tools/core/`) and V2 (`src/tools/v2/`) tool systems coexist via `V2ToolRouterAdapter`; no recorded V1 retirement plan | `AlembicAgent/src/tools/`, `config/agent-public-api-boundary.json` keeps `./tools` and `./tools/v2` separate | Medium (intentional transition, undocumented end state) |
| G3 | `MemoryStore` maintains raw SQLite CRUD for `semantic_memories` (22 references) while schema ownership sits in Core (`schema.ts`, migration 001, `memory.ts`, `MemoryRepository.ts`) | `AlembicAgent/src/agent/memory/MemoryStore.ts` (611 lines; RC0 corrected from ~194) | Verified by RC0 — ownership decision needed (RC6 SD-4) |
| G4 | `config/core-import-boundary.json` has empty `referenceLimits`; boundary intent not encoded | `AlembicAgent/config/core-import-boundary.json:5` | Medium |
| G5 | `agent-public-api-boundary.json` `expectedCounts` hand-maintained (stable-public 15) | `AlembicAgent/config/agent-public-api-boundary.json:4-8` | Medium |
| G6 | `release:pack-preview` / `.alembic-source.json` staging flow verified consistent with README | `scripts/stage-agent-publish-package.mjs:184-185` | High — no action |

## 5. AlembicDashboard

Audit grade A. 31/31 contract tests pass, lint zero violations, no
out-of-scope logic, no orphan views, no hardcoded paths/keys. Only optional
items (shared paginated-list extraction in RecipesView/CandidatesView;
`api.ts` explicit-any budget at 2/3). **Excluded from cleanup waves in this
sequence; participates in validation only.**

## 6. AlembicPlugin

| # | Finding | Evidence | Confidence |
| --- | --- | --- | --- |
| P1 | `README.md` is a stale full copy of the main Alembic README (lines 1-96) describing the whole product, not the Codex plugin | `AlembicPlugin/README.md` vs `Alembic/README.md` | High |
| P2 | Legacy compatibility markers with no retirement condition. RC0 corrected locations and verdicts: `LEGACY_DIRECT_CALL_COMPATIBILITY_TOOLS` (`lib/codex/mcp/tools.ts:113-117`, EMPTY array gated by `scripts/probe-mcp-clean-output-final-cleanup.mjs` — remove-now candidate); legacy error-code mapping (`lib/codex/mcp/error-taxonomy.ts:54-76`, mapper `:205-214` — LIVE, emitter `CodexMcpServer.ts:768`, tests pin — deadline-mark); `LEGACY_IDE_AGENT_SOURCE` (defined in AlembicCore `src/shared/source-contracts.ts:3`, plugin imports at `lib/codex/SourceBoundary.ts:2` — cross-repo live data-compat bridge — deadline-mark); `legacyEffectiveIdentityFallback` (`lib/codex/runtime/ProjectRuntimeContext.ts:55-115`, diagnostic-only, serialized, tests pin — deadline-mark) | files cited | Verified by RC0 |
| P3 | `scratch/` holds 140+ `afapi-*` report/probe directories; no cleanup mechanism | `AlembicPlugin/scratch/` | High |
| P4 | 19 HTTP routes retained (vs 30+ in Alembic); actual plugin-runtime consumers of each route not yet mapped | `AlembicPlugin/lib/http/routes/` | Medium — RC0 builds consumer map |
| P5 | `vendor/AlembicCore` snapshot lags AlembicCore main by 6 commits (`648055b` vs `043e0f8`); by design, sync flow undocumented | `AlembicPlugin/vendor/AlembicCore` | High |
| P6 | MCP registration consistent: 23 tools (21 agent + 2 admin) all map to handlers and zod schemas; tests all active | `lib/codex/mcp/tools.ts`, `lib/shared/schemas/mcp-tools.ts` | High — no action |

## 7. Cross-Repository Duplication & Drift (Alembic ↔ AlembicPlugin)

- `lib/` overlap: 87 same-named files; 61 differ in content; 0 byte-identical;
  ~27.7k duplicated lines (~42% of Alembic lib).
- Intentional divergence (verified healthy): `DaemonJobRunner.ts`
  (1203 vs 299 lines, observability stack stripped), `HttpServer.ts`
  (11 Dashboard/AI routes removed), `bootstrap.ts` (env-based project scope vs
  ProjectScopeRegistry).
- **Drifted shared assets (the dangerous part):** RC0 correction: the two
  runtimes' MCP tool surfaces genuinely differ (main exposes `alembic_guard`
  and has no `alembic_code_guard` / source-graph tools), so SKILL.md
  tool-contract drift is intentional per-host divergence, NOT missed backports.
  RC5 must single-source via shared-template + host-overlay with a drift gate
  on shared sections; verbatim merging would document nonexistent tools.

| Asset | Alembic last touch | AlembicPlugin last touch | Drift |
| --- | --- | --- | --- |
| `injectable-skills/alembic-{create,guard,recipes,structure}/SKILL.md` | 2026-05-11 | 2026-06-09 | Plugin newer; wording diverged |
| `injectable-skills/alembic-devdocs/` | present | absent | main-only |
| `templates/constitution.yaml` | 2026-05-18 | 2026-06-01 | role naming diverged (`External Agent` vs `Codex Host Agent`, `chat_agent` semantics) |
| `templates/instructions/` | present | absent | main-only |
| `config/default.json` | has `ai` provider block | `ai` block removed | intentional but undocumented |

- `Alembic/dashboard/dist` is a manually synced build artifact of
  AlembicDashboard (both ~4.8-4.9MB); no automation.

## 8. RC0 Verification Updates (2026-06-11)

RC0 re-verified every medium/low-confidence item with controller-run scans
(raw outputs in the RC0 state root `evidence/raw/`). Per-item readiness,
owning demands, validation commands, and stop conditions live in the RC0
readiness matrix (`rc0-readiness-matrix-2026-06-11.md`, archived with the RC0
state root). Corrections were applied in place above: A3 (untracked, not
tracked), A5 (verified clean), A6 (configured runtime dir — keep), A7
(regenerated ignored output — no action), C1 (zero consumers — delete in
Core, nothing to relocate), G1 (stale-build nuance + untracked), G3 (611
lines), P2 (locations + per-marker verdicts), section 7 (tool-surface
divergence is intentional). New findings: A4b — plugin twin
`_slimSearchItem` at `AlembicPlugin/lib/codex/mcp/handlers/search.ts:355`,
zero consumers, routed to RC4; C2 precision — trigger now at
`TokenUsageStore.ts:159`, plus `ViolationsStore.ts:91` uses Math.random for
ID entropy only (non-behavioral, keep-with-reason, do not churn).

## 9. Final Dispositions (RC7 closure, 2026-06-12)

Every audit item's final state after RC0-RC7. "fixed-RCn" = repaired and
accepted in that demand with raw evidence; "kept-with-reason" = deliberate
keep with a recorded reason/owner; "escalated-to-SD-x" = routed to an RC6
design candidate (accepted as a future demand needing user confirmation).
Commits: RC1 Alembic 5decbd7(+655403fe scratch archive), RC2 Core ed42960
(amended hash of 97c454cf), RC3 Agent dc6d6f7, RC4 Plugin be10c80+7382c62,
RC5 Alembic 7a63e7b + Plugin 41ee2a7, RC7 Alembic 1bd60af.

| Item | Final disposition |
| --- | --- |
| A1 | fixed-RC1 — AGENTS.md source map rewritten to the real lib/ tree. |
| A2 | fixed-RC1 — dashboard/ documented as built output; AlembicDashboard is source of truth. |
| A3 | resolved-RC1 — 9 untracked scratch decisions archived to `wakeflow-ledger/workspace/archive/2026-06/alembic-scratch-decisions/` (SHA-256 cross-checked); no git operation needed (RC0 corrected the tracked claim). |
| A4 | fixed-RC1 — deprecated `_slimSearchItem` deleted from main resident handler. |
| A4b | fixed-RC4 — plugin twin `_slimSearchItem` deleted (fresh 5-repo zero-consumer scan). |
| A5 | no-action — RC0 verified 0 disabled tests; all 116 skip matches are business identifiers. |
| A6 | kept-with-reason — empty `skills/` is the configured runtime mount point (config/default.json + bootstrap); documented in RC1 AGENTS.md rewrite. |
| A7 | no-action — `.release/alembic-ai/` is gitignored regenerated staging output. |
| A8 | kept-with-reason + escalated-to-SD-2 — vendor/AlembicDashboard is a git submodule (2 gitlinks; 307MB is local node_modules); RC6 accepted: keep, document refresh flow, wire build:dashboard into release staging (future demand). |
| C1 | fixed-RC2 — interactive `promptDiscovererChoice` + readline deleted from headless Core (zero consumers); `loadPreference`/`savePreference`/`detectConflict` preserved; RC7 spot-check: 0 occurrences, persistence intact. |
| C2 | fixed-RC2 — pruning trigger now deterministic (every-Nth-write counter); `ViolationsStore` id-entropy kept-with-reason (non-behavioral). |
| C3 | resolved-RC2 — migration gap explained in-tree (002 never existed; 003 deleted with remote schema; runner is filename-ordered and gap-tolerant). |
| C4 | escalated-to-SD-5 — RC6 accepted two-phase deprecate-then-delete of the 67 zero-consumer transitional exports, release-aligned with a deliberate Plugin vendor refresh. |
| C5 | kept-with-reason — HNSW level randomness is algorithm-inherent (out-of-scope note, section 9 of the original audit retained below as section 10). |
| G1 | fixed-RC3 — probe import corrected (untracked tmp/), stale dist/external removed, validated without live API call. |
| G2 | registered-RC3 + escalated-to-SD-3 — dated V1 retirement register in Agent AGENTS.md; RC6 accepted the two-phase convergence plan (future demand). |
| G3 | resolved-RC3 + escalated-to-SD-4 — MemoryStore boundary note (Core owns schema); RC6 accepted keep-in-Agent + schema tripwire + recorded end-state trigger. |
| G4 | fixed-RC3 — referenceLimits frozen from the real import scan + 8 enforced limit-0 denies; demonstrated lint failure. |
| G5 | fixed-RC3 — expectedCounts compared against mechanically computed export counts; demonstrated mismatch failure. |
| P1 | fixed-RC4 — README/README_CN rewritten to plugin scope. |
| P2 | fixed-RC4 — docs/legacy-register.md: P2a removed-now (empty compat array + probe assertion flip); P2b/P2c/P2d keep+deadline-marked with owners and retirement conditions (P2c names Core as owner). |
| P3 | fixed-RC4 — clean-scratch retention script (dry-run default, ledger whitelist) + one supervised run: 4.4GB/136 entries → 9.5MB/79. |
| P4 | fixed-RC4 — 8 of 9 remove-candidates deleted with per-route fresh scans; auth RECLASSIFIED keep-with-consumer (RC0 zero-consumer verdict falsified — dynamic import + cross-repo HTTP literals; matrix corrected); modules keep-with-consumer (Dashboard); evolution/panorama → R-1 deadline-marked keep (RC6 decision; deletion trigger at next plugin release decision). |
| P5 | fixed-RC4 — vendor/AlembicCore refresh flow documented in Plugin AGENTS.md; lag (24 commits at RC6 re-check) is an explained state; deliberate refresh paired with SD-5 phase 2. RELEASE-PLAYBOOK pointer rides the next alembic-codex release commit (RC6 R-1 decision; no standalone submodule commit). |
| §7 drift | fixed-RC5 — shared-asset manifest (authority + comparison modes + variant declarations), 4 SKILL.md restructured to shared core + host overlay (tool contracts intentionally divergent), strict drift gate green from BOTH repos with demonstrated injected-drift failures; remaining lib/ code duplication (re-measured ≈16.6k matched lines) escalated-to-SD-1 (drift-gate extension now, Core sinking only after SD-5). |
| §5 Dashboard | validation-only — zero code diff for the whole sequence proven at RC7 (git status empty + check green, raw log in RC7 state root). |
| RC7 regression | resolved-RC7 — DynamicComposer 2-test failure root-caused to the intentional D23 projection (Agent d63ce7d) masked by a stale linked dist; test pins updated with leakage guards; process lesson: rebuild sibling dist before acceptance runs following sibling commits. |

## 10. Out of Scope for This Sequence

- AlembicBook, AlembicDesign, AlembicTest, BiliDili, PCVM, ledgers (user
  decision 2026-06-11: only the five product repositories).
- HNSW randomness (C5), model registry deprecation flow (Agent), DB-migration
  compatibility `@deprecated` types in Core (`src/types/reactive-evolution.ts`,
  `src/repository/evolution/ProposalRepository.ts`) — all verified as
  intentional mechanisms.
