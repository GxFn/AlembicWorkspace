# 检索、向量与实时 ProjectContext 架构主线实现 进度

## 统一状态

<!-- unified-status:start -->
需求: alembic-retrieval-vector-projectcontext-architecture-mainline1-2026-07-13 - 检索、向量与实时 ProjectContext 架构主线实现
主状态: planned
阶段: 无
当前任务包: p1-core-retrieval-vector-projectcontext-architecture-p1(pending)
窗口: AlembicCore(pending)
阻塞项: 无
下一步: prepare-dispatch-from-state, add-task-package, wakeflow-render-progress
评审: 无
自动化: 未启用
需要用户决策: 无
最后更新: 2026-07-13 11:30 CST
来源状态: revision 3 / event evt-20260713033056-0003
<!-- unified-status:end -->

## 目标

按已确认架构修复 BiliDili 宽泛英文查询召回，建立 Core 统一且可隔离的向量读写、混合检索与生命周期能力，并在 Plugin 实现 Graph/Recipe Map 共用、可取消、可续取、临时且只读的实时 ProjectContext build session。

## 完成定义

Core 先于 Plugin 验收：八条固定 BiliDili 英文查询稳定召回权威 Recipe <redacted>，且不硬编码同义词、不修改 Recipe；Core 提供 EmbeddingPort、VectorIndexReader/Writer、统一 KnowledgeRetrievalPort/候选/投影/refill 与 provider-independent VectorLifecycleCoordinator，查询链不持有写端口；Plugin 的 Search/Prime 使用同一检索语义，Graph 与 Recipe Map 共用按 canonical root/scope/source facts 指纹化的临时 ProjectContextBuildSession，支持 AbortSignal、120 秒 deadline、opaque continuation cursor、OS temp chunk 与终止/取消/错误/TTL 清理；不增加 Git/revision/checkpoint、host selection、role/Admin、intent、knowledge status 或可见性门禁；Controller 最后只读验证现有 AlembicWorkspace 与 BiliDili，DB/WAL/SHM/config/vector 指纹不变；不创建 Test 窗口。

## 阶段计划

P1 AlembicCore combined package and acceptance -> P2 AlembicPlugin combined package consuming the exact accepted Core commit -> P3 AlembicWorkspace controller read-only dual-project acceptance -> complete/archive. No Test window.

## 任务包

## 回填摘要

## 决策和追加日志

## Task Packages

- 2026-07-13T03:30:56.244Z p1-core-retrieval-vector-projectcontext-architecture-p1 → AlembicCore — Implement one combined AlembicCore producer package for the confirmed retrieval/vector/ProjectContext architecture.

Required implementation:
- Add an EmbeddingPort with embedQuery/embedDocuments and an explicit provider/model/dimension descriptor; adapt existing providers without exposing provider details to callers.
- Split VectorIndexReader from VectorIndexWriter. Query/search/Prime/RecipeContext construction may depend only on the reader. Indexing, reconcile and lifecycle maintenance own the writer.
- Introduce canonical KnowledgeRetrievalCandidate, HybridCandidateRetriever, KnowledgeTruthProjector and bounded refill. Make Search, Prime and RecipeContext use the same KnowledgeRetrievalPort semantics.
- Preserve raw dense, sparse and RRF contribution evidence; remove per-page max normalization that makes broad English ranking unstable.
- Evolve SyncCoordinator into provider-independent VectorLifecycleCoordinator while retaining a compatibility facade for existing consumers. Removal/cleanup must not require embedding availability; live replacement remains safe.
- Extend ProjectContext execution with an optional non-serialized AbortSignal request option so Plugin can enforce real cancellation without changing persisted/public schemas.
- Add deterministic tests for the fixed eight-query BiliDili-style matrix and authoritative Recipe id <redacted> using fixtures at Core seams; do not hardcode query synonyms or edit Recipe content.

Compatibility and non-goals:
- Preserve current public consumers and existing source-truth/refill/filter behavior.
- No Plugin, Alembic main, Test, vendor, release, real knowledge data, Git/revision/checkpoint, host-project, role/Admin, intent, knowledge-state or tool-visibility changes.
- No query-time writes and no provider-specific policy leaking into retrieval contracts.

Required evidence:
- Commit only AlembicCore.
- Focused RED/GREEN tests for reader/writer isolation, shared Search/Prime retrieval parity, refill/truth projection, raw score contribution stability, provider-independent lifecycle removal, cancellation, and eight-query ranking.
- Full Core unit suite, typecheck/check, build, exact changed files/symbols and compatibility review.
- Two-stage self-review: requirement compliance, then code quality/failure paths/performance.
- Return the exact commit for Plugin consumption. (intent: Make authoritative Recipe candidates and vector lifecycle boundaries shared, deterministic Core capabilities while preserving existing consumers.)
- 2026-07-13T04:21:45.919Z p2-plugin-retrieval-vector-projectcontext-integration-p1 → AlembicPlugin — Implement the one confirmed AlembicPlugin consumer package after accepted AlembicCore commit 7ccddf2372ddaeac2dbcfc1f651157d753709cd7. Baseline AlembicPlugin commit is be7bc058fcd188f38b48ee551f912377c2d19f1d; keep all work in AlembicPlugin and return one clean commit.

P1 — request-scoped reader-only retrieval:
- Consume the accepted Core public EmbeddingPort, VectorIndexReader, KnowledgeRetrievalPolicy/Port and raw candidate evidence.
- Refactor public Search and Prime request graphs so they construct only snapshot DB truth, query embedding, a reader-only HNSW adapter and canonical retrieval. They must not construct IndexingPipeline, VectorService, VectorIndexWriter, lifecycle/reconcile/init/rescan/rebuild, or any write-shaped request adapter.
- Search and Prime must use the same query/filter/topK candidate sequence. Remove Prime's independent 0.45/relative admission threshold and score calibration; Prime may only group/project the already-authoritative candidates and must expose bounded ordered candidateRecipeIds.
- Preserve dense similarity/rank, sparse score/rank and per-lane/raw RRF contribution through public projection. With embeddings unavailable, sparse truth still works; empty authoritative knowledge returns empty.
- Frozen eight English BiliDili query fixtures must be non-empty and place Recipe <redacted> in Top 3 without synonym hardcoding or Recipe edits.

P2/P3 — shared real-time ProjectContext:
- Add a Plugin-owned ProjectContextBuildSessionManager injected into Graph and Recipe Map, keyed by canonical real project root + normalized scope + source/manifest fact fingerprint (exists, size, high-resolution mtime, sha256), never Git state.
- Merge same-key in-flight work and use ref-counted cancellation. Graph and Recipe Map consume one fact session; Map only adds deterministic Recipe mounts and must not rebuild source facts.
- Route explicit file/symbol/sourceSlice/anchor requests through a narrow fast path before space/all-repo/map/module traversal. The containing repo identity may be read; unrelated repositories must not be scanned.
- Full-space work publishes deterministic per-repository chunks. Repo failures/timeouts remain explicit coverage outcomes.
- Add opaque, project-confined continuation with {resultRef,factSessionRef,nextCursor,hasMore,page,accumulatedCounts}. Pages are deterministic deltas and reconstruct without duplicates or missing stable IDs; partial results must stay honestly partial.
- Store chunks only in owner-only OS/runtime temp, never projectRoot/dataRoot. Terminal consumption, explicit cancel, exception and TTL expiry must remove all temp files. Expired cursors return bounded retry guidance and need not survive process restart.
- Propagate one AbortSignal from the unchanged 120-second host deadline through handler/session/Core ProjectContext; abort before response and wait for bounded cleanup acknowledgement so a slow worker actually stops.
- Keep deprecated raw-path fullMapRef null; do not expose paths in result refs/cursors.

Boundaries:
- Preserve Guard as a read-only regression consumer; do not redesign Guard.
- No persistent answer cache, knowledge writes, Git revision/checkpoint/dirty/branch checks, host-selected-project coupling, role/Admin/intent/knowledge-state/tool-visibility gates, hardcoded synonyms, Recipe edits, Test window, Alembic/AlembicAgent/AlembicDashboard source edits, vendor or release changes.
- No higher heavy-tool deadline and no query-time vector maintenance.

Required verification/evidence:
- Focused RED/GREEN tests for Search/Prime parity, orphan/deprecated truth, public raw evidence, reader-only fingerprint/zero mutation, BuildSession reuse/invalidation/ref-count cancellation, narrow fast path, 2+ cursor-page reconstruction, explicit repo failure, cursor confinement, terminal/cancel/error/TTL cleanup, and real worker-stop timeout.
- Required Design commands: focused unit suites; Zod schema integration; full npm run test:unit; npm run build:check; npm run check; npm run build; npm run verify:codex-runtime-package; plugin package/manifest verification if defined by repository rules.
- Record exact Core and Plugin commits, changed files/symbols, runtime/package entry hashes, zero-write fingerprints, and two-stage self-review (requirement compliance, then failure paths/performance/cleanup).
- Do not claim final real BiliDili/AlembicWorkspace acceptance: controller will independently reload the canonical runtime and run the fixed BiliDili Search/Prime matrix plus dual-project Graph/Map/Guard, cursor, fast-path and zero-write checks after accepting this commit. (intent: Make every public knowledge query request-scoped and reader-only, and make Graph/Map share a cancellable ephemeral fact build with honest pagination.)
- 2026-07-13T07:55:06.115Z p3-core-qwen-query-format-retrieval-quality-p1 → AlembicCore — Continue the same confirmed demand after controller real BiliDili acceptance exposed an in-scope Core defect and an unmet retrieval completion gate.

Authoritative raw evidence:
- Accepted Core commit is 7ccddf2372ddaeac2dbcfc1f651157d753709cd7; AlembicCore worktree is clean.
- Public Search and Prime candidate ordering now matches, but the frozen eight-query real BiliDili matrix places Recipe <redacted> at ranks 5, absent from Top 8, 1, 1, 1, absent from Top 8, 3, 2. Expanded Search ranks for the three failures are 7/16/17, with dense ranks 5/21/8 and sparse ranks 20/15/39.
- OllamaEmbedProvider currently sends identical raw text from embedQuery and embedDocuments and declares formatProfile=symmetric for qwen3-embedding:0.6b.
- Qwen3-Embedding's authoritative usage contract requires each query to carry a one-sentence task instruction while retrieval documents carry no instruction. Read-only probes show generic project-knowledge instructions materially improve the target dense ranks, proving the adapter defect, but the broad second query still remains below Top 3; query formatting alone is not sufficient.
- Generic English stemming/question-stopword simulation alone also does not satisfy the matrix and regresses ordering, so do not land a blind tokenizer patch.
- The target Recipe's actual authoritative content directly describes layered Features→Infrastructure→Core dependencies and forbidden feature-to-feature coupling; higher candidates for the failing broad query are mostly superficial Swift/app/module matches, so this is candidate-quality failure, not an unsupported frozen target.

Required repair:
1. Make OllamaEmbedProvider purpose handling honest and provider-adapter-owned. For Qwen3 embedding models, embedQuery must apply one stable, generic software-project-knowledge retrieval instruction in the official Qwen format; embedDocuments and legacy document ingestion must remain byte-for-byte unprefixed. The capability descriptor must expose the real asymmetric format profile/input purposes. Do not leak model-specific prompt construction above the adapter and do not require rebuilding existing document vectors.
2. Continue on the same canonical KnowledgeRetrievalPolicy chain and implement the smallest general candidate-quality correction needed for the real eight-query matrix. It must be principled for project knowledge, must preserve authoritative truth filtering, per-Recipe region aggregation, bounded refill, raw dense/sparse/RRF evidence and Search/Prime parity, and must not hardcode any frozen query term, BiliDili/SwiftPM synonym, target Recipe ID, or Recipe content. Do not tune RRF constants or add stemming unless adversarial and real evidence shows the change improves the entire matrix without degrading already-correct queries.
3. Preserve vector reader/writer isolation, provider-independent maintenance, sparse fallback when embedding is unavailable, and zero query-time writes. No Plugin/Alembic/Test/vendor/release/data edits.

Required verification:
- RED/GREEN provider tests assert exact Qwen query transport text, unchanged document transport text, honest capabilities, abort/batch behavior, and non-Qwen compatibility.
- RED/GREEN canonical retrieval tests reproduce the real failure shape rather than fabricated always-winning ranks; include irrelevant Swift/app/module candidates and all eight frozen queries without special-case branches.
- Run focused retrieval/vector tests, full Core test/check/build/public-surface gates, git show --check, and two-stage self-review.
- Run a read-only real BiliDili snapshot probe from copied DB/vector/config artifacts only (never open live alembic.db directly), reporting all eight target ranks and protected live fingerprints before/after. Do not claim final public Search/Prime acceptance; controller will verify after Plugin consumes the exact Core commit.
- Commit only AlembicCore and return exact commit, changed symbols, raw commands, runtime probe output, boundaries and residual risks. (intent: Keep model-purpose formatting inside the Core provider adapter, then improve canonical candidate quality generically at Core retrieval seams without query-specific synonyms, Recipe edits, or a Plugin-side prompt.)
- 2026-07-13T08:47:05.248Z p4-plugin-graph-recipemap-real-acceptance-defects-p1 → AlembicPlugin — 修复总控真实 BiliDili Graph/Recipe Map 验收暴露的同一 ProjectContext continuation 链缺陷：Graph 不得向 Core 发送不存在或大小写失真的 modulePath；Recipe Map rollup 必须按 nodeId 唯一；continuation 必须在 inline-byte 裁剪之前保存完整 bounded 结果，使多页 union 无重复、无缺失，并让 terminal status/conservation/region.truncated 反映完整分页后的真实状态。保持 shared fact session、5/5 repo progressive coverage、opaque cursor、只读快照、120 秒 deadline、清理与零写入边界。不要修改 Core、Recipe 数据、检索排名或插件外仓库。 (intent: Keep Core strict, reconcile Plugin module seeds to existing canonical source paths, dedupe rollups by stable node id, and paginate the complete bounded Recipe Map before applying per-page inline budgets.)
- 2026-07-13T09:54:12.344Z p5-plugin-search-fixture-schema-closure-p1 → AlembicPlugin — Close the only remaining demand-local integration regression after accepted Core schema evolution. Current AlembicPlugin HEAD must be d26fe67b3b25c653fddcca517b27de04a90ed7af and the worktree must be clean before editing. Reproduction is fixed: PublicOrphanVectorTruth.test.ts has 2 failures and ReadOnlySearchFingerprint.test.ts has 1 failure because their synthetic knowledge_entries tables omit dontClause while the accepted Core Search reader selects that field. Update only the minimal stale fixture schema/insert definitions needed for the current read contract. Do not change production Search/Prime behavior, Core, retrieval ranking, Graph/Recipe Map, real knowledge data, plugin packaging, or any repository outside AlembicPlugin. Add or retain an assertion that the fixtures exercise the real reader path rather than bypassing it. Verify the two focused files pass, then run the full Plugin unit suite and build:check; review git diff/check, commit only AlembicPlugin, and return the exact parent/commit, changed files, command summaries, and two-stage self-review. This is a test-fixture compatibility closure, not authority to reopen vector ranking or redesign retrieval. (intent: Keep current production behavior unchanged and align the two stale synthetic knowledge_entries schemas with the accepted Core read contract so the Plugin unit suite is again trustworthy.)

## Decisions And Append Log

- 2026-07-13T03:31:48.788Z dispatched p1-core-retrieval-vector-projectcontext-architecture-t1 → AlembicCore (delivery delivery-p1-core-retrieval-vector-projectcontext-architecture-p1__AlembicCore__p1-core-retrieval-vector-projectcontext-architecture-t1)
- 2026-07-13T04:21:16.376Z decision accept (candidate tc-20260713042058-0005) — Controller independently reviewed Core commit 7ccddf2372ddaeac2dbcfc1f651157d753709cd7 against the confirmed Core-first requirement: real EmbeddingPort and reader/writer adapters are publicly consumable; canonical retrieval filters authoritative truth and aggregates Recipe regions before raw RRF with bounded refill; Search/Prime/RecipeContext share the canonical port; query-side sparse fallback remains available without a provider; lifecycle mutation stays in the separate coordinator; and non-serialized AbortSignal reaches real ProjectContext I/O/DFS. Controller reran serial build plus the full 182-file/1696-test suite and all scoped public/layer/consumer/smoke/lint gates. The sole doctrine failure is inherited ASTChunker code whose HEAD and parent blobs are identical. The synthetic eight-query Core matrix is accepted only as deterministic contract coverage; real BiliDili recall remains intentionally unaccepted until Plugin integration and controller dual-project read-only acceptance.
- 2026-07-13T04:23:00.356Z dispatched p2-plugin-retrieval-vector-projectcontext-integration-t1 → AlembicPlugin (delivery delivery-p2-plugin-retrieval-vector-projectcontext-integration-p1__AlembicPlugin__p2-plugin-retrieval-vector-projectcontext-integration-t1)
- 2026-07-13T05:33:05.635Z decision rework (candidate tc-20260713053231-0009) — Controller review rejects the Plugin result because four confirmed P2 completion conditions are not implemented. (1) Full-space Graph still awaits Promise.all over every repo before producing results, so continuation is post-build array slicing rather than per-repo incremental publication and the first page cannot return while a slow repo is still running. (2) repo timeout is Promise.race only; it does not own/abort a child AbortController, so timed-out repo work continues in the background. (3) continuation records store no fact scope/fingerprint and readContinuation only checks canonical projectRoot; a controller runtime probe changed the source after page 1 and page 2 was still served (acceptedChangedSource=true). (4) explicit file/symbol fingerprinting recursively visits the entire projectRoot; a controller runtime probe changed only an unrelated source and invalidated the narrow session (unrelatedSourceInvalidatedNarrowSession=true). Existing focused tests pass 68/68 but do not exercise these four required behaviors, so green tests cannot support acceptance. Rework the same Plugin task: implement true per-repo incremental production with deterministic reconstruction, abort-and-settle repo deadlines, per-page source/fingerprint revalidation with typed retryable invalidation and cleanup, and narrow-scope fingerprinting that touches only the requested file plus necessary containing-repo manifests. Add regressions that fail the current behavior; preserve the accepted reader-only Search/Prime work and do not reopen Core or create Test.
- 2026-07-13T05:34:18.895Z dispatched p2-plugin-retrieval-vector-projectcontext-integration-t1 → AlembicPlugin (delivery delivery-p2-plugin-retrieval-vector-projectcontext-integration-rework1-p1__AlembicPlugin__p2-plugin-retrieval-vector-projectcontext-integration-t1)
- 2026-07-13T06:44:12.848Z decision rework (candidate tc-20260713064307-0012) — Controller rejects rework1 after fresh diff review and runtime probes. Three prior defects are genuinely fixed and must be preserved: continuation pages revalidate source facts, explicit file/symbol fingerprints ignore unrelated sources, and cooperative repo deadlines abort the child signal and await settlement. Two confirmed requirements remain broken because the progressive model uses mutable whole-output snapshots plus only a boolean `settled`. First, structure.ts graphStablePageEntries deliberately filters every unsettled snapshot to the project root only; the public runtime probe returned status=partial, requested=1, attempted=0, nodeTypes=[project], relationCount=0. Even when repository outcomes are already spooled, no repo node/relation/ref can be emitted until the whole build settles, so this is a placeholder page followed by post-build pagination, not public per-repository incremental facts. The new test codifies attempted:0 and never asserts that a completed repo is present before a slower repo settles. Second, BuildRecord stores no terminal error. If a producer publishes a partial snapshot and later rejects, catch marks settled=true and #waitForProgress returns the old progressValue as a successful settled snapshot. A controller runtime probe publishing {phase:partial} then throwing terminal-boom returned errorPropagated=false with settled=true. This violates session-error broadcast, honest partial truth, and exception cleanup semantics. Focused 69/69 passing confirms the tests miss these behaviors, not correctness. New root-cause hypothesis for rework2: do not treat a mutable projected snapshot and `settled` boolean as the authoritative progress protocol. Model immutable deterministic repository-outcome chunks plus an explicit terminal outcome (success/error/cancelled). Public Graph page 1 must wait for and include at least one completed repo-derived fact/outcome while another repo remains unsettled (attempted >=1 and < requested), then cursor pages consume stable-key deltas without duplicates/missing facts. A producer error after any partial publication must be stored, broadcast to every progressive waiter/cursor, invalidate the continuation, and leave zero temp state; it must never become settled success. Add RED/GREEN tests for both exact controller probes. Preserve commit 490e3dad's source-revalidation, narrow-fingerprint, deadline, Search/Prime, Core and Guard work; no Test, no new task, no scope expansion.
- 2026-07-13T06:44:47.242Z dispatched p2-plugin-retrieval-vector-projectcontext-integration-t1 → AlembicPlugin (delivery delivery-p2-plugin-retrieval-vector-projectcontext-integration-rework2-p1__AlembicPlugin__p2-plugin-retrieval-vector-projectcontext-integration-t1)
- 2026-07-13T07:35:35.515Z decision accept (candidate tc-20260713073516-0015) — Controller independently reviewed Plugin commit cbcda68722daf14fd35ef688a98c40db9f4ba420 against parent 490e3dad and the rework2 objective. The shared progressive protocol now records ordered repo-outcome events with explicit success/error/cancelled terminal truth; public Graph first output contains an attempted repository outcome rather than attempted=0 placeholder; exact terminal error reaches waiters and live cursor with zero session/continuation/temp residue. Controller probes passed, focused 159/159 and full unit 1574/1574 passed, npm run check and build:check passed against accepted Core 7ccddf2, git show --check passed and the worktree is clean. This accepts only the Plugin target; final dual-project real read-only acceptance remains controller-owned.
- 2026-07-13T07:56:26.680Z dispatched p3-core-qwen-query-format-retrieval-quality-t1 → AlembicCore (delivery delivery-p3-core-qwen-query-format-retrieval-quality-p1__AlembicCore__p3-core-qwen-query-format-retrieval-quality-t1)
- 2026-07-13T08:36:29.469Z decision rework (candidate tc-20260713083539-0019) — Controller rejects Core commit d17db939ed49ea30bdbf380f400b783530b6d6a8 because the required real BiliDili eight-query Top-3 gate still fails under the canonical read-only Qwen route. Fresh copied-snapshot public Search ranks are 1/8/1/1/1/5/2/1, and serial rechecks reproduce query 2 at rank 8 and query 6 at rank 5. Query 2 has no dense contribution in the canonical candidate window and only sparse rank 3, so five incidental dual-lane candidates outrank it; query 6 has dense rank 2 plus sparse rank 23 yet remains rank 5. This is the same in-scope candidate-quality defect, not an environment issue: Ollama qwen3-embedding:0.6b was reachable, every call used auto(canonical-hybrid), and DB/WAL/SHM/config/vector exists/size/mtimeNs/SHA-256 fingerprints were identical before/after. The new FieldWeightedScorer topic/kind heuristic is too narrow and the deterministic fixture masks the real failure by injecting favorable dense ranks. Rework must retain query-only Qwen formatting and all established truth/refill/raw-evidence/read-only boundaries, but replace or justify the insufficient candidate-quality correction with a general solution proven against the real failure shape. Do not hardcode frozen queries, Recipe id/content, BiliDili/SwiftPM synonyms, tune only to the target, edit data, rebuild vectors, or modify Plugin. Add regression evidence that would fail for the observed lane shapes, rerun full Core gates, and return one clean superseding Core commit plus a copied-snapshot eight-query run with all ranks <=3 and unchanged protected fingerprints.
- 2026-07-13T08:38:27.917Z dispatched p3-core-qwen-query-format-retrieval-quality-t1 → AlembicCore (delivery delivery-p3-core-qwen-query-format-retrieval-quality-rework1-p1__AlembicCore__p3-core-qwen-query-format-retrieval-quality-t1)
- 2026-07-13T08:41:28.690Z decision accept (candidate tc-20260713084122-0022) — 接受的是 AlembicCore 对用户停止命令的合规执行，不是宣称八查询 Top-3 排名已达标。用户已明确撤销当前排名阻塞并要求先验收 Graph、排名后续在 Recipe 生产端统一优化；目标窗口未继续开发，仓库 HEAD 仍为 d17db939ed49ea30bdbf380f400b783530b6d6a8 且工作树干净。当前应转入总控真实 Graph/Recipe Map 验收。
- 2026-07-13T08:47:48.793Z dispatched p4-plugin-graph-recipemap-real-acceptance-defects-t1 → AlembicPlugin (delivery delivery-p4-plugin-graph-recipemap-real-acceptance-defects-p1__AlembicPlugin__p4-plugin-graph-recipemap-real-acceptance-defects-t1)
- 2026-07-13T09:42:54.993Z decision accept (candidate tc-20260713094229-0026) — Controller accepts only P4 Graph/Recipe Map defect repair at AlembicPlugin commit d26fe67b3b25c653fddcca517b27de04a90ed7af. Independent diff review confirmed canonical existing project-confined module seeds, nodeId-unique rollups, and complete bounded facts persisted before per-page inline budgeting. Focused Graph/Map/session/schema tests passed 158/158; build:check and build passed. Independent public EmbeddedToolExecutor probes passed for BiliDili (Graph 5/5, 80 stable facts, Recipe Map 172 stable facts, 41/41 mounts) and AlembicWorkspace (Graph 5/5, 80 stable facts, Recipe Map 137 stable facts, 15/15 mounts), with zero duplicate facts, no invalid-scope/error/timeout, shared fact sessions, terminal conservation, pages below 20 KiB, and unchanged DB/WAL/SHM/config/vector fingerprints. This acceptance does not claim the full unit suite is green: three Search fixture tests still fail because synthetic schemas lack dontClause; P4 changed no Search/Prime/fixture files, so that residual is not accepted as solved and remains a demand-closeout risk.
- 2026-07-13T09:55:48.346Z dispatched p5-plugin-search-fixture-schema-closure-t1 → AlembicPlugin (delivery delivery-p5-plugin-search-fixture-schema-closure-p1__AlembicPlugin__p5-plugin-search-fixture-schema-closure-t1)
- 2026-07-13T10:08:09.957Z decision accept (candidate tc-20260713100750-0030) — P5 is a minimal test-fixture schema compatibility repair: independent review confirmed commit 79f46cda has parent d26fe67b, changes only the two named synthetic fixture files by adding nullable dontClause declarations, leaves production code untouched, and preserves a clean worktree. Controller reran the focused 2-file/4-test suite, full Plugin unit suite, build:check, and diff check successfully.
- 2026-07-13T10:09:13.347Z demand completed — All five target tasks are accepted. Core and Plugin architecture/integration are landed; controller dual-project public Graph/Recipe Map probes passed with deterministic pagination, shared fact sessions, complete repository coverage, zero duplicate/missing stable facts, bounded page sizes, clean terminal continuation, and unchanged DB/WAL/SHM/config/vector fingerprints. P5 restored the full Plugin unit suite with test-fixture-only schema compatibility. This completion explicitly does not claim the withdrawn eight-query Top-3 ranking gate: the user directed that ranking be deferred to later Recipe producer optimization and that current Graph/Map work be closed and archived first.
- 2026-07-13T10:09:37.150Z archived → wakeflow-ledger/workspace/archive/2026-07/alembic-retrieval-vector-projectcontext-architecture-mainline1-2026-07-13 — Demand completed at revision 32 after 5/5 task acceptance and independent controller verification. Archive now so later Recipe producer ranking optimization is handled separately and is not conflated with this closed Graph/Map and retrieval-architecture demand.

## Backfill Summaries

- 2026-07-13T04:16:34.945Z AlembicCore/p1-core-retrieval-vector-projectcontext-architecture-t1 returned completed (result tr-p1-core-retrieval-vector-projectcontext-architecture-t1)
- 2026-07-13T05:25:13.846Z AlembicPlugin/p2-plugin-retrieval-vector-projectcontext-integration-t1 returned completed (result tr-p2-plugin-retrieval-vector-projectcontext-integration-t1)
- 2026-07-13T06:33:34.330Z AlembicPlugin/p2-plugin-retrieval-vector-projectcontext-integration-t1 returned completed (result tr-p2-plugin-retrieval-vector-projectcontext-integration-t1)
- 2026-07-13T07:29:00.032Z AlembicPlugin/p2-plugin-retrieval-vector-projectcontext-integration-t1 returned completed (result tr-p2-plugin-retrieval-vector-projectcontext-integration-t1)
- 2026-07-13T08:30:51.202Z AlembicCore/p3-core-qwen-query-format-retrieval-quality-t1 returned needs-review (result tr-p3-core-qwen-query-format-retrieval-quality-t1)
- 2026-07-13T08:39:52.954Z AlembicCore/p3-core-qwen-query-format-retrieval-quality-t1 returned needs-review (result tr-p3-core-qwen-query-format-retrieval-quality-t1)
- 2026-07-13T09:32:38.266Z AlembicPlugin/p4-plugin-graph-recipemap-real-acceptance-defects-t1 returned completed (result tr-p4-plugin-graph-recipemap-real-acceptance-defects-t1)
- 2026-07-13T10:03:38.110Z AlembicPlugin/p5-plugin-search-fixture-schema-closure-t1 returned completed (result tr-p5-plugin-search-fixture-schema-closure-t1)
