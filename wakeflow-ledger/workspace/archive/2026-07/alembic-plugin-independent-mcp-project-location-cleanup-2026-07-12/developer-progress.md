# AlembicPlugin 独立 MCP 与单一项目定位清理 进度

## 统一状态

<!-- unified-status:start -->
需求: alembic-plugin-independent-mcp-project-location-cleanup-2026-07-12 - AlembicPlugin 独立 MCP 与单一项目定位清理
主状态: planned
阶段: 无
当前任务包: p1-plugin-independent-mcp-project-location-cleanup-p1(pending)
窗口: AlembicPlugin(pending)
阻塞项: 无
下一步: prepare-dispatch-from-state, add-task-package, wakeflow-render-progress
评审: 无
自动化: 未启用
需要用户决策: 无
最后更新: 2026-07-12 17:49 CST
来源状态: revision 3 / event evt-20260712094928-0003
<!-- unified-status:end -->

## 目标

Make AlembicPlugin MCP fully independent from the Alembic main application and runtime selection. Every call uses exactly one request-scoped projectRoot/ghost/dataRoot/database locator; public tools are always visible and return tool-native empty/not-found results when knowledge is absent, with no host state, revision, intent, role or availability gate blocking project knowledge queries.

## 完成定义

1) No AlembicPlugin MCP production path reads or reacts to Alembic main selected/active/runtime-control, resident/daemon availability, handoff or cross-project fallback. 2) One project-location service is the sole resolver of projectRoot/projectId/ghost/dataRoot/databasePath/databaseExists and all consumers use it. 3) All public MCP tools are always listed/callable; missing database/knowledge/match returns truthful tool-native empty/not-found/unavailable results without auto-init, visibility/prebootstrap/readiness gates or repair choreography. 4) alembic_task and old daemon-bridge residue are absent from production/public contracts. 5) Status and five knowledge tools have zero Git revision/checkpoint consumer gates or fields; Git checkpoint remains only in proven Recipe evolution maintenance consumers. 6) Prime has no intent admission or required intent frame and no host-level trust/readiness gate that suppresses queryable Recipe/Guard material. 7) Admin/tier/role visibility machinery is removed and there is one ordinary tool surface. 8) Plugin write provenance uses one canonical actual producer source without the duplicate legacy normalizer or caller spoofing. 9) Centralized project/data-root confinement and explicit write/destructive protections remain; no per-tool duplicate identity policy or cross-project fallback remains. 10) Focused tests, full unit/check/build/runtime/distribution/plugin verification pass; AlembicPlugin owns and commits the complete diff; controller reviews raw evidence, refreshes the local plugin, and directly runs real five-tool calls for explicit AlembicWorkspace and BiliDili roots after restart, with no Test window.

## 阶段计划

P1 AlembicPlugin performs one combined implementation package in the required order: caller inventory and failing regressions; single project-location service; migration/removal of main-resident-daemon, knowledge-state/visibility, revision, Prime intent/trust, admin/tier, alembic_task and legacy source chains; dead surface cleanup; focused and full gates; one repository commit. P2 controller independently reviews the complete diff and raw evidence, reruns required gates, refreshes the local plugin, then directly executes status plus the five real tools against explicit AlembicWorkspace and BiliDili roots after the user restarts Codex. No Test window or synthetic environment.

## 任务包

## 回填摘要

## 决策和追加日志

## Task Packages

- 2026-07-12T09:49:28.786Z p1-plugin-independent-mcp-project-location-cleanup-p1 → AlembicPlugin — Implement the user's authoritative AlembicPlugin independence architecture as one combined repository change and commit.

Current phase goal:
- Make AlembicPlugin MCP a standalone plugin runtime whose only project identity is the current call's explicit projectRoot (or the current host workspace root when omitted), resolved through exactly one project-location service into projectId, ghost mode, dataRoot, databasePath and database existence.
- Completely remove Alembic main selected/active/runtime-control influence and remove Plugin MCP dependence on Alembic resident/daemon availability, project selection, handoff, fallback probing or status. Do not modify Alembic main, AlembicCore, Agent, Dashboard or BiliDili.
- All public MCP tools remain visible and callable regardless of knowledge initialization/usability/freshness. Remove HostKnowledgeState-driven visibility, knowledge/tool policy, preflight knowledge gate and agent-public prebootstrap isolation block. If the request project has no database, no knowledge, or no match, each tool returns its own truthful empty/not-found/unavailable result without auto-init, blocking, cross-project fallback, stale downgrade or invented repair flow.
- Centralize projectRoot/ghost/dataRoot/databasePath resolution and physical confinement in one service used by status, initialization, query executors, skill/workflow paths and runtime composition. Remove duplicate per-handler identity validation after the service returns a resolved location. Keep only centralized path confinement and explicit destructive-write confirmation; do not create multiple new policy layers.
- Delete all selected/active mismatch/handoff fields, actions, diagnostics, onboarding states and bootstrap/rescan response metadata, including HostProjectAlignment/buildLocalSelectionMismatch and resident selected/active scope fallback.
- Delete old daemon MCP bridge residue. Preserve only a Plugin-owned embedded execution path if it has a real current caller; remove obsolete bridge adapters/comments/tests and all Alembic main/resident routing from public query execution.
- Delete alembic_task completely: retired intercepts, error taxonomy entries used only by it, ServiceRequestBoundary special cases, legacy policy/classification residues, prompt/docs/catalog/test references and compatibility tests. Do not replace it with another hidden route.
- Remove every MCP-consumer Git revision/checkpoint gate and public field: status/search/get/expand/graph/recipe-map/prime/guard must not read HEAD/checkpoint posture, attach gitDiffCheckpoint/sourceRevisionManifest, downgrade/suppress results, clear materials, remember manifests or demand rescan because commits differ. Keep Git diff/checkpoint code only where it is actually consumed by internal Recipe evolution/incremental maintenance; prove no public query consumer remains.
- Remove all Prime intent admission: obsolete intent rejection, taskAction/requirementGoal/locator-facet requirements, automation-envelope/sourceRefs requirement, intentKind non-code blocking, low-information intent suppression and related schemas/reasons/tests. Prime consumes optional query/context solely to retrieve and present project knowledge; absence of intent is not a blocker.
- Query tools may validate only executable input shape and use the centralized project location. Remove external readiness/trust/freshness checks that suppress otherwise queryable Recipe/Guard results. Recipe lifecycle/content/schema/sourceRefs and tool-local matching/ranking/display facts remain data semantics, not host-level availability gates.
- Remove Admin/tier/role visibility machinery (ALEMBIC_MCP_TIER, ALEMBIC_CODEX_ENABLE_ADMIN, effective tier, admin diagnostics/preflight/catalog tier filtering). Expose one ordinary tool surface. Keep or delete knowledge lifecycle only by real current capability, but it must not require an Admin role.
- Review the Plugin write-source normalizer. The current caller is the Plugin host-agent submission route; remove duplicate legacy source normalization and caller spoofable source overrides, using one canonical actual producer source at the write boundary or the existing Core canonical helper. It must not affect read/query behavior.
- Remove associated obsolete schemas, projections, descriptions, skills/prompts, tests and dead code; do not leave comments asserting deleted architecture.

Explicit non-goals:
- No Test window, no new synthetic environment, no edits to real BiliDili/AlembicWorkspace knowledge data.
- No Alembic main/Core/Agent/Dashboard product edits.
- Do not remove centralized projectRoot/dataRoot physical confinement, request input schema needed for execution, Guard explicit code/files scope, Recipe-owned lifecycle/schema/content rules, destructive rebuild/cleanup confirmation, or write-zone/path safety merely because host-level gates are removed.
- Do not create a universal policy framework, replacement state machine, hidden fallback, automatic bootstrap/rescan, or new status vocabulary.

Required implementation order:
1. Inventory real current callers and add failing regressions for project-location independence, always-visible/no-KB behavior, no revision gate, intent-free Prime, one public tier and complete alembic_task absence.
2. Land the single project-location service and migrate all consumers.
3. Remove main/resident/daemon/visibility/state/revision/intent/admin/legacy-source chains.
4. Remove dead schemas/projections/tests/docs and run a production import scan.
5. Commit the complete AlembicPlugin change.

Required validation and evidence:
- Focused before/after regressions proving foreign selected/active/runtime-control files cannot change any MCP identity or response; no knowledge returns tool-native empty/not-found; an advanced/dirty HEAD does not change public knowledge results; Prime works with minimal/no intent frame; all tools list without role/tier env; alembic_task and old bridge symbols have zero live/public callers.
- Request-root/ghost isolation tests proving AlembicWorkspace and BiliDili projectRoot values resolve distinct projectId/dataRoot/databasePath using the single service, with zero cross-project fallback.
- Exact full repository gates: npm run test:unit, npm run check, npm run build, npm run verify:codex-runtime-package, npm run verify:plugin-distribution, npm run verify:codex-plugin (or report exact unavailable script name as a blocker, never silently skip).
- Return touched files/symbols, deleted-chain inventory, production import/caller scans, focused/full raw summaries, representative public JSON for no-KB and knowledge-present cases, final commit hash and two-stage self-review.
- Completion leads to controller diff/raw-evidence review, local Plugin refresh and controller-owned direct real five-tool calls against explicit AlembicWorkspace and BiliDili projectRoot values after restart. No Test dispatch. (intent: Replace scattered host-state gates with one request-scoped project locator and make every public Plugin MCP call independent, always callable and truthful by its own result semantics.)
- 2026-07-12T12:04:34.229Z p2-plugin-deferred-request-root-bootstrap-p1 → AlembicPlugin — Fix the verified in-scope MCP startup defect discovered by controller-owned local refresh. The installed cache was successfully rewritten to the accepted local build, but `npm run dev:codex-plugin:reload -- --project-root <workspace-root>` failed its fresh-process probe with `MCP error -32000: Connection closed`. Direct raw reproduction from the installed plugin-cache cwd exits before MCP connect with `Codex MCP Server failed to start: Project root points inside the Codex plugin cache.` Root cause: `HostMcpServer` resolves and rejects a default project root in its constructor/startup before any tool request exists, so a valid explicit per-call `projectRoot` can never be received. This violates the confirmed architecture that Plugin MCP identity belongs to the current call and must not depend on Alembic main or the plugin installation cwd.

Required behavior:
- The MCP process must start, initialize and list its static tools when launched from the installed plugin cache cwd, without treating that cache as project identity or persistence location.
- A tool call carrying explicit `projectRoot` must create/use a request-scoped runtime resolved only through `ProjectLocationService` and return that root's projectId/dataRoot/databasePath; the bootstrap server context must not reject or override it first.
- If a call omits `projectRoot`, use a valid current host workspace candidate only when one exists. If none exists or the only candidate is the plugin cache/package root, return a truthful tool-level project-root-required error rather than killing the MCP process, selecting Alembic main state, falling back across projects, initializing data, or writing into the plugin cache.
- Preserve the accepted static catalog, five public tool semantics, centralized physical confinement, destructive-write confirmation, Recipe-internal Git maintenance and all previously removed coupling boundaries.
- Fix the fresh probe error reporting so child MCP stderr is retained when startup fails, if needed to keep the failure diagnosable; do not add process lifecycle management.

Validation:
- Add a subprocess/MCP regression that starts from a cache-shaped cwd with no trusted workspace env, connects/lists tools, then calls `alembic_status` with an explicit temporary/request root and proves request identity/storage use that root and never the cache.
- Add omitted-root coverage proving the process remains alive and returns a tool-native project-root-required result rather than connection close.
- Re-run `npm run dev:codex-plugin:reload -- --project-root <workspace-root>`; it must report ok=true and a fresh `alembic_status` readback from the rewritten installed cache.
- Run focused tests, `npm run test:unit`, `npm run check`, `npm run build`, `npm run verify:codex-runtime-package`, `npm run verify:plugin-distribution`, and `npm run verify:codex-plugin` on the exact final commit.
- Return exact raw hashes, clean worktrees, startup stderr before/after evidence, reload report path/summary and representative explicit-root JSON. No Test dispatch. (intent: Defer project identity from MCP process startup to each tool request: the transport and static catalog may start without a project, while every actual operation resolves its explicit or valid host-workspace root through the single ProjectLocationService.)

## Decisions And Append Log

- 2026-07-12T09:50:57.577Z dispatched p1-plugin-independent-mcp-project-location-cleanup-t1 → AlembicPlugin (delivery delivery-p1-plugin-independent-mcp-project-location-cleanup-p1__AlembicPlugin__p1-plugin-independent-mcp-project-location-cleanup-t1)
- 2026-07-12T11:30:21.437Z decision rework (candidate tc-20260712113004-0005) — Controller raw-evidence review rejects acceptance despite green focused/full gates. The deletion/caller scan was too narrow and missed user-visible remnants of the architecture the user explicitly removed: lib/host-runtime/status/OnboardingContract.ts still emits a scope gate requiring projectRoot/dataRoot to match the active Codex host project; docs/declared-effects.md still says Plugin consults Alembic-owned runtime-control.json for alignment; and lib/host-runtime/mcp/host/tool-visibility.ts plus getVisibleTools(...ignoredLegacyPolicyArguments) remains an obsolete visibility compatibility layer, with HostMcpServer still passing ignored legacy arguments. Rework must remove those active-project/runtime-control assertions and replace the visibility compatibility layer with a plain static tool catalog API with no legacy policy arguments, then scan all production/docs/skills/scripts (not a curated file list) for selected/active/runtime-control alignment and tool-visibility residue, rerun focused independence tests, npm run check, npm run test:unit, npm run build, and the three exact plugin verification commands, and return a new commit plus raw scan evidence. Preserve the already-correct request-scoped location service, public tool behavior, path confinement, destructive-write protections, and Recipe-internal Git evolution logic.
- 2026-07-12T11:30:49.827Z dispatched p1-plugin-independent-mcp-project-location-cleanup-t1 → AlembicPlugin (delivery delivery-p1-plugin-independent-mcp-project-location-cleanup-rework1-p1__AlembicPlugin__p1-plugin-independent-mcp-project-location-cleanup-t1)
- 2026-07-12T11:47:34.805Z decision rework (candidate tc-20260712114715-0008) — Implementation review is provisionally clean, but the TargetResultEnvelope conflicts with raw VCS evidence and cannot be accepted. It declares parent commit 95fadcf50b722b5831bc37a301098a82ff20a579 and Codex shell commit 03ed6e65a58a55a8295126437824b301ba369ce5; neither object exists in the checked-out repositories. Raw git rev-parse reports parent HEAD 95fadcf8c2b56c23472e7b36105a00f153a90169 and Codex shell HEAD 03ed6e6a6e2b676a9faecad5c6188b03ed1e7bc9, with clean worktrees. New root-cause hypothesis: the backfill expanded short hashes synthetically instead of copying command output. This is evidence repair only: do not change product code or create a new commit unless the worktree is unexpectedly dirty. Re-read raw git rev-parse HEAD and git -C plugins/alembic-codex rev-parse HEAD, verify both objects with git cat-file, then return a new TargetResultEnvelope whose change-scope/commit fields and summary use the exact real hashes, explicitly state that no code changed during evidence repair, and preserve the prior full scan/test evidence. Controller independently confirmed zero rejected residue and reran the two focused files: 16/16 tests passed.
- 2026-07-12T11:47:56.192Z dispatched p1-plugin-independent-mcp-project-location-cleanup-t1 → AlembicPlugin (delivery delivery-p1-plugin-independent-mcp-project-location-cleanup-rework2-p1__AlembicPlugin__p1-plugin-independent-mcp-project-location-cleanup-t1)
- 2026-07-12T11:51:25.284Z decision rework (candidate tc-20260712115101-0011) — Controller rejects the result on fresh raw full-suite evidence. Although the corrected Git hashes now match and the evidence-only rework changed no code, npm run test:unit on the actual final HEAD fails: test/unit/CodexDevReloadScript.test.ts > help text does not advertise current MCP lifecycle flags. The help still has the correct meaning, but line wrapping inserts a newline between 'stops,' and 'or restarts', while the assertion requires one contiguous phrase; suite result is 133/134 files and 1520/1521 tests. New root-cause hypothesis for this recurring task: the previous implementation result preserved a pre-final/stale full-suite claim instead of executing the full unit suite on the final committed tree, so a brittle help-text assertion introduced by the rework escaped validation. This is a bounded code/test defect, not a requirement mismatch: repair only the help-text/assertion seam so whitespace formatting cannot invalidate the intended statement that AlembicPlugin never manages the current Codex MCP lifecycle; do not reopen the MCP architecture or five-tool chain. On the final new commit, run the focused CodexDevReloadScript test, npm run test:unit, npm run check, npm run build, verify:codex-runtime-package, verify:plugin-distribution, and verify:codex-plugin; copy exact exit summaries and raw git rev-parse hashes into the next result. Do not preserve old green evidence as a substitute for final-tree execution.
- 2026-07-12T11:51:43.156Z dispatched p1-plugin-independent-mcp-project-location-cleanup-t1 → AlembicPlugin (delivery delivery-p1-plugin-independent-mcp-project-location-cleanup-rework3-p1__AlembicPlugin__p1-plugin-independent-mcp-project-location-cleanup-t1)
- 2026-07-12T12:02:36.636Z decision accept (candidate tc-20260712120215-0014) — Spec compliance and implementation quality are accepted for the AlembicPlugin target. Controller reviewed the full architecture diff and rework increments against the user's confirmed Plugin independence requirements: request-scoped ProjectLocationService is the sole MCP identity path; Alembic main selected/active/runtime-control, resident/daemon routing, visibility/readiness, public Git checkpoint, Prime intent, admin/tier, alembic_task, stale onboarding/docs and legacy visibility catalog residues are removed; centralized confinement, explicit destructive protections and Recipe-internal Git maintenance remain. Controller independently verified exact final commit bf47f9939f0fd455ad1d022faebb12cad5a478f1 and nested shell objects, clean worktrees, zero rejected shipped residue, focused request-identity/status/reload regressions, full unit 134/134 files and 1521/1521 tests, npm run check, npm run build, runtime-package entrypoint probe, plugin distribution and Codex plugin verification. No Test window is needed. Target result is accepted, but the demand is intentionally not complete: controller must refresh the local plugin, obtain a Codex restart, and directly call the five tools against explicit AlembicWorkspace and BiliDili roots before final completion/archive.
- 2026-07-12T12:05:24.097Z dispatched p2-plugin-deferred-request-root-bootstrap-t1 → AlembicPlugin (delivery delivery-p2-plugin-deferred-request-root-bootstrap-p1__AlembicPlugin__p2-plugin-deferred-request-root-bootstrap-t1)
- 2026-07-12T12:30:41.226Z decision accept (candidate tc-20260712123021-0018) — Spec compliance and quality both pass. Controller reviewed commit 981d5e449af2a41a368faf82dd71636447f71ab5 and its five-file diff: MCP startup no longer resolves a rejected cache cwd into project identity; explicit projectRoot is resolved per request through ProjectLocationService; omitted root returns PROJECT_ROOT_REQUIRED while transport remains usable; no five-tool business logic, Alembic main coupling, cross-project fallback, cache persistence, lifecycle management, or protected safety boundary was added. Controller independently reran focused subprocess tests (3 files/16 tests), full unit (135 files/1524 tests), check (zero errors), build, runtime package, distribution and plugin verifiers, then reran the canonical local reload. Fresh installed-cache probe returned ok=true on the exact commit, with explicit AlembicWorkspace root/storage readback, omitted-root tool error, and explicit-root success afterward. Worktree is clean. Demand is not yet complete: current Codex must restart, then controller must directly exercise the five knowledge tools against explicit AlembicWorkspace and BiliDili roots.
- 2026-07-12T12:38:13.138Z demand completed — All AlembicPlugin tasks are accepted and the controller-owned post-restart real-project acceptance loop is complete. In the restarted Codex host, request-scoped alembic_status resolved AlembicWorkspace to projectId ecf32806/dataRoot workspace ecf32806 and BiliDili to projectId 02a25032/dataRoot workspace 02a25032, both with existing databases and no host-selection mismatch, revision gate, role gate, intent admission, transport close, or cross-project fallback. The controller directly called the five knowledge tools against both explicit roots: Prime returned project-specific knowledge; Recipe Map returned Alembic/Plugin mounts for AlembicWorkspace and Swift/iOS mounts for BiliDili; Search returned exact direct Recipe matches in both roots; Graph exact-file queries returned HostMcpServer and AppDelegate symbols with complete repo coverage; Guard checked one AlembicPlugin TypeScript file and two BiliDili Swift files with complete file coverage, applicable project Recipes, and zero violations. Honest quality limits remain visible but do not block tool usability: BiliDili reports some unresolved legacy source refs, vector-unavailable concept search has weak recall until using an exact map/title/ref, and broad free-text graph ranking can be noisy while exact file queries are ready. No knowledge data was mutated and no Test window was used.
- 2026-07-12T12:38:44.580Z archived → wakeflow-ledger/workspace/archive/2026-07/alembic-plugin-independent-mcp-project-location-cleanup-2026-07-12 — Archive completed AlembicPlugin independent MCP project-location cleanup after controller-owned post-restart dual-project five-tool acceptance.

## Backfill Summaries

- 2026-07-12T11:24:48.488Z AlembicPlugin/p1-plugin-independent-mcp-project-location-cleanup-t1 returned needs-review (result tr-p1-plugin-independent-mcp-project-location-cleanup-t1)
- 2026-07-12T11:45:29.112Z AlembicPlugin/p1-plugin-independent-mcp-project-location-cleanup-t1 returned needs-review (result tr-p1-plugin-independent-mcp-project-location-cleanup-t1)
- 2026-07-12T11:46:44.595Z AlembicPlugin/p1-plugin-independent-mcp-project-location-cleanup-t1 returned needs-review (result tr-p1-plugin-independent-mcp-project-location-cleanup-t1)
- 2026-07-12T11:48:34.323Z AlembicPlugin/p1-plugin-independent-mcp-project-location-cleanup-t1 returned needs-review (result tr-p1-plugin-independent-mcp-project-location-cleanup-t1)
- 2026-07-12T11:56:48.058Z AlembicPlugin/p1-plugin-independent-mcp-project-location-cleanup-t1 returned needs-review (result tr-p1-plugin-independent-mcp-project-location-cleanup-t1)
- 2026-07-12T12:23:13.362Z AlembicPlugin/p2-plugin-deferred-request-root-bootstrap-t1 returned needs-review (result tr-p2-plugin-deferred-request-root-bootstrap-t1)
