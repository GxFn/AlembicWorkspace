# AlembicPlugin 五工具向量默认能力与检索质量修复 进度

## 统一状态

<!-- unified-status:start -->
需求: alembic-plugin-five-tool-vector-quality-2026-07-12 - AlembicPlugin 五工具向量默认能力与检索质量修复
主状态: planned
阶段: 无
当前任务包: p1-plugin-five-tool-vector-quality-p1(pending)
窗口: AlembicPlugin(pending)
阻塞项: 无
下一步: prepare-dispatch-from-state, add-task-package, wakeflow-render-progress
评审: 无
自动化: 未启用
需要用户决策: 无
最后更新: 2026-07-12 20:51 CST
来源状态: revision 3 / event evt-20260712125102-0003
<!-- unified-status:end -->

## 目标

Make the five AlembicPlugin knowledge tools genuinely high-quality on each explicit request project: local vector retrieval is a default local-first Plugin capability, public Search actually uses the real SearchEngine and honors retrieval modes, Prime reports the real retrieval lane, Graph ranks multi-term intent rather than common-token noise, Recipe Map reports stale references without duplicate noise, and Guard/precise structure behavior remains correct.

## 完成定义

1) `vector.localEmbedding` defaults to local-first auto-detection enabled for new and config-absent Plugin projects; an explicit config/env opt-out still forces keyword-only, and absent Ollama/model degrades quickly and honestly without downloads, external cloud APIs, Alembic main, resident, daemon, or request blocking. 2) Public `alembic_search` routes search operations through the request-scoped read-only SearchEngine/vector snapshot, honors auto/keyword/semantic, preserves get/expand, returns bounded clean output with real route/diagnostic truth, and never writes the live DB/WAL/index. 3) On BiliDili, the conceptual SwiftPM module-isolation query returns the known Feature-isolation and layered-dependency Recipes through the public MCP tool; on AlembicWorkspace with no explicit localEmbedding config but available local Ollama/model, default auto-detection uses the vector lane. 4) Prime derives vector-used/keyword-fallback diagnostics from actual SearchEngine evidence; it must not emit `prime-vector-evidence-unavailable` merely because `regionEvidence` is empty. 5) Graph multi-term ranking rewards term coverage/specific identifiers so HostMcpServer/ProjectLocationService-intent results outrank generic `request` nodes while exact file-symbol queries remain correct. 6) Recipe Map deduplicates equivalent unresolved source-ref diagnostics and returns a bounded actionable reconciliation/rescan hint without mutating knowledge. 7) Guard continues to use project Recipes with complete explicit-file coverage. 8) AlembicPlugin owns one committed diff; focused tests, full unit/check/build/runtime-package/distribution/plugin verification, local plugin reload, restart, and controller direct dual-project five-tool validation pass. No Test window and no mutation/rescan of the real knowledge bases during acceptance.

## 阶段计划

P1: one combined AlembicPlugin package implements default local-first vector selection, public SearchEngine routing, truthful Prime diagnostics, multi-term Graph ranking, Recipe Map diagnostic dedupe/actionability, and regression coverage. P2: controller reviews raw diff/evidence, reruns all gates, refreshes the local plugin, requests one Codex restart only after the fresh-process probe passes, then directly validates all five tools on explicit AlembicWorkspace and BiliDili roots. No Test window.

## 任务包

## 回填摘要

## 决策和追加日志

## Task Packages

- 2026-07-12T12:51:02.023Z p1-plugin-five-tool-vector-quality-p1 → AlembicPlugin — Implement and commit one AlembicPlugin quality closure for the five knowledge tools.

Required fixes:
- Change local embedding from explicit opt-in to default local-first auto-detection. `resolveLocalEmbeddingConfig` and newly generated Plugin config must default enabled; explicit `ALEMBIC_LOCAL_EMBEDDING_ENABLED=0`, config `enabled:false`, or `laneOrder:keyword-only` remains authoritative. Do not package/download a model or call cloud providers. Probe the existing default local Ollama endpoint/model with a bounded timeout; unavailable service/model must immediately and honestly fall back to keyword.
- Replace public `alembic_search` search-operation table-list/all-token filtering with the already-created request-scoped read-only SearchEngine. Honor `mode=auto|keyword|semantic`, query, filters and budget/limit; keep get/expand bounded through the read-only repository. Project identity and read-only snapshot confinement stay intact. Output must distinguish real semantic/vector use, keyword fallback, zero match, and unavailable vector lane without leaking internal/raw provider data.
- Carry actual SearchEngine route evidence through PrimeSearchPipeline and build Prime diagnostics from it. Delete the false rule that equates empty regionEvidence with unavailable vector; retain semantic-region evidence as a separate signal if useful.
- Improve Plugin ProjectGraphProvider ranking: normalize CamelCase/specific identifiers, reward multi-term coverage/rare or specific term matches, and prevent one common term such as `request` from filling the top results ahead of HostMcpServer/ProjectLocationService matches. Do not turn Graph into semantic knowledge search or claim source-of-truth.
- Deduplicate Recipe Map diagnostics for equivalent path anchors such as `:330` and `:330-330`; add a bounded nextAction explaining source-ref reconciliation/rescan when unresolved/stale refs exist. Query remains read-only and must not repair live knowledge.
- Preserve Guard semantics, exact graph/file queries, Recipe Map conservation counts, all public schema/output projection gates, request-scoped roots, no-KB truthful results, no revision/host/admin/intent gates, and no main/resident/daemon coupling.

Required evidence and validation:
- RED/GREEN tests proving current public semantic Search zero-result bug despite underlying vector success, then public success through SearchEngine.
- Tests for config absent => auto-detect enabled; explicit disable => no probe/keyword; Ollama absent/model absent => bounded keyword degrade; Ollama available => vector.
- Prime vector-used and keyword-fallback diagnostic tests based on real route evidence.
- Graph ranking fixture with HostMcpServer/ProjectLocationService/request terms and Recipe Map duplicate diagnostic fixture.
- Read-only snapshot/WAL/index hash or fingerprint proof before/after.
- Focused tests, full `npm run test:unit`, `npm run check`, `npm run build`, `npm run verify:codex-runtime-package`, `npm run verify:plugin-distribution`, `npm run verify:codex-plugin`, and canonical local reload on exact final commit.
- Return final commit, touched files/symbols, representative public JSON for both roots/modes, route diagnostics, no-write proof, two-stage self-review, and any honest residual limits. No Test dispatch. (intent: Repair the already-proven Plugin retrieval chain rather than adding another policy layer: use the existing request-scoped snapshot and Core SearchEngine/vector APIs, make local Ollama auto-detection the default with honest fallback, and improve public evidence/ranking while preserving read-only project isolation.)
- 2026-07-12T15:37:16.350Z p3-plugin-five-tool-truth-graph-drift-quality-p1 → AlembicPlugin — Phase P3: integrate accepted Core vector truth filtering and close the two Plugin quality defects exposed by real five-tool calls.

Upstream dependency:
- Consume sibling AlembicCore commit 9e499809b3721a0d49e78853db4e115515b281ac through the existing @alembic/core file dependency. Build against that exact Core. Do not update vendor/AlembicCore or release pointers.

Included mainline:
1. Public Search/Prime truth integration
- Add Plugin public-route tests where the vector index returns a mixture of live and orphan/deprecated entry ids while the request-scoped read-only database contains only live rows.
- Prove alembic_search semantic/auto and alembic_prime never expose orphan ids/titles, surviving live ordering/limits remain useful, route evidence stays honest, and filtered-orphan metadata/diagnostics are bounded and do not become a gate.
- Verify public get for a filtered orphan remains not found. Do not repair/rebuild the index during query.

2. Real multi-repository Graph candidate quality
- Fix ProjectGraphProvider candidate construction, not only final-node scoring: rank module seeds and file-flow targets by exact/full identifier and multi-term coverage before applying bounded target caps.
- Add a large multi-repo/noise fixture exceeding the existing candidate cap. Query `HostMcpServer ProjectLocationService request` must include the actual HostMcpServer and ProjectLocationService file/symbol nodes within the bounded top results; weak `request`/`service` matches must not exclude them before final ranking.
- Avoid collecting unrelated module-layer contexts solely because of weak terms when stronger exact candidates exist, so unrelated cyclic/invalid-scope warnings do not degrade this focused query. Preserve exact file queries, budgets, source-of-truth=false, and structural—not semantic knowledge—Graph behavior.

3. Recipe Map drift truth
- Extend Plugin Recipe source-ref normalization to understand Core `status=drifted` instead of converting it to `unresolved`.
- A drifted ref means the file exists but anchored content changed: keep it eligible for deterministic path mounting, emit a bounded drift-specific diagnostic/nextAction, and never label it missing/unresolved. Preserve stale, renamed, unresolved and metadata-only semantics.
- Keep equivalent single-line anchors such as `:330` and `:330-330` deduplicated for the same recipe; do not collapse distinct recipes sharing a source location.
- Query remains read-only and must not rescan, repair, rewrite or gate knowledge visibility.

Non-goals and boundaries:
- No Git revision/checkpoint, host-project, current Alembic selection, role/admin, intent, knowledge-state, tool-visibility or no-KB gates.
- No changes to Alembic main, AlembicCore, AlembicAgent, Dashboard, Test, real project files, live databases/vector indexes, plugin marketplace version, or vendor snapshots.
- Do not broaden into keyword ranking changes: earlier keyword/semantic disagreement was caused by orphan semantic entries, not proven keyword scorer failure.

Validation/backfill:
- Commit the AlembicPlugin change.
- Run focused Search public-route/Prime/Graph/Recipe Map/read-only tests; full unit suite; pnpm check; pnpm build; verify Codex runtime package/plugin distribution/Codex plugin; canonical local reload on the exact final Plugin commit.
- After reload, controller will run real AlembicWorkspace and BiliDili MCP calls. Target should return fixture public JSON and may run its own safe fresh-process probes, but must not mutate real knowledge.
- Backfill final commit, exact Core provenance, changed files/symbols, RED/GREEN evidence, public JSON samples, no-write proof, full commands, two-stage self-review, and honest residual limits.

Completion means the Plugin consumer chain is ready for controller real-project five-tool acceptance; it does not complete the demand and does not dispatch Test. (intent: Keep all five tools request-scoped and read-only: consume Core database-truth filtering, repair Plugin candidate selection before Graph caps, and carry Core drift semantics honestly instead of adding readiness or maintenance gates.)
- 2026-07-12T17:01:18.857Z p4-core-swiftpm-target-path-truth-p1 → AlembicCore — Phase P4 producer repair: fix the verified Core ProjectContext RepoContext truth defect that blocks the existing five-tool Graph completion. In a real SwiftPM root package, Package.swift target declarations with explicit paths (for example Home -> Sources/Features/Home and VideoPlay -> Sources/Features/VideoPlay) currently project target refs with filePath `.` while only a broad `Sources` sourceRoot survives. Preserve each main-package target's normalized declared path in RepoContext target refs so downstream consumers can construct module/file-flow candidates without parsing Package.swift again.

Required behavior:
- Add a BiliDili-shaped deterministic Core regression with multiple root-package targets under nested `Sources/Features/*`, several local path packages under `Packages/*`, test targets, and noisy AOXPlayer files.
- `ProjectContextCapabilities.execute(kind=repo)` must return Home/VideoPlay target refs scoped to their real declared target paths, not `.`; target names, repoId/sourceFolder, local-package target refs, sourceRoots, entrypoints, and existing Node/Swift/Rust/etc behavior must remain compatible.
- Determine and fix the actual Core parser/projector seam that loses SwiftPM target paths. Do not add a Plugin-side Package.swift parser, Graph ranking workaround, knowledge gate, Git gate, rescan, or live-project mutation.
- Keep ProjectContext read-only and bounded. No AlembicPlugin, BiliDili, vendor, database, vector index, marketplace, Test, or release pointer edits.

Validation:
- Commit the Core change.
- Show RED/GREEN on the new exact RepoContext target-path regression and relevant repo/module ProjectContext suites.
- Run focused tests, full Core test suite, build, check/public API/layer/scope verification, and a read-only direct ProjectContextCapabilities probe against the existing BiliDili checkout proving Home and VideoPlay refs use `Sources/Features/Home` and `Sources/Features/VideoPlay`.
- Return changed files/symbols, raw command results, public JSON summary, compatibility/failure-path review, commit, and two-stage self-review.

Completion means the Core producer truth is ready for Plugin consumer integration; it does not accept the Plugin task or complete the demand. (intent: ProjectContext Core should preserve declared SwiftPM target ownership paths as repository truth; Plugin Graph should consume that truth instead of reparsing manifests or compensating with more ranking heuristics.)
- 2026-07-12T17:19:27.853Z p5-plugin-core-swiftpm-truth-integration-p1 → AlembicPlugin — Phase P5 sequential consumer closure: integrate the accepted Core SwiftPM target-path truth into the existing AlembicPlugin five-tool implementation and close the exact real BiliDili Graph failure without another speculative ranking round.

Fixed provenance and first action:
- Plugin baseline is commit a1f3282e2f29af9fb25ff1c7a12125814b757a19.
- Core producer is accepted commit ce1a29a16bbc7239b3fdfedc466fef6c45e02c44.
- Use the existing `@alembic/core` local file dependency. Build Core and refresh the Plugin dependency/runtime artifacts so every probe demonstrably uses ce1a29a. Do not update vendor or release pointers.
- Before changing Plugin source, rerun the focused Graph fixture and fresh-process public Graph probes. If the Core integration alone resolves the failure, make no source edit and return an explicit no-change/no-new-commit conclusion with provenance. Only if the same public call still fails may you trace that exact chain and make the smallest evidenced Plugin correction; do not add another broad ranking heuristic.

Required real read-only probes:
1) AlembicWorkspace query `HostMcpServer ProjectLocationService request` must retain exact file plus same-name symbol representation for both identifiers, bounded results, complete repo coverage, no irrelevant weak-scope degradation, and no errors.
2) BiliDili query `VideoPlayLoadGenerationGate HomeRequestRefreshGate request` must retain exact file plus same-name symbol representation for both identifiers from the real SwiftPM target paths, with bounded results and no AOXPlayer/common-token starvation or irrelevant weak-scope degradation.
- Capture ProjectContext/Core provenance and public JSON summaries. Do not write/rescan/repair either knowledge base or modify either real project.

Regression and packaging:
- Preserve accepted public Search/Prime orphan filtering, Recipe drift truth/deduplication, Guard behavior, exact Graph/file queries, request-scoped roots, read-only behavior, budgets and public schemas.
- Run focused Graph/Search/Prime/Recipe/read-only tests, full Plugin unit suite, check, build, runtime-package/distribution/Codex-plugin verification, canonical local plugin reload, and fresh-process readback of exact Plugin/Core provenance.
- If a Plugin source change is truly required, commit only that minimal change. Otherwise retain a1f3282 and provide the repository-clean no-commit reason.

Forbidden: Test dispatch; Alembic/AlembicCore/BiliDili/AlembicWorkspace source edits; live DB/vector/index writes; Git/host/admin/intent/knowledge-state/tool-visibility gates; new environment chains; new broad ranking work; vendor/marketplace/release changes.

Completion means the controller can rerun the same two real public Graph queries on the installed plugin and then perform the remaining direct five-tool acceptance. It does not by itself complete or archive the demand. (intent: Consume the corrected Core repository truth first; do not compensate for an upstream target-path defect with more Plugin ranking heuristics.)
- 2026-07-12T23:49:55.769Z p6-plugin-public-guard-readonly-quality-p1 → AlembicPlugin — Fix verified public MCP Guard no-write and projection-quality failures. Public alembic_code_guard with explicit files and no primeRef/workRef currently changes the real AlembicWorkspace SQLite guard_violations table/WAL, contradicting the demand's all-five-tools request-scoped read-only contract and final acceptance prohibition on live knowledge mutation. Make the public MCP path analyze only a confined read-only snapshot and never persist violationRuns, guardHits, primeAdoptions, or other effects; preserve explicit-scope coverage, violations, applicable Recipe rules, primeAlignment observation, no-KB truth, confinement, and public schemas without falsely reporting persisted feedback. Keep persistence only on real non-public owning surfaces. Refactor the public projection so the same Guard scope genuinely passes ast_method_complexity for projectGuardPublicResult (currently 24 > 20) without suppressing rules. Add RED/GREEN fingerprints for passing, violating, and primeRef calls including DB/WAL/vector/knowledge stats/guard_violations; run focused/full/check/build/package/distribution/plugin verification; commit only AlembicPlugin. No Test, rescan, real-project/live-data/Core/vendor/release/marketplace/gate/environment changes. (intent: Make public Plugin MCP Guard genuinely request-scoped and read-only while preserving explicit-scope analysis/results, and simplify the public projection without changing its schema.)
- 2026-07-13T00:33:56.610Z p7-core-recipe-vector-lifecycle-consistency-p1 → AlembicCore — Phase P7 producer repair: make Recipe vector production and maintenance lifecycle-consistent so normal full refresh, update, deprecate, reactivate and delete paths do not create or retain orphan semantic-region vectors.

Controller raw evidence (BiliDili, read-only):
- The HNSW snapshot contains 1,307 vectors: 75 entry_* vectors and 1,232 recipe_region_* vectors.
- SQLite contains 75 non-deprecated Recipe rows. All 75 entry_* ids match exactly (0 entry orphan, 0 missing entry vector).
- Region vectors resolve to 147 distinct recipeIds; 72 recipeIds are absent from SQLite, leaving 595 orphan recipe_region_* vectors. The public query-time count only reports orphan candidates encountered by that query, not the total.
- Current index mtime is 2026-07-10 23:38:45 +0800. The current live Recipes were created before that sync, proving the full region build appended the current corpus but failed to prune the prior corpus.
- Root cause in RecipeRegionVectorIndex.syncRecipeSemanticRegionVectors: removeStale only removes a region id when recipeId is present in the current entries batch and the exact chunk id is no longer expected. It intentionally skips recipeIds absent from entries. Plugin full build calls the method in batches, so vanished Recipes are never pruned.
- Additional same-chain gaps: KnowledgeService lifecycle transitions emit lifecycle:transition but SyncCoordinator listens only knowledge:changed/knowledge:deleted; knowledge:deleted currently removes entry_* only, not recipe_region_*; KnowledgeSyncService direct orphan deprecation bypasses lifecycle events.

Required Core behavior:
1. Introduce an explicit, fail-safe distinction between subset refresh and authoritative-corpus maintenance. Subset refresh for one/batched Recipe must never delete unrelated Recipe regions. An authoritative full-corpus call must remove every recipe_region_* whose recipeId is absent from the authoritative non-deprecated Recipe id set, and must also remove obsolete chunks for live Recipes whose region content changed. Do not infer authority merely from a non-empty array.
2. Make canonical Recipe lifecycle removal close both vector families: physical delete and transition to deprecated remove entry_<id> plus every recipe_region_<id>_* vector. Reactivation/publish/update must have an explicit refresh route that regenerates current entry and region vectors from current DB truth; no query-time mutation.
3. Bring direct maintenance deprecation (including KnowledgeSyncService orphan detection) onto the same observable lifecycle/vector-maintenance path or provide one canonical post-maintenance authoritative reconciliation that callers can await. Do not leave silent direct SQL writes that bypass vector cleanup.
4. Extend explicit repair/reconcile proof so existing orphan recipe regions are countable/removable on an isolated fixture/copy. Query-time Search filtering remains defense-in-depth and must stay read-only; it is not the correctness mechanism.
5. Shutdown/destruction must not silently drop queued removals needed for acknowledged Recipe mutations. Preserve bounded diagnostics for failures.
6. Preserve existing local embedding, Search, Prime, Graph, Recipe Map, Guard, project confinement, no-Git-gate and no-host-selection semantics. No live BiliDili/AlembicWorkspace DB or vector mutation in target validation.

Validation:
- RED/GREEN unit/integration fixtures reproducing exactly: 75 live + absent prior recipe regions; authoritative full sync prunes absent ids; subset/batched refresh preserves unrelated live regions; content update replaces old chunks; deprecate/delete remove entry+regions; reactivate/publish regenerate; direct sync orphan deprecation is closed; shutdown/flush does not lose removal.
- Focused Core vector/knowledge lifecycle suites, full Core test suite, npm run check and npm run build.
- Return final Core commit, changed symbols, raw count proofs, failure-path analysis, command summaries and two-stage self-review.
- No Test window and no real knowledge-base writes. Plugin authoritative-caller integration will be a dependent consumer package after this producer is accepted. (intent: 把索引正确性放回 Recipe 的生产与生命周期维护链：显式区分子集刷新和权威语料刷新，让所有删除/停用路径同步清理主向量与区域向量，并保留查询过滤仅作防御。)
- 2026-07-13T00:59:12.934Z p8-plugin-recipe-vector-maintenance-integration-p1 → AlembicPlugin — Phase P8 Plugin consumer integration for the accepted Core Recipe vector lifecycle producer.

Upstream:
- Consume AlembicCore commit 627fd7d64457c425605493e974dba9211f889098 through the existing local @alembic/core dependency.
- Core now provides explicit subset vs authoritative-corpus Recipe-region maintenance, lifecycle/delete closure for entry+region vectors, region-orphan reconcile counts, an awaited KnowledgeVectorMaintenance boundary for direct KnowledgeSync writes, and async VectorService.destroy() draining.

Required Plugin behavior:
1. In buildRecipeSemanticRegionVectors, obtain the complete non-deprecated Recipe corpus once. When batching syncRecipeSemanticRegions, pass the SAME complete nonDeprecatedRecipeIds authority set to every batch through Core maintenanceScope. Never infer authority from a batch. Empty authoritative corpus must be representable and must prune regions only when this path genuinely owns the full corpus.
2. Inject a real KnowledgeVectorMaintenance implementation into every production KnowledgeSyncService construction that can mutate Recipe rows. After direct file→DB create/update/deprecate work, syncAll must await one canonical authoritative vector maintenance operation built from current DB truth. Reuse the existing Plugin region build/refresh orchestration where appropriate; do not duplicate a second maintenance algorithm.
3. Ensure normal create/update/lifecycle/delete event wiring uses the accepted Core full-entry payload behavior and remains initialized. Do not add query-time mutation, state gates, Git gates, host-selection coupling, Alembic-main coupling or daemon dependencies.
4. Update Plugin shutdown so VectorService.destroy() is awaited BEFORE database close and any vector-store final flush/destroy ordering remains safe. Audit all Plugin callers affected by Core's void→Promise destroy contract.
5. Preserve replacement safety: if embedding is unavailable or replacement generation fails, live old chunks are not deleted. Surface honest bounded diagnostics; no silent success for an unconfigured maintenance port after direct DB mutations.
6. Add an isolated data-root integration proof reproducing the real shape: 75 live entry vectors plus 595 recipe_region vectors for 72 absent Recipe IDs. Run the real Plugin authoritative maintenance caller and prove exactly 595 stale regions removed, all 75 entry vectors and current live regions retained, a second run is idempotent, and subset refresh cannot delete unrelated regions.
7. Do not mutate BiliDili or AlembicWorkspace live DB/WAL/vector/config. Controller will separately decide whether/when to repair the existing real index; this package proves the production path on copies/temp fixtures only.

Validation:
- Focused Plugin tests for region-vector batching, KnowledgeSyncService DI/direct orphan deprecation, lifecycle/delete/update integration, shutdown ordering/drain, and isolated 75/595 repair.
- Full Plugin unit suite, check, build, runtime-package/distribution/Codex-plugin verification.
- Commit the Plugin diff and return exact Core provenance, files/symbols, fixture counts, public/diagnostic output, command summaries and two-stage self-review.
- No Test window. Completion means code prevents the known orphan-production paths; it does not authorize live knowledge cleanup or demand completion without controller verification. (intent: 让 Plugin 的真实 bootstrap/rescan/sync/shutdown 调用链消费 Core 的显式权威维护契约；不增加查询门禁，也不通过查询过滤掩盖生产问题。)
- 2026-07-13T01:36:01.350Z p9-core-provider-independent-vector-removal-p1 → AlembicCore — Phase P9 Core repair discovered during controller review of P8: Recipe removal correctness must not depend on an embedding provider.

Verified failure:
- On accepted Core 627fd7d, VectorService with autoSyncOnCrud=true, a valid EventBus and embedProvider=null initializes with autoSyncEnabled=false.
- Emitting knowledge:deleted for an index containing entry_dead and recipe_region_dead_* produces removed=[].
- Plugin correctly degrades to keyword when Ollama/model is unavailable, so this is a normal supported runtime state, not an exceptional test-only condition.

Required behavior:
1. Decouple removal listeners from embedding. With autoSyncOnCrud and EventBus configured, delete and lifecycle transition to deprecated must remove entry_<id> and every recipe_region_<id>_* even when embedProvider is null/unavailable. Do not require Ollama for deletion.
2. Upsert/create/update/reactivate paths may honestly defer/degrade when no provider; they must not fabricate vector success or delete existing live Recipe chunks.
3. In syncRecipeSemanticRegionVectors authoritative-corpus mode, remove regions whose recipeId is absent from the complete authority set even when pending live chunks cannot be embedded. Preserve old chunks for still-live Recipes until replacement embedding succeeds. Subset mode remains unable to delete unrelated Recipes.
4. Ensure explicit reconcile/maintenance can remove entry and region orphans without a provider. Missing live vectors must not be falsely reported as synchronized when no embedding can occur; return an honest bounded deferred/degraded signal compatible with existing callers.
5. Preserve provider-available behavior, event payloads, replacement ordering, Search filtering, query read-only semantics and all no-gate/no-host-selection boundaries.
6. No Plugin edits, no live BiliDili/AlembicWorkspace mutations, no Test.

Validation:
- RED/GREEN exact runtime regression: embedProvider=null + delete/deprecate removes both vector families.
- Authoritative corpus fixture with provider null removes absent Recipe regions while retaining old live Recipe chunks and reports degradation for ungenerated replacements.
- Subset safety, provider-available update/reactivate regeneration, concurrent flush/destroy, reconcile count/error semantics.
- Focused vector/lifecycle suites, full Core tests, build/check/public API.
- Commit Core and return actual full commit hash, changed symbols, raw JSON probe, commands and two-stage self-review.
Completion unblocks P8 Plugin rework; it does not accept P8 or authorize real index cleanup. (intent: 把删除正确性与向量生成能力解耦：生成需要 embedding，删除不存在/废弃 Recipe 的派生向量不需要；live Recipe 的旧块只在新块成功后替换。)
- 2026-07-13T01:58:51.460Z p10-plugin-provider-independent-authoritative-maintenance-p1 → AlembicPlugin — Plugin-only companion rework to consume accepted-in-evidence Core P9 commit 6b7de17494bac4236822d8a0baaa61cc3b01b670.

Verified remaining defect:
- AlembicPlugin HEAD 7d92768f3e143d0e9fa4458788041b4d9f95d00a calls vectorService.getAvailability() and returns before knowledgeService.list() and syncRecipeSemanticRegions() whenever availability is degraded/unavailable.
- RecipeRegionVectorAvailability.test.ts explicitly expects zero maintenance calls in this state.
- Therefore Core P9 provider-independent delete/reconcile/authoritative prune cannot run through the Plugin's normal authoritative maintenance caller when Ollama/model is unavailable.

Required behavior:
1. Remove availability as a precondition for authoritative maintenance. Keep availability as bounded diagnostic/report data only. Provider missing, unavailable, or availability-probe failure must not prevent reading the complete non-deprecated Recipe corpus and invoking syncRecipeSemanticRegions with maintenanceScope.kind=authoritative-corpus and the same full nonDeprecatedRecipeIds set for every batch (including empty corpus).
2. Let Core own generation degradation. When embedding is unavailable, Core must remove only regions for deleted/deprecated/authority-absent Recipes, preserve all old chunks for still-live Recipes, perform zero fabricated upserts, and return honest degradedReason/embed-provider-unavailable.
3. Preserve provider-available generation, batching, semantic-memory behavior, shutdown drain, direct KnowledgeSync maintenance wiring, subset safety, request-scoped project independence, query read-only behavior, and all no-gate/no-Git/no-host-selection boundaries.
4. Replace the obsolete test that expects maintenance to be skipped. Add RED/GREEN tests for unavailable and availability-probe-error paths proving authority is still listed and cleanup is called; add an integration fixture using the real Core path proving absent regions removed, live old regions retained, zero upserts without provider, and second run idempotent.
5. Consume the current local Core 6b7de17. Do not edit Core. Do not mutate BiliDili/AlembicWorkspace live knowledge DB/WAL/vector/config. No Test window.
6. Run focused Plugin tests, full unit suite, check/build/runtime-package/Codex-plugin verification as applicable. Commit Plugin and return the exact full HEAD (verify with git rev-parse HEAD), changed symbols, raw fixture JSON, commands, and two-stage self-review.

Completion means the normal Plugin authoritative maintenance caller actually invokes provider-independent cleanup in no-Ollama/no-provider states; it does not authorize live index repair or demand completion. (intent: 把 availability 降级为诊断信息，不再作为权威清理前置门禁；无 provider 时仍传递完整非废弃 Recipe authority 给 Core，让 Core 只清不存在/废弃派生向量并保留 live 旧块。)

## Decisions And Append Log

- 2026-07-12T12:52:22.010Z dispatched p1-plugin-five-tool-vector-quality-t1 → AlembicPlugin (delivery delivery-p1-plugin-five-tool-vector-quality-p1__AlembicPlugin__p1-plugin-five-tool-vector-quality-t1)
- 2026-07-12T13:28:45.863Z decision rework (candidate tc-20260712132829-0005) — Request rework: the default local-first vector path is still broken for a config-absent real project. After restart, BiliDili explicit model qwen3-embedding:0.6b returns semantic/vector results, but AlembicWorkspace with no vector config returns actualMode=weighted, vectorUsed=false, fallbackReason=embed-provider-unavailable. Raw Ollama probes prove POST /api/embed with default qwen3-embedding returns model not found while qwen3-embedding:0.6b succeeds with a 1024-dimensional vector. Root cause: probe modelMatches accepts a tagged installed model for a base-name request, but the selected provider still sends the unresolved base name. Rework must resolve and use the exact installed model name from probe (while preserving explicit model/config/env authority, opt-out, bounded fallback, no download/cloud), add a regression for default base-name plus installed tagged model, and prove AlembicWorkspace semantic Search/Prime use vector while BiliDili remains correct. Do not expand Graph/Recipe Map/Guard work until this main Search chain passes.
- 2026-07-12T13:29:23.004Z dispatched p1-plugin-five-tool-vector-quality-t1 → AlembicPlugin (delivery delivery-p1-plugin-five-tool-vector-quality-rework1-p1__AlembicPlugin__p1-plugin-five-tool-vector-quality-t1)
- 2026-07-12T13:54:12.285Z decision accept (candidate tc-20260712135348-0008) — Controller independently reviewed rework commit 67ce4f6 and the exact model-resolution call chain, reran focused tests (23/23), full unit tests (1534/1534), pnpm check, and pnpm build, refreshed the Codex plugin cache, then used a fresh MCP subprocess against the real AlembicWorkspace and BiliDili Ghost knowledge roots. Both Prime calls emitted prime-vector-used; both explicit semantic searches reported actualMode=semantic and vectorUsed=true with the correct requestProjectIdentity/dataRoot; BiliDili returned the expected SwiftPM isolation/dependency rules. Keyword search, Recipe Map, Graph, and explicit-file Guard all returned structured project-scoped results, with both Guard calls ready and 0 violations. Pre/post database, WAL, and SHM hashes were identical. No Test dispatch is needed.
- 2026-07-12T13:54:28.486Z demand completed — The confirmed vector-quality demand is complete: exact installed Ollama model resolution is implemented and committed; controller unit/check/build gates pass; refreshed-plugin fresh-process validation proves Prime and semantic Search use vectors for both real AlembicWorkspace and BiliDili knowledge roots; all five public knowledge tools return project-scoped structured results; real knowledge storage remained unchanged; the only target task is accepted and no Test or follow-up package is required.
- 2026-07-12T15:14:42.160Z demand continued (verified-bug) — The user explicitly asked to continue resolving the quality problems discovered by controller real-tool acceptance. These are inside the existing five-tool retrieval-quality completion definition: semantic/Prime currently surface vector-index entries that the request-scoped knowledge database cannot retrieve; the same acceptance also proved Plugin Graph candidate preselection misses exact identifiers and Recipe Map misclassifies Core status=drifted as unresolved. The continuation preserves all prior accepted vector-default work and fixes only these evidenced truth/quality defects.; first package p2-core-filter-orphan-vector-search-p1 → AlembicCore
- 2026-07-12T15:15:49.733Z dispatched p2-core-filter-orphan-vector-search-t1 → AlembicCore (delivery delivery-p2-core-filter-orphan-vector-search-p1__AlembicCore__p2-core-filter-orphan-vector-search-t1)
- 2026-07-12T15:36:34.862Z decision accept (candidate tc-20260712153610-0013) — Controller independently reviewed Core commits 4476b3f and 9e499809b3721a0d49e78853db4e115515b281ac. The implementation projects semantic, auto/RRF, and legacy vector candidates through the request-scoped non-deprecated knowledge repository before metadata filters, ranking, and slicing; orphan/deprecated candidates cannot reach public results, surviving order/scores and fixed over-retrieval budgets are preserved, truth lookup failure honestly falls back to weighted, and no query-time mutation/reconcile path was added. Controller reran SearchEngine 45/45, full Core 177 files/1649 tests, and npm run build successfully. npm run check passes build/public API/layer/consumer/scope/smoke/budget/space gates and stops only at a pre-existing ASTChunker doctrine violation whose blob exactly matches start commit f427557; this unrelated existing gate is recorded but does not invalidate the scoped SearchEngine fix. The Core producer is accepted; real Plugin consumer validation remains required in the next package.
- 2026-07-12T15:38:45.072Z dispatched p3-plugin-five-tool-truth-graph-drift-quality-t1 → AlembicPlugin (delivery delivery-p3-plugin-five-tool-truth-graph-drift-quality-p1__AlembicPlugin__p3-plugin-five-tool-truth-graph-drift-quality-t1)
- 2026-07-12T16:16:56.039Z decision rework (candidate tc-20260712161625-0017) — Spec compliance failed at the controller-owned real Graph seam after canonical reload. Fresh installed-cache MCP on Plugin affdbb6/Core 9e49980 did retrieve both AlembicPlugin target paths, but HostMcpServer had no file node in the bounded top 18 and the focused query remained degraded by a cyclic module-layer diagnostic. More decisively, BiliDili query `VideoPlayLoadGenerationGate HomeRequestRefreshGate request` returned neither exact Gate file/symbol; AOXPlayer weak video/play/load/request candidates occupied all 18 nodes and invalid-scope still degraded the response. New root-cause hypothesis: ranking only works after a file-owning ModuleContext reaches file-flow; real module-seed/probe selection still starves modules containing exact filenames, and final node expansion permits one candidate's many symbols to exhaust the item budget. Rework must reproduce both real project shapes deterministically, rank exact file ownership before module/file-flow caps, reserve bounded representation for each strong full identifier, and suppress only unrelated weak-scope layer diagnostics. Preserve accepted Search/Prime orphan filtering and Recipe drift behavior; no Test, live writes, new gates, keyword changes, or cross-repo edits.
- 2026-07-12T16:18:13.563Z dispatched p3-plugin-five-tool-truth-graph-drift-quality-t1 → AlembicPlugin (delivery delivery-p3-plugin-five-tool-truth-graph-drift-quality-rework1-p1__AlembicPlugin__p3-plugin-five-tool-truth-graph-drift-quality-t1)
- 2026-07-12T17:00:51.500Z decision rework (candidate tc-20260712170022-0020) — Rework1 passes unit/full/check and fixes the AlembicWorkspace real query after canonical reload, but still fails the controller-owned BiliDili main loop on fresh installed-cache Plugin a1f3282/Core 9e49980: `VideoPlayLoadGenerationGate HomeRequestRefreshGate request` returns neither exact file nor symbol, remains degraded, and is filled by AOXPlayer weak matches. The new root cause is upstream Core RepoContext truth, not another Plugin ranking point: BiliDili Package.swift explicitly maps Home to Sources/Features/Home and VideoPlay to Sources/Features/VideoPlay, yet Core ProjectContextCapabilities repo output projects those main-package target refs as filePath `.` and exposes only broad `Sources` as their usable source root. The passing synthetic fixture had narrower source-root structure and did not reproduce this collapse. Because this is the second failed Plugin attempt, do not redispatch Plugin directly. Create a sequential AlembicCore producer package to preserve real SwiftPM target path refs for main-package targets with BiliDili-shaped regression evidence; after Core acceptance, redispatch Plugin only for consumer integration and the same two real Graph probes. Preserve Plugin a1f3282 behavior, Search/Prime/Recipe acceptance, bounded read-only queries, and all non-goals.
- 2026-07-12T17:02:17.390Z dispatched p4-core-swiftpm-target-path-truth-t1 → AlembicCore (delivery delivery-p4-core-swiftpm-target-path-truth-p1__AlembicCore__p4-core-swiftpm-target-path-truth-t1)
- 2026-07-12T17:18:15.495Z decision accept (candidate tc-20260712171748-0024) — Accept the AlembicCore producer result only: commit ce1a29a preserves declared root-package SwiftPM target paths without changing local-package compatibility. Controller independently reviewed the two-file diff, reran the focused RepoContext suite (6/6), build:check, build, and the full Core suite (177 files / 1650 tests), and executed the public read-only RepoContext probe against real BiliDili. The probe returned Home=Sources/Features/Home, VideoPlay=Sources/Features/VideoPlay, AOXPlayer=Packages/AOXPlayer, errors=[], with clean pre/post worktrees and an unchanged BiliDili Package.swift hash. npm run check stops only at the pre-existing ASTChunker doctrine violation whose blob is identical at baseline 9e49980 and accepted commit ce1a29a, so it is unrelated and not promoted into this package. The existing Plugin task remains needs-rework and requires a sequential consumer integration refresh; this acceptance does not complete the demand.
- 2026-07-12T17:20:36.901Z dispatched p5-plugin-core-swiftpm-truth-integration-t1 → AlembicPlugin (delivery delivery-p5-plugin-core-swiftpm-truth-integration-p1__AlembicPlugin__p5-plugin-core-swiftpm-truth-integration-t1)
- 2026-07-12T17:38:52.081Z decision rework (candidate tc-20260712173826-0028) — P5 cannot be accepted because controller reran the required full Plugin unit suite after building against exact Core ce1a29a and obtained a stable failure: HostAgentProjectContextDirectSwitch expects AOXFoundationKit fileCount=3 but receives 2 (and the same seam implies AOXNetworkKit 4->3). The failing test passes the focused Graph goal but proves the target's `npm run test:unit passed` claim is not reproducible. Root cause is an unrealistic fixture that declares AOXFoundationKit/AOXNetworkKit both as root-package targets with nested `Packages/*/Sources/*` paths and again as local-package targets; precise root target paths now cause briefing module counts to exclude each local Package.swift. Real BiliDili declares those packages through `.package(path:)` and uses different root target names. Rework only the fixture/expectation seam so it models real BiliDili local packages and preserves intentional local-package counts (manifest plus Swift sources: 3/4), while retaining exact Graph success. Do not change production ranking, Core, real projects, knowledge data, gates, marketplace, or installed cache until focused and full suites are green. Then rerun the same two read-only public Graph probes and return exact Plugin/Core provenance. This is a verified in-scope regression repair, not a new feature.
- 2026-07-12T17:39:30.252Z dispatched p5-plugin-core-swiftpm-truth-integration-t1 → AlembicPlugin (delivery delivery-p5-plugin-core-swiftpm-truth-integration-rework1-p1__AlembicPlugin__p5-plugin-core-swiftpm-truth-integration-t1)
- 2026-07-12T17:56:21.128Z decision accept (candidate tc-20260712175550-0031) — Accept P5 rework1. Spec compliance: commit afe2b4e changes only HostAgentProjectContextDirectSwitch.test.ts, replacing duplicate root/local AOX target declarations with the real BiliDili topology of `.package(path:)` local dependencies plus distinct Networking/Home root targets; it preserves intentional local-package manifest-plus-source counts and makes no production Plugin, Core, real-project, knowledge, vendor, marketplace, gate, or Test change. Quality: controller independently reviewed the diff; reran the 5-file focused suite (56/56), full Plugin unit suite (139 files / 1539 tests), check, build, runtime-package, distribution, and Codex-plugin verification; reran fresh-process public Graph probes for AlembicWorkspace and BiliDili with exact identifiers, 5/5 repo coverage, no AOXPlayer noise, diagnostics=[], errorCount=0, partial=false, and no project/data-root fingerprint changes. Controller canonical reload succeeded and fresh installed-cache MCP readback proves Plugin afe2b4e, Core ce1a29a, and the loaded public entry hash all match. P5 is complete. The demand is not yet complete: the current Codex MCP process lifecycle is host-managed, so after the user restarts Codex the controller must perform the final direct dual-project five-tool acceptance before completion/archive.
- 2026-07-12T23:50:59.256Z dispatched p6-plugin-public-guard-readonly-quality-t1 → AlembicPlugin (delivery delivery-p6-plugin-public-guard-readonly-quality-p1__AlembicPlugin__p6-plugin-public-guard-readonly-quality-t1)
- 2026-07-13T00:18:15.134Z decision accept (candidate tc-20260713001304-0035) — Accept P6. Spec compliance: public Host MCP Guard now runs only against a confined request snapshot; violationsStore, guardHits and primeAdoptions are observe-only and no live effect flush remains, while the normal injected ViolationsStore remains writable for non-public owning surfaces. Prime alignment remains visible but honestly reports feedbackRecorded=false without consuming the feedback id. The public schema and explicit-scope behavior are preserved. Quality: controller reviewed the full afe2b4e..45c4d4f diff and clean worktree, reran the 6-file focused suite (27/27), full unit suite (140 files / 1541 tests), npm run check, npm run build, runtime-package verification, plugin-distribution verification and Codex-plugin verification. A freshly rebuilt HostMcpServer public Guard call against the real AlembicWorkspace database passed the exact output/handler scope 2/2 with zero violations and proved DB/WAL/SHM/config/vector hashes, knowledge stats and guard_violations unchanged. Dist provenance is Plugin 45c4d4f192a97e4a6dcfc767d031ac7feecaf526 and Core ce1a29a16bbc7239b3fdfedc466fef6c45e02c44. No Test or TODO addition. Demand remains open only for controller-owned canonical installed-cache reload, user Codex restart, and final installed dual-project five-tool acceptance.
- 2026-07-13T00:34:22.317Z dispatched p7-core-recipe-vector-lifecycle-consistency-t1 → AlembicCore (delivery delivery-p7-core-recipe-vector-lifecycle-consistency-p1__AlembicCore__p7-core-recipe-vector-lifecycle-consistency-t1)
- 2026-07-13T00:58:42.427Z decision accept (candidate tc-20260713005819-0039) — Accept P7 Core producer. Controller reviewed ce1a29a..627fd7d and found the implementation aligned with the root cause: explicit authoritative-corpus scope prunes absent Recipe regions while subset refresh preserves unrelated regions; replacement ordering retains old live chunks until new embedding succeeds; delete/deprecate remove entry and region vectors; live lifecycle changes carry current DB truth and regenerate both families; direct KnowledgeSync writes expose an awaited authoritative maintenance boundary; reconcile counts/removes recipe-region orphans; destroy drains queued/in-flight work. Controller reran the 5 focused files (100/100), full Core suite (177 files/1664 tests), build:check, public API boundary and build; Core worktree is clean at 627fd7d. Acceptance is producer-only: Plugin still must pass the complete non-deprecated Recipe ID authority set across batches, inject/await KnowledgeVectorMaintenance, and await VectorService shutdown before the real production chain is closed. No live knowledge data was mutated and no Test task is authorized.
- 2026-07-13T00:59:54.928Z dispatched p8-plugin-recipe-vector-maintenance-integration-t1 → AlembicPlugin (delivery delivery-p8-plugin-recipe-vector-maintenance-integration-p1__AlembicPlugin__p8-plugin-recipe-vector-maintenance-integration-t1)
- 2026-07-13T01:35:35.815Z decision rework (candidate tc-20260713013512-0043) — P8 is not acceptable yet. The available-provider path is well integrated, but the completion definition is provider-independent for destructive lifecycle correctness. Controller runtime evidence against accepted Core 627fd7d shows that with embedProvider=null and autoSyncOnCrud=true, VectorService initializes with autoSyncEnabled=false; emitting knowledge:deleted removes neither entry_dead nor recipe_region_dead_* (removed=[]). Plugin P8 then compounds this by returning at the vector availability gate, so direct sync/delete/deprecate while Ollama/model is unavailable still creates or retains orphan vectors. Replacement safety requires keeping old chunks for still-live Recipes when embedding is unavailable, but it does not authorize retaining vectors for Recipes that are deleted/deprecated/absent from the authoritative corpus. Repair requires an upstream Core producer change: removal/reconcile must operate without an embed provider and authoritative maintenance must be able to prune absent Recipe ids while preserving live old chunks; then Plugin must invoke that cleanup-only path even when generation is degraded. Also correct the TargetResultEnvelope commit attestation: reported 7d92768acaaf... does not exist; actual Plugin HEAD is 7d92768f3e143d0e9fa4458788041b4d9f95d00a. Do not mutate live knowledge data and do not add Test.
- 2026-07-13T01:36:31.545Z dispatched p9-core-provider-independent-vector-removal-t1 → AlembicCore (delivery delivery-p9-core-provider-independent-vector-removal-p1__AlembicCore__p9-core-provider-independent-vector-removal-t1)
- 2026-07-13T01:58:25.798Z decision rework (candidate tc-20260713015608-0047) — The atomic rework candidate cannot be accepted because it contains unresolved P8 Plugin behavior. P9 Core itself is controller-verified good at 6b7de17494bac4236822d8a0baaa61cc3b01b670 and must not be changed or redispatched: provider-null delete/deprecate/reconcile behavior passed runtime, focused, full-suite and boundary checks. However current Plugin HEAD 7d92768f3e143d0e9fa4458788041b4d9f95d00a still exits buildRecipeSemanticRegionVectors before listing authority or invoking syncRecipeSemanticRegions whenever vector availability is degraded; its test explicitly expects zero calls. Therefore the normal no-Ollama state still skips Core P9 cleanup-only capability and leaves deleted/deprecated/absent Recipe regions. Continue with one Plugin-only rework companion: always obtain authoritative non-deprecated Recipe ids and invoke Core maintenance even when generation is unavailable; preserve live old chunks, report degraded generation honestly, update the obsolete skip test, use isolated fixtures only, and attest the actual Plugin commit. No Core edits, no live data mutation, no Test.
- 2026-07-13T01:59:19.121Z dispatched p10-plugin-provider-independent-authoritative-maintenance-t1 → AlembicPlugin (delivery delivery-p10-plugin-provider-independent-authoritative-maintenance-p1__AlembicPlugin__p10-plugin-provider-independent-authoritative-maintenance-t1)
- 2026-07-13T02:32:29.943Z decision accept (candidate tc-20260713023200-0051) — Accept the P8/P9/P10 atomic rework chain. Core P9 at 6b7de17494bac4236822d8a0baaa61cc3b01b670 was previously controller-verified to keep removal/reconcile available with embedProvider=null while preserving live Recipe chunks and reporting deferred generation honestly. Plugin P10 at clean exact HEAD be7bc058fcd188f38b48ee551f912377c2d19f1d removes the remaining availability precondition: getAvailability is diagnostic-only; the normal caller still lists the complete non-deprecated Recipe corpus and invokes Core authoritative-corpus maintenance for unavailable, probe-error, batched, and empty-corpus cases. Controller source review and focused rerun passed 38/38; the real Core provider-null fixture removed absent regions, retained live old regions, made zero batch upserts, and was idempotent. Full Plugin rerun passed 143/143 files and 1553/1553 tests; check/build passed with Core 6b7de17; runtime package, distribution, and Codex plugin verification passed. No live BiliDili/AlembicWorkspace knowledge data was mutated. Residual console warnings are pre-existing warning-only script findings and do not affect this scope.
- 2026-07-13T02:45:07.217Z demand completed — All accepted P1-P10 implementation packages and the controller's post-restart dual-project live five-tool acceptance now satisfy the confirmed completion definition without mutating either real knowledge base.
- 2026-07-13T03:29:55.958Z archived → wakeflow-ledger/workspace/archive/2026-07/alembic-plugin-five-tool-vector-quality-2026-07-12 — Archive the previously completed five-tool vector quality demand so the confirmed retrieval/vector/projectcontext architecture work can continue sequentially on the main fleet.

## Backfill Summaries

- 2026-07-12T13:18:01.580Z AlembicPlugin/p1-plugin-five-tool-vector-quality-t1 returned needs-review (result tr-p1-plugin-five-tool-vector-quality-t1)
- 2026-07-12T13:37:56.726Z AlembicPlugin/p1-plugin-five-tool-vector-quality-t1 returned needs-review (result tr-p1-plugin-five-tool-vector-quality-t1)
- 2026-07-12T15:32:37.216Z AlembicCore/p2-core-filter-orphan-vector-search-t1 returned completed (result tr-p2-core-filter-orphan-vector-search-t1)
- 2026-07-12T15:33:20.164Z AlembicCore/p2-core-filter-orphan-vector-search-t1 returned completed (result tr-p2-core-filter-orphan-vector-search-t1)
- 2026-07-12T16:05:12.168Z AlembicPlugin/p3-plugin-five-tool-truth-graph-drift-quality-t1 returned needs-review (result tr-p3-plugin-five-tool-truth-graph-drift-quality-t1)
- 2026-07-12T16:53:24.854Z AlembicPlugin/p3-plugin-five-tool-truth-graph-drift-quality-t1 returned needs-review (result tr-p3-plugin-five-tool-truth-graph-drift-quality-t1)
- 2026-07-12T17:14:07.611Z AlembicCore/p4-core-swiftpm-target-path-truth-t1 returned completed (result tr-p4-core-swiftpm-target-path-truth-t1)
- 2026-07-12T17:33:50.346Z AlembicPlugin/p5-plugin-core-swiftpm-truth-integration-t1 returned needs-review (result tr-p5-plugin-core-swiftpm-truth-integration-t1)
- 2026-07-12T17:50:03.746Z AlembicPlugin/p5-plugin-core-swiftpm-truth-integration-t1 returned completed (result tr-p5-plugin-core-swiftpm-truth-integration-t1)
- 2026-07-13T00:12:11.109Z AlembicPlugin/p6-plugin-public-guard-readonly-quality-t1 returned completed (result tr-p6-plugin-public-guard-readonly-quality-t1)
- 2026-07-13T00:53:22.991Z AlembicCore/p7-core-recipe-vector-lifecycle-consistency-t1 returned completed (result tr-p7-core-recipe-vector-lifecycle-consistency-t1)
- 2026-07-13T00:53:56.032Z AlembicCore/p7-core-recipe-vector-lifecycle-consistency-t1 returned completed (result tr-p7-core-recipe-vector-lifecycle-consistency-t1)
- 2026-07-13T01:31:08.499Z AlembicPlugin/p8-plugin-recipe-vector-maintenance-integration-t1 returned completed (result tr-p8-plugin-recipe-vector-maintenance-integration-t1)
- 2026-07-13T01:51:15.971Z AlembicCore/p9-core-provider-independent-vector-removal-t1 returned completed (result tr-p9-core-provider-independent-vector-removal-t1)
- 2026-07-13T02:25:53.597Z AlembicPlugin/p10-plugin-provider-independent-authoritative-maintenance-t1 returned completed (result tr-p10-plugin-provider-independent-authoritative-maintenance-t1)
