# recipe-coldstart-production-quality-2026-07-15 — 从空知识库重建 Alembic 五仓与 BiliDili 的 DeepSeek Recipe 生产链，以 immutable manifest、coverage cells、四层门禁、session finalization、可恢复持久化、首个 sparse/vector generations 和五个公共 MCP 真实 oracles 证明覆盖、准确性、丰富度、检索性与实际价值。

> State-root index. Generated from wakeflow-state.json (revision 343, event evt-20260730084051-0343). Regenerate with wakeflow-render-progress; do not hand-edit.

## Core records

- [demand.json](demand.json) — immutable demand record
- [wakeflow-state.json](wakeflow-state.json) — authoritative state machine (state: review-ready, revision 343)
- [controller-events.jsonl](controller-events.jsonl) — append-only controller event log
- [projection.json](projection.json) — machine-readable projection + structured slices
- [developer-progress.md](developer-progress.md) — human progress document

## Task packages

- `i1-i2-core-project-context-foundation-p1` (accepted) — 执行单一 demand 的第一个强阻断阶段 I1–I2 / PC-F 上游 AlembicCore combined package。先在真实加载产物上复现历史 ProjectContext 1/5 与 repeatability 问题，区分 stale dist/package/config/root identity 与当前源码缺陷；再只修复被证据确认的 Core 缺陷。若完整证据证明无需修复，repairCommits=[] 可以成立，但不能以 fixture、接口可调用或 fallback 成功代替完整通过。

必须连接并交付的 Core 范围：
1. 盘点并验证现有 ProjectContextContracts/Service/capabilities 与语言/parser 初始化，覆盖九类 request：anchor-range、space、repo、map、module、module-layers、file-flow、file-symbols、source-slice。逐 project-mode / repo 记录 applicable 或 typed N/A、selector/scope、language、parser/query initialization、terminal status、continuation 与 output hash。
2. Core capture 是唯一权威 repo-set producer：MR-ALEMBIC 必须覆盖 Alembic、AlembicCore、AlembicAgent、AlembicPlugin、AlembicDashboard 5/5；SP-BILIDILI 必须覆盖 root + Packages/AOXFoundationKit、AOXNetworkKit、AOXPlayer、AOXUIKit。输出 canonical repoId+relativeRoot+revision 的 SourceRevisionVectorV1；clean/dirty/content revision 都要可判定，绝不写绝对路径或用 dirty=true 充当 revision。
3. 在现有单源上补齐 single injectable capture、canonical artifact builder、private durable content-addressed artifact/store/receipt/run-bound lease、strict readiness/canonicalization、serializer/reopen 与 host ports。artifactId 必须由 lineage-free projections 的 canonical manifest 内容 hash 得出，opaque preparationId 与 artifactId 分离；公共 lineage 附着不得形成循环 hash。
4. facts 必须分成完整 inventory plane 与有界 detail plane。inventory 不受 Main 6/3/8、Plugin 25/3/8、Core raw Plan 5000/400、ModuleService 500 或公共显示限制影响；detail 必须有守恒选择、continuation 和 content-addressed full chunks。所有 ref 均需落到有界 source range。
5. 明确清点 Core Plan collectProjectContext/projectSourceFacts raw scanner：它是 PC-F inventory 中的真实 entry，但 strict consumer call count 必须为 0，并有不可达或 artifact-only adapter 证据。不得在本包实现 I3+ deterministic Plan compiler、Recipe authoring/gates、coverage/publication/vector 主功能。
6. 提供后续 Alembic/Plugin host adapter 可消费的稳定端口与 compatibility contract，不替两仓实现 adapter，不收集第二份 ProjectContext，不修改其他仓。
7. 同一 source revision 的 factsContentHash、certificationBindingHash、artifactId 与 SourceRevisionVectorV1 必须在 fresh process 两次重开中一致；facts/source/scope/capability/config drift 的失效语义分离。覆盖 cancellation、timeout、error、continuation、lease crash/replay、第二消费者拒绝与同 run 恢复。
8. 产出可审查的 ProjectContextCapabilityAuditReport Core 部分：实际加载 commit/tree/package/dist/runtime hash、复现输入与根因、九请求矩阵、Core producer/raw-entry inventory、MR5/5 与 SP root+4、inventory/detail conservation、fresh-process 对比、openConfirmedDefects、每个确认缺陷 owner + failing-before/passing-after + regression、repairCommits[] 与 residual risks。

验证和交付：
- 遵守 AlembicCore/AGENTS.md；先确认真实 repo/branch/worktree 与干净基线，保留用户已有改动。
- 对确认缺陷先留 RED，再实现 GREEN；运行 focused tests、fresh-process/runtime probes、npm run check 及仓库规则要求的 build/lint/test/boundary gates。
- 两阶段自审：先逐条对 Requirement §§7/18.0 合规，再审 correctness、failure/recovery、hash non-circularity、性能/大仓容量、兼容性和范围。
- 只提交 AlembicCore 一个干净 commit；返回 parent/commit/tree、changed files/symbols、原始命令摘要、evidence artifact 路径与内容 hash、兼容性和剩余风险。不得写 Alembic/AlembicPlugin/AlembicAgent/AlembicDashboard，不得 reset Ghost/data root，不得启动 Test。
- 完成后按 wakeflow-target 生成 TargetResultEnvelope；本次 automationEnabled 的 controller return 仅把证据返回总控，不代表自验收或允许 I3+。
- `i1-i2-core-project-context-foundation-rework1-p1` (accepted) — AlembicCore PC-F 首轮 rework combined package。以首轮 commit 51e71a5d203f733ace8a5feb71202ac31dd41ed7 为基线，只修复控制器已记录并复现的 I1–I2 缺陷；产出一个边界清晰的 repair commit。不得修改其他仓、不得进入 I3+、不得 reset Ghost/data root、不得启动 Test，也不得顺手修复与本需求无关的 ASTChunker doctrine 基线问题。

必须逐项闭环：
1. 真实 loaded-artifact RED/根因：在 parent 10e43a66fac8748b1da176844bd77505aa949b0e 或实际配置加载的 package/dist/root 上执行历史 ProjectContext 链路，真实复现或明确证伪 MR 1/5 与 repeatability 问题，保留输入、输出、hash、loaded commit/tree/package/dist/runtime 身份；分类 stale dist/package/config/root identity 与源码缺陷。“新模块当时不存在”不得作为 RED。
2. 九请求审计诚实性：废除每仓仅单文件 module seed 的伪完整矩阵，按真实 project/module ownership 与适用 scope 为 MR-ALEMBIC 5/5、SP-BILIDILI root+4 建立有意义的九请求覆盖；每条非空 diagnostics/errors 必须归类为 expected typed external/advisory 或 open confirmed defect。readiness 与 openConfirmedDefects 不得忽略 errors；修正内部 BiliDili modules/sibling roots 被 outside-scope/query-unavailable 的错误归属。只有分类和修复后才允许 openConfirmedDefects=[]。
3. inventory coherence：移除跨 capture 的无限 descriptors cache，或让缓存严格绑定一致 revision/content；同一 host ports 实例上 add/delete/modify 后必须重新枚举且不漏 eligible file。加入等价于 controller probe 的 RED/GREEN 回归。
4. 单一 source-state capture：capture 全程必须认证同一个 coherent source state；对 capture 期间 add/delete/modify、clean→dirty、dirty/content drift 均 fail closed，不得签发“旧 clean commit/tree + 新 blob hash”且 readiness=passed 的 artifact。加入可重复的 race/漂移 RED/GREEN 测试，并明确重试/失败语义。
5. crash durability：artifact/store/receipt/run-bound lease 的持久化必须具备真实崩溃安全发布与恢复语义，包括同目录临时写、file fsync、atomic publish/rename、directory fsync、必要 readback，以及 partial temp/final、collision、crash/replay 的处理。不得把直接 final-path wx 或无 fsync 的 rename 称为 durable；用故障注入/重放测试证明。
6. 证据重生成：重新生成 ProjectContextCapabilityAuditReport Core JSON 与 execution evidence，逐项响应本 rework 原因。静态 raw scanner scan 只能作辅助，raw Plan strict-zero 仍需实际不可达或 artifact-only adapter 证据。openConfirmedDefects=[] 仅在上述分类、探针和回归全部通过后成立。

验证与交付：
- 先留下上述缺陷可复现的 RED，再实现 GREEN；运行 focused tests、controller-probe 等价回归、fresh-process/runtime probes、build:check、public-api/layer boundary、npm run check 和完整 npm test。
- 若 npm run check 仍只因 unchanged src/infrastructure/vector/ASTChunker.ts:32 doctrine baseline 失败，必须用 parent/HEAD 对照证明并作为显式 waiver；不得修该无关问题。
- 两阶段自审：第一阶段逐条对 controller rework 1–6 和 Requirement §§7/18.0；第二阶段审 correctness、source consistency、failure/recovery、crash durability、hash non-circularity、性能/大仓容量、兼容性与 scope。
- 返回 parent/commit/tree、changed files/symbols、原始命令摘要、evidence 路径与内容 hash、逐条 rework response、兼容性和剩余风险。repair commit 必须干净可审；不得把 fixture/接口可调用/fallback success 当完整验收。
- 完成后按 wakeflow-target 记录新的 TargetResultEnvelope 并真实 controller-return；返回只触发总控复审，不代表接受，也不解锁 Alembic/Plugin 或 I3+。
- `i1-i2-core-project-context-foundation-rootcause2-p1` (accepted) — AlembicCore PC-F root-cause re-derivation combined package。原任务 recurringProblem=true，本包不是第三次普通 point-fix；必须用新的根因模型收敛 controller 在 commit 5414d681c1fb1a34578d4f55fa829115364900fe 上独立复现的两项缺陷。只允许修改 AlembicCore 对应 foundation/audit/tests/evidence；不进入 I3+，不修改 Alembic/Plugin/Agent/Dashboard，不启动 Test，不重做已通过的 store/cache 设计，不修 ASTChunker。

根因假设 A — ownership namespace 不完整：
- 当前 audit 的 directory ownerModuleIds 与真实 import/package namespace 脱节；MR repositories 未提供 @alembic/* package/export aliases，classifier 又不能识别 current repo self-package imports，造成至少 170 条已认证范围内依赖被归为 expected-external。
- 重新建立可审计的 canonical dependency ownership：aliases/exports 必须从真实 package metadata或显式、版本化、逐 repo 冻结的 ownership 输入派生，绑定 repoId/module owner，不得按字符串前缀把所有 @alembic/* 放行。
- current repo self-package/subpath 必须归入 declared internal；若 ProjectContext 仍无法用真实 seeds/ownership解析，必须是 confirmed defect并让 readiness/openConfirmedDefects失败。已批准 sibling repo 必须标出 relatedRepoId并守恒，真正第三方才可 expected-external。
- MR 五仓与 SP root+4 均重新生成九请求矩阵；报告给出 internal-resolved、approved-sibling、expected-external、confirmed-defect 的 counts/samples/ownership evidence。禁止仅修改标签而不证明 request output、module seeds和dependency graph可用。

根因假设 B — pre-read revision 不能认证 read content：
- 当前每轮 captureRepositories 只在读文件前 observeRevision；重复两次同样流程仍允许 source 在两轮 observe 后、read 中变更，最终签出 git-clean commit/tree + dirty blob 且 readiness=passed。
- 设计并实现真正的 coherent snapshot protocol。每轮必须有读取前后 revision/content fence；git-clean 声明还必须证明被读取 eligible bytes 与所声明 tree一致，不能只比较两次 pre-read commit/tree。dirty/content 模式必须把最终完整 content hash与稳定观察绑定。capture 结束后只能认证一个有明确边界的状态。
- 将 controller 对抗探针加入 RED/GREEN：两次 observe 均返回同 clean revision、两次 read 均切入同一 dirty bytes、最终 dirty；修复后必须抛 PROJECT_CONTEXT_SOURCE_STATE_DRIFT。另覆盖 transient clean→dirty→clean、add/delete/modify、dirty→dirty 和 abort，避免把取消误报 drift。
- 若需要扩展 host-port observation contract，保持公共子路径向后兼容或提供显式版本迁移；不得把锁或宿主专用 Git 实现硬编码进纯 Core contract。

证据与验证：
- 在 5414d681… 上保留这两个 controller probe 的 failing-before，提交一个新的清晰 repair commit。
- 重跑 build:check、focused suite、完整 npm test、public API/layer/consumer import/scope/smoke/output-budget/space/naming/Biome/retired-symbol gates；npm run check 的 ASTChunker 基线只可用不变 hash waiver，不得修改。
- 重新运行双项目 fresh-process audit和 parent loaded-artifact reproduction；报告不得以 openConfirmedDefects=[] 掩盖任何 internal/sibling 未解析项。
- 两阶段自审必须先逐条回答根因 A/B，再审 compatibility、TOCTOU/failure/replay、性能与报告诚实性。返回 parent/commit/tree、diff、命令、报告 hash、逐项 probe 输出和剩余风险。
- 返回 TargetResultEnvelope 只触发 controller 最后一轮同根因复验；不解锁下游。
- `i1-i2-core-content-terminal-fence-p1` (accepted) — AlembicCore PC-F 最小 content terminal fence 包。基线为 443ab564623cc6f3a5ee70451feb67d5e9d338a3；只关闭 revision.kind=content 且 host 未提供 verifySnapshot 时，Core fallback 把候选直接视为 verified 的缺口。

唯一实现目标：
1. 在 fallback 成功前，用现有 ports、inventory policy、规范化与 hash 逻辑执行一次终态完整 eligible inventory/content 重读；逐项比较 path、mode、owner、size/blob、eligibleInventoryHash 与 workingTreeContentHash。任何新增、删除、修改或内容不一致都抛 PROJECT_CONTEXT_SOURCE_STATE_DRIFT。
2. 保留稳定 legacy content host 兼容性；不把 verifySnapshot 改为必填，不新增锁、重试器、snapshot 子系统、公共 contract/type、缓存或通用框架。优先只改 capture.ts 与 ProjectContextFoundation.test.ts；若需局部 helper，留在同模块。
3. 先保留 RED：两轮均认证 A，但每轮 post-observation 后终态为 B，A/B hash 不同，当前错误返回 readiness=passed；修复后该探针必须确定性失败。另验证稳定 content 继续通过、terminal add/delete/modify 失败、AbortError 不被改写。
4. 回归现有 clean-Git/dirty-Git、ownership、artifact integrity、store/cache/durability 测试，证明没有重做或回退既有成果。
5. 只提交一个小而清晰的 AlembicCore commit。重跑 build:check、focused suite、完整 npm test 与现有边界门禁；机械重生成 PC-F audit/evidence 仅用于绑定新 commit/tree/dist/runtime hash及双 fresh-process，不扩展报告 schema或重写已通过的 ownership 逻辑。

明确非目标：不修改 node Git verifier、dependency ownership/classifier、store/lease/durability、其他仓、ASTChunker、I3+、Test、Wakeflow；不做顺手重构。完成仅允许总控重新验证 PC-F，不能自行解锁下游。
- `i1-i2-core-foundation-authority-v2-p1` (accepted) — 同一 demand 的 AlembicCore PC-F authority-v2 combined package。基线必须是已接受且干净的 AlembicCore@cb517460d9c1f9d7853b51bd914062cd5ba3a116；保留现有 capture/store/readiness/lease、ownership/coherent-snapshot/content-terminal-fence 成果，只关闭完整需求重新比对后确认的最上游权威缺口。

实现边界：
1. 新增 Core-owned pre-capture ProjectScopeManifestV1 builder/receipt，由已接受 project-mode scope、confined source roots 与 project identity 独立生成 MR5、SP root+4 canonical tuples/scope hash；capture repositories 与 readiness expectations 必须从该 receipt 派生。同步删除/增加/alias caller repo/expected lists仍须失败，不能继续自证。
2. 版本化 strict request outcome/index/readiness：稳定行身份包含 repoId、kind、selectorHash、canonicalScopeHash、适用 language/parser/owner surface；允许并守恒多 selector、多 scope、多语言/多 surface 行。删除、复制、语言交换、scope-only swap/alias 均 fail closed；v1 单行只保留兼容，不得证明 strict readiness。
3. 在现有 Foundation store 内冻结每个 readable eligible inventory file 的 content-addressed blob/ref，而不只是 detail 选中项；detail/page/continuation/full-chunk 必须从冻结 bytes 解析。重启后即使 live 文件变化或删除，仍返回原 bytes；绝不 live fallback。守恒为 eligibleFiles=frozenBlobAvailable+readFailed，critical read failure=0。
4. inventory owner 增加 origin/confidence/evidence 与 shared/ambiguous typed disposition；path heuristic 单独不能决定 criticality、exclusion 或 coverage。保持既有 dependency ownership 成果，不重写 classifier。
5. 把 base certification 与 consumer evidence 分开：base receipt 在 store put/readback 时封印且后续 hash 不变；提供 actual loaded adapter 可调用的 ProjectContextConsumerProjectionReceiptV2 与 immutable post-open lineage contract/canonicalizer。capture caller/audit placeholder 不得作为生产 consumer 证据；本包只提供 Core 端口/合同与真实端口测试，不替 Alembic/Plugin 实现实际 adapters。
6. 重新生成 Core PC-F audit/runtime evidence：实际 commit/tree/package/dist/load hashes、独立 scope、V2 九请求矩阵、全量 blob/read/owner/ref conservation、base-put-open-receipt 顺序、fresh-process/mutation/differential probes，以及 Core-owned counters。报告必须明确 global PC-F 仍等待 Main/Plugin actual consumer、Graph/Map 包，不能用 openConfirmedDefects=[] 宣称全局完成。

验证与交付：
- 先保留针对上述 V1 缺口的 RED，再实现 GREEN；运行 build:check、focused Foundation suite、完整 npm test、npm run check 与现有公共 API/layer/boundary gates。
- mutation 必须覆盖 synchronized repo self-certification、V2 row delete/duplicate/language/scope swap、omitted-file live mutation/restart、owner evidence removal、placeholder projection和base hash post-open mutation。
- 只提交 AlembicCore 一个干净 commit；返回 parent/commit/tree、changed files/symbols、命令原始摘要、evidence 路径/hash、兼容性与剩余风险。
- 禁止修改 Alembic/AlembicPlugin/AlembicAgent/Dashboard，禁止 I3+ Plan/Fact mining/Recipe gates/publication/vector 主链，禁止 Ghost/data root、Test、第二 store/registry/parser、第二 Plan/graph/proof/mining 平台。
- `i1-i2-core-foundation-authority-v2-rework1-p1` (accepted) — AlembicCore authority-v2 最小返工，基线必须为干净的 e86aca85997b48e0d9180e98722746d1e2e239d8。

只修一个根因：公开 V1 captureCertifiedProjectFacts 当前会因对象额外携带 projectScope/requestMatrix 而进入 strict-v2，但未验证 ProjectScopeCaptureBindingV1，导致 accepted root A 与实际 sourceRoot B 不一致仍可取得 pcf-readiness-v2=passed。

完成条件：
1. V1 与 V2 入口分叉显式且不可结构嗅探绕过；所有 strict-v2 执行必须先验证完整 scope binding，并从 binding 派生 repositories/projectMode/request plans/projections。
2. 固化 controller A/B probe：accepted root 有 eligible file、shadow root 为空；调用 V1+额外 V2 字段或任何等价旁路必须 fail closed，不能生成 V2 artifact。合法 V2 正常通过，纯 V1 兼容行为不变。
3. 检查导出入口和内部共享实现，避免新增第二套 capture/store/readiness；不修改既有 authority-v2 其他合同。
4. 重跑 build:check、Foundation focused suite、完整 npm test、边界 gates 和双模式 fresh-process audit；审计基于提交后干净工作树，报告继续明确 global PC-F pending。
5. 只提交 AlembicCore 一个干净修复 commit并回填 parent/commit/tree、RED/GREEN、原始命令摘要、审计路径/hash与剩余风险。

禁止触碰 Alembic/AlembicPlugin/AlembicAgent/Dashboard/Test、I3+、Ghost/data root、第二 store/parser/registry/Plan 平台。
- `i2-1-alembic-main-pcf-adapters-p1` (accepted) — 执行同一 demand 的 I2.1 / PC-F2 Alembic Main combined package。基线必须是干净的 Alembic@e0cd0f018c3b6b436325da94769c3a99482616e1，并实际加载控制器已接受的 AlembicCore@f82bfec58e36ea71e575a23bee356623a7a28381；若仓库、分支、依赖加载或 hash 不一致，停止回填 blocker。

唯一目标：Main 的 Plan、Recipe generation、dependency graph、module coverage 四个真实生产消费者，只能从一次已持久化并重新打开的 strict-v2 CertifiedProjectFactsArtifact 派生。四个 actual loaded adapters 必须通过 Core CertifiedProjectFactsConsumerPort 产生各自 deterministic ProjectContextConsumerProjectionReceiptV2；四行共享相同且非空的 artifactId/sourceVectorHash/factsContentHash/certificationBindingHash，projectionContentHash 各自按载荷确定，base certification 在 put/readback 后保持不变。实际 cold-start/Generate session 的 create→persist→fresh reload 必须携带并校验该 binding；缺失、partial、scope/vector/hash drift 或 stale binding 一律 fail closed。

必须替换 strict 路径中的旁路，而非新增兼容层：
1. PlanSelectionGate 不得再同时 buildProjectContextWorkflowFacts + collectPlanProjectContext；Plan 投影从 reopen artifact 产生。
2. ProjectContextWorkflowFacts / ProjectContextConsumerFacts 不得直接调用 ProjectContextCapabilities、合成 ProjectScope repo、二次 repo/map/graph 采集；四类投影均从同一 reopen artifact 纯适配。
3. AiDimensionPreparation 与 Main ModuleService 不得直接 projectContextDependencyGraph，也不得 catch 后以 null/空图继续 strict cold-start。
4. ProjectMapModules 的 Package.swift 解析、live filesystem scan 与 80 cap、Main 的 12-owned-files/module-seeds 及 6/3/8 detail caps不得参与 strict completeness；展示/detail 可以有界，但 completeness 来自 frozen inventory/continuation。
5. IncrementalRescanWorkflow、legacy snapshot/consumer entrypoints逐一记录 applicable 或 typed-unreachable N/A + reachability evidence；任何 applicable 路径不得成为第二 producer/capture。

验收必须由实际 loaded entrypoints 证明，不是接口/fixture存在：四类 strict lineage rows；cold-start session persist+fresh reload；MR5 与 SP root+4 复用 accepted scope/vector；>12 与 >80 fixtures inventory/module/owned-file守恒；删除/复制/partial/hash/scope/vector/stale-binding mutation均拒绝；strict counters directProjectContextCallCount=0、rawFilesystemFallbackCount=0、synthesizedProjectScopeFactCount=0、cappedModuleProjectionCount=0。输出 Main PC-F evidence report/receipt，绑定 Alembic parent/commit/tree、实际 Core package/dist/load hash、入口 call trace、四 receipt、counter/conservation/mutation 结果及文件/semantic hash；明确 global PC-F 仍等待 Plugin 与 Graph/Map、不得声称 PCFBaselineReceipt 或进入 I3+。

验证：先保留当前双采集/合成/直接 graph/Package.swift/12/80 的 RED 或等价可复现证据，再 GREEN；运行 npm run build:check、focused PC-F/ProjectContext/plan/session/ModuleService tests、npm run check、完整 npm run test、git diff --check，并按 Alembic/AGENTS.md 两阶段自审（先逐条 Requirement §§7.1–7.3/18.0/18.3，再审 correctness、fail-closed、session reload、性能/兼容性/范围）。只提交 Alembic 一个干净 commit；返回 parent/commit/tree、changed files/symbols、原始命令摘要、evidence 路径/hash、RED/GREEN、兼容性与剩余风险。

禁止修改 AlembicCore/AlembicPlugin/AlembicAgent/AlembicDashboard/Test、Ghost/data root 和 Wakeflow；禁止 I2.2 Plugin/Graph/Map、I3+ Plan cognition/fact mining/Recipe gates/persistence/vector/publication、第二 store/registry/parser/graph/Plan 平台及顺手重构。若 accepted Core API 无法支持实际 Main adapter，停止并以精确缺口回填，不得在 Main 复制 Core authority。
- `i2-1-alembic-main-pcf-adapters-rework1-p1` (accepted) — Alembic Main I2.1 rework1，基于干净的 Alembic@b5b63edfc3ee2921d71d67e052f0dbc8fe450afd；AlembicCore 保持已接受的 f82bfec58e36ea71e575a23bee356623a7a28381，不修改其他仓库。

根因：当前中央 runtime 一次性预制四个投影/receipt 并把完整 frozen facts 重复塞进 session，真实四入口没有各自 reopen；路径丢失 relativeRoot，ModuleService 还可能永久缓存 legacy；旁路计数是常量而非测量。

只完成以下修复：
1. 一份 immutable artifact/binding 持久化；session carrier 保持有界，不重复携带四份文件正文。Plan、Recipe generation、dependency graph、module coverage 在各自实际生产入口通过 Core consumer port reopen，并生成入口准确、base identity 相同的 receipt。
2. 保留 repository relativeRoot，单仓和 ProjectScope 投影路径必须还原到真实存在的源文件；禁止 `${repoId}/${relativePath}` 当作磁盘相对路径。
3. ModuleService 在 certified carrier 出现或变化时必须重新绑定或 fail closed，不能继续已缓存的 legacy 数据；presenter/graph 缺失或解析失败不得吞错返回空载荷。
4. direct/raw/synthesized/capped 四类 counter 与 legacy reachability 必须来自实际 instrumentation/可复现探针，不能硬编码零或只写 typedReason。
5. 用真实 capture fixture 验证：>12 files；>80 modules 且 owned-file 守恒、无空模块伪计数；ProjectScope 的 control root 自身 + 4 member repos，scope/vector/tuple 全量一致；删除/partial/hash/scope/vector/stale mutation 全部 fail closed。
6. 加回归探针覆盖 projected path 存在、carrier 不重复文件正文且尺寸有界、ModuleService 先 legacy 后 certified 不得静默沿旧路、四个实际 entrypoint receipt 准确且 distinct。

重新运行 build:check、focused PC-F/plan/session/ModuleService tests、npm run check、完整 npm run test、git diff --check；重建 Main evidence report，不能把合成 fixture 写成 Alembic/BiliDili 实跑。只提交 Alembic 一个干净 repair commit。

禁止范围：不改 Core/Plugin/Agent/Dashboard/Test/Wakeflow；不做 I2.2、I3+、冷启动 reset 顺序重构或第二套 runtime/store/parser/graph 平台。若 Core API 确实不能支撑 actual-entrypoint reopen，停止并回填精确 blocker。
- `i1-i2-core-session-project-context-update-rootcause3-p1` (accepted) — Core PC-F rootcause3，基于已接受 AlembicCore@f82bfec58e36ea71e575a23bee356623a7a28381，只补一个缺失的 producer 能力：GenerateSession 当前仅在 constructor 复制 projectContext，toSnapshot 只是读取，Main 的 dependency/module actual-entrypoint receipts 在 session 创建后产生却无法触发持久化；新进程重开因此只剩 plan/recipe。

最小实现合同：
1. 在既有 GenerateSession/GenerateSessionManager 责任内提供受控的 projectContext replace/update API，保持同 session id、projectRoot、lease、dimensions 和已有进度不变，并调用既有 onChange/#persist；禁止调用方选择 dataRoot、禁止第二 store/journal/registry。
2. 更新值必须作为持久化 snapshot 的权威值；调用返回后同进程 toSnapshot 与 fresh GenerateSessionManager(dataRoot) 重开完全相同。不得依赖后续 dimension completion 等偶然 onChange 才落盘。
3. 防止外部对象在未调用更新 API 时通过浅引用静默改变 session；至少 projectContext 更新输入和 toSnapshot 输出不能成为绕过 onChange 的可变后门。
4. 加回归：创建 session(carrier receipts=[plan,recipe])→受控更新为 [plan,recipe,dependency]→再更新 module→同 id fresh-process/file reload 得到四项；未调用更新时修改读出对象不得改变内存或磁盘；现有 session lease/complete/expiry 行为不回归。
5. 仅提交 AlembicCore 一个干净 commit，运行 build/check 与 GenerateSession focused/full tests、git diff --check，回填 commit/命令/原始 runtime JSON 或日志。

禁止范围：不改 Alembic/Plugin/Agent/Dashboard/Test/Wakeflow；不实现 Main carrier、PC-F counters、strict journal、I3+；不新建存储。若现有 session contract 不能在不破坏兼容性的前提下支持，回填精确 blocker，不用旁路文件。
- `i2-1-alembic-main-pcf-adapters-rootcause3-p1` (accepted) — Alembic Main PC-F rootcause3，基于干净的 Alembic@7a8c1f1364efca2001fd8452bb919ca7ec735c41，并实际加载已复验的 AlembicCore@4a3e8bab3fc7aa05ef7b680593599634422513aa；只修整组接受前剩余的两个 Main 根因。

完成合同：
1. 在 Plan/Recipe 已创建 session 后，dependency-graph 与 module-coverage 的实际生产入口必须通过 Core GenerateSession.replaceProjectContext()（或同一已接受边界）把各自产生的 receipt/counter 写回同一个 session id；每次调用返回即已持久化，fresh GenerateSessionManager/新进程依次看到 plan+recipe -> +dependency -> +module。不得把 toSnapshot() 副本当可写 session，也不得靠后续偶然 onChange 落盘。
2. 四个 Main receipt 必须各自由真实入口产生、共享同一 artifact identity/scope/vector，并保存在有界 carrier 中；禁止中央循环预制四个 receipt 作为主证明。Main 只证明自己的四行，不得伪造 Plugin 的 dimension-completion，也不得宣称五行全局 lineage 已完成。
3. 删除 buildCertifiedModules 中对无 owner eligible source 的 `repo:<repoId>` 静默合成，expectedOwnerModuleCount 不得复用合成规则制造假零。含 owned src/owned.ts 与 unowned loose.ts 的 fixture 必须 fail closed；成功返回的 synthesizedProjectScopeFactCount=0 必须真实表示没有发生合成。
4. capture-time 空 legacy instrumentation 不能作为 post-open 可达性证明；严格成功以实际入口执行后的 carrier/receipt/counter 与 fresh reload 为准。保留既有 relativeRoot、路径约束、ModuleService certified 重绑定、malformed presenter/graph fail-closed、>12 files、>80 modules、control root+4 members 的已验语义。
5. 回归必须执行真实生产顺序，证明同 session 四次演进、每阶段 fresh reload、stale/hash/scope/vector mutation fail closed、carrier 无文件正文且有界；不得仅手工构造 reducer event 或直接循环中央 helper。

验证并回填：npm run build:check；focused ProjectContext/plan/session/dependency/ModuleService tests；npm run check；完整 npm run test；git diff --check。提交 Alembic 单一干净 commit，提供 parent/commit/tree、changed files/symbols、RED/GREEN、原始 runtime JSON/日志路径与 hash、两阶段自审。

禁止范围：不改 Core/Plugin/Agent/Dashboard/Test/Wakeflow；不改 reset/阶段顺序；不做 I2.2/I3+；不建第二 store/journal/runtime；不放宽现有门禁。若真实入口无法在保持 session identity 下消费新 Core API，停止并回填精确 blocker。
- `i2-2-alembic-plugin-pcf-graph-map-p1` (accepted) — 执行唯一需求的 I2.2 AlembicPlugin combined package，基线必须是干净的 AlembicPlugin@bdcb3de1d6aa35732a141cbd699b4007b6bbafa6/tree 2112821431e6364457b65902d16dbdc76f49f281，并加载已接受的 AlembicCore@4a3e8bab3fc7aa05ef7b680593599634422513aa 与已验 Main PC-F lineage。范围仅为 PC-F3 Plugin migration + PC-F4 Graph/Map truth；不进入 I3+、R0+、publication/five-tool serving 隔离或 Test。

先从实际加载产物复现并归因，不因设计文字预设所有路径都坏。当前控制端已确认至少这些严格旁路仍在源码中：plan-tool 直接 collectPlanProjectContext；project-context-analysis 自建无 certified carrier 的 session 且默认 25/3/8；Plugin ModuleService 有 repo fallback 和 slice(0,24)；dimension completion 对 module/receipt/coverage 缺失可跳过或吞错；tool-router 缺 module axis 时返回空并允许 Core passthrough。对 Graph/Map 则必须用终态运行证据决定修复，不能用代码关键词或旧报告代替复现。

完成合同：
1. Plugin 的 plan/cold-start/实际 HostAgent session、dimension-completion、ModuleService、plan-confirm/module-axis callers、tool-router.resolveSubmitKnowledgeModuleAxis 必须从 Core 已认证且持久化重开的同一 artifact/session carrier 取事实；共享 artifactId、facts/source-vector/scope/binding，base certification 不被 post-open receipt 改写。strict path 不再执行 competing project-context-analysis/collectPlanProjectContext、direct ProjectContext、raw filesystem、synthetic ProjectScope、24-cap 或 empty-axis/Core-passthrough；legacy/manual 路径保留但必须按可达性明确分支。
2. dimension-completion 的实际入口产生全局第五个 artifact-bound consumer receipt；required module axis、receipt 或持久化失败必须在 strict run 返回前阻断，不能 advisory skip/吞错。ModuleService 对完整 canonical owner axis 守恒，>24 fixture 不截断、不合成；tool-router/plan-confirm 读取同一轴，missing axis fail closed。knowledge-rescan 必须是 applicable 或带真实 reachability 证据的 typed N/A，不能未登记第二采集。
3. SP fixture 固定 root + AOXFoundationKit/AOXNetworkKit/AOXPlayer/AOXUIKit 四包，与 Core scope receipt 的 repoId+relativeRoot+revision exact tuple 对账；.gitmodules 仅作为 Graph/shared live probe，extra/missing/revision drift 都失败，不能成为第二权威。
4. Graph/region 保持 request-scoped live-source、Recipe-free。用 terminal receipt 区分 progress；对账 certified/observed vector、canonical scope、terminalSemanticOutputHash 与 repo/module identity。修复经复现确认的 partial/degraded/suppressed error、重复 canonical scoped root/path、遗漏 repo；每个 canonical repo 恰一 node，command script 不得成为 repo。mtime-only 允许 diagnostic fingerprint 变但 semantic hash/vector稳定。
5. Recipe Map 必须把 mountAccountingCompleteness 与 projectCoverageStatus/final coverage 明确分开；没有 final coverage receipt 时不得把 mounts+deferred 守恒叫项目/知识覆盖完成。continuation 按 node/edge/mount 等类型报告 shown/total/remaining/cumulative，后页或 mounts-first 页不能把零 base nodes 冒充 final。只修 PC-F 事实/契约，不实现后续 publication resolver。
6. 回归覆盖 actual entrypoints、每阶段 fresh manager/新进程重开、carrier 有界且无 frozen正文、>24、missing axis、hash/vector/scope/stale mutation、SP root+4 tuple drift、Graph terminal/progress/suppressed/duplicate/script taxonomy、Map 分页/账目命名。生成只读 MR5/SP runtime JSON和审计报告，明确 Main四行+Plugin第五行才是全局 lineage候选，不能自行宣称 PCFBaselineReceipt 或 whole-demand 完成。

验证与回填：npm run build:check；相关 focused tests；npm run check；由于 Plugin check 不含测试，另跑 npm run test:unit 和相应 integration/full test；按改动运行 smoke:codex-plugin、verify:codex-plugin、verify:plugin-distribution、verify:codex-session 与边界门禁。只提交 AlembicPlugin 一个干净 commit，提供 parent/commit/tree、loaded Core/package hashes、changed files/symbols、RED/GREEN、原始 runtime/evidence 路径+SHA-256、两阶段自审和剩余风险。

禁止：不改 Core/Main/Agent/Dashboard/Test/Wakeflow，不更新 vendor/gitlink/marketplace cache，不发布，不建第二 store/session/Graph registry/ProjectContext collector，不让 Graph 消费 Recipe，不实现 I3+ Plan compiler、Recipe gates、reset/public CAS/vector/public五工具链。
- `i2-2-alembic-plugin-pcf-graph-map-rework1-p1` (accepted) — 执行 AlembicPlugin I2.2 rework1，基线必须为干净的 AlembicPlugin@729cf3b8066e69462157dab9242a392469975512/tree d0320ad2a368fa123f0c3215c9d452d69cda874b，并继续加载已接受 AlembicCore@4a3e8bab3fc7aa05ef7b680593599634422513aa。只修 PC-F3/PC-F4 当前验收根因，不进入 I3+、publication/five-tool serving、R0+ 或 Test。

首要根因：空库实际顺序先执行 Plan/ProjectContext analysis，后创建 HostAgent session；现有 strict 分支只会 reopen 已存在 carrier，而 Plugin 没有实际 artifact/carrier producer，所以首轮 Plan/cold-start 必然走 collectPlanProjectContext/direct ProjectContext。PluginStrictCounters 也只初始化为 0，未观测实际旁路。当前 runtime 证据同时显示 MR5 60s timeout、SP 仅 1 repo且无 carrier/root+4/第五 receipt，全部不可接受。

最小修复合同：
1. 在现有 Plugin prepare→Plan draft/confirm→cold-start→HostAgent session 路径上，用 Core 已接受的 Foundation 公共 API 单次 capture/put/reopen 并持久化 carrier；不能复制 Main 实现、创建第二 store/collector/registry，不能 pre-reset facts 或 double capture。空库首轮 Plan、generation、dependency graph、module coverage、dimension-completion 必须真实共享 artifactId/sourceVectorHash/facts/binding；base certification 不回写。
2. strict run 不得在 carrier 缺失时降级；legacy/manual 分支只能由明确非 strict 入口到达。对 collectPlanProjectContext、direct ProjectContext/raw FS/synthetic scope、cap、empty-axis/Core passthrough 做实际入口可观测计数或等价可复核 trace，不能把常量 0 当证据。
3. 实际 dimension-completion 产生并持久化第五 receipt；fresh manager/新进程重开后仍存在。>24 轴守恒，missing/stale axis、receipt或持久化失败在副作用前 fail closed。knowledge-rescan 使用同一 artifact 或以真实 reachability 给 typed N/A。
4. SP 必须由独立 scope manifest得到 root + AOXFoundationKit/AOXNetworkKit/AOXPlayer/AOXUIKit 四包，精确对账 repoId+relativeRoot+revision；.gitmodules 仅 live probe。MR5 与 SP 的 Graph/Map 均在有 carrier 的实际加载入口完成 terminal receipt，Graph Recipe-free、required error/duplicate/script-as-repo=0，Map 仅称 mount accounting complete且在无 final receipt 时 projectCoverageStatus 不得 complete，并证明分页逐类型守恒。
5. 新增一个实际 empty-start loaded-entrypoint/fresh-process 回归，不接受手工 fixture 预塞 carrier代替生产入口；同时保留 focused >24/missing-axis/mutation/Graph/Map 测试。重新生成只读 runtime/audit JSON，所有必需 invariant 为 true；超时、partial、unavailable 或缺 root+4 都是失败。
6. 运行 build:check、focused tests、check、test:unit、integration/full、build、smoke/verify plugin/distribution/runtime-package；verify:codex-session若仍不存在则提供真实等价 Codex session/load probe，而不是只写 N/A。提交一个新的干净 Plugin rework commit，回填 parent/commit/tree、原始命令输出、loaded hashes、actual entrypoint trace、runtime JSON SHA-256和两阶段自审。
- `i2-2-alembic-plugin-pcf-graph-map-rootcause2-p1` (accepted) — AlembicPlugin I2.2 root-cause re-derivation，基线必须为干净的 AlembicPlugin@9c8a485825197ab32e0cf264111f00fb240ce711/tree 2aef1a7a435a30b4b1a075983ed713491bc4d3c0，并继续加载已接受 AlembicCore@4a3e8bab3fc7aa05ef7b680593599634422513aa。保留本轮已证明的 Core 公共 API capture/store/reopen、五个 distinct receipts、fresh manager、MR5/SP root+4、>24 module axis 及 Graph/Map terminal 成果；禁止重写 producer/store/session/Graph/Map，不进入 I3+、reset/restore、LLM Plan cognition、Recipe/publish/vector/五工具 serving 或 Test。

根因重划：I2.2 只验收“pristine、absent-before-create/已证明 blank 的 data root 上，Plugin 五个 actual adapters 共用一个 PC-F artifact”；authorized-rebuild 的 exact reset→single facts→LLM Plan 顺序属于 I3，当前包不得实现，也不得把 testMode/cleanup:none 证据称为完整生产 rebuild。当前 rework1 仍有两个 I2 硬缺口：
1. strict-bypass 只有事件类型，没有任何生产发射点；五个 counter 从空事件集得到 0，不能观察 collectPlanProjectContext、direct ProjectContext、raw FS、synthetic scope、cap/empty-axis/Core passthrough。必须在现有实际入口做最小可观测绑定，或把旁路静态消除并用可复核 load trace/负向探针证明不可达；常量 legacyEntries/typedReason/空数组不算观测。强制触发或注入每类旁路的负向回归必须出现非零事件并 fail closed，正常 pristine strict run 才能以完整入口 denominator 得到 0。
2. 新 producer 的 inventory policy 无授权地排除整个 scripts/，而 Core 完整 inventory 与后续 build/config/migration anatomy 不能靠隐藏 source 来达成 script-as-repo=0。复用/对齐已接受 Core canonical policy 或提供 Requirement-authorized typed exclusion；MR/SP 的脚本 taxonomy 必须靠 repo/scope 分类正确，而不是从 inventory 删除 scripts。不得引入第二套 policy authority。

证据合同：新增/修正 loaded-package actual-entrypoint regression，从未预塞 carrier、pristine blank data root 开始，经过 Plan adapter、generation、dependency graph、module coverage、dimension completion，证明同 artifact/source/facts/binding、五 receipt、base hash 不变、fresh manager；负向旁路探针证明 instrumentation 可失败而非恒零；包含 scripts 中 eligible source 的 inventory/blob/owner/ref 守恒；MR5/SP root+4 与 Graph/Map terminal/audit 重新生成且无 timeout/partial。报告必须明确 I2 adapter-only 与 I3 rebuild pending，不可宣称完整 Recipe 冷启动生产链。

运行 build:check、focused loaded-entrypoint/PC-F/Graph/Map/ModuleService tests、check、test:unit、integration/full、build、plugin/distribution/runtime-package/smoke gates；若既有无关失败保留 parent/HEAD 对照。只提交一个新的干净 Plugin root-cause commit，返回 parent/commit/tree、changed symbols、原始命令摘要、loaded Core/Plugin hashes、runtime/audit SHA-256、RED/GREEN 与两阶段自审。
- `i2-2-alembic-plugin-pcf-graph-map-rootcause3-p1` (accepted) — AlembicPlugin I2.2 rootcause3 最小 Graph terminal-truth 包。基线必须为干净的 AlembicPlugin@612738bd750324577e35a40689100e1fdc450763，并继续实际加载已固定的 AlembicCore@4a3e8bab3fc7aa05ef7b680593599634422513aa。不得重开或回退 rootcause2 已通过的五个实际消费者 receipt、strict bypass 非零失败证据、Core pcf-production-source-v1 inventory/scripts 纳入、Graph identity/Map pagination 成果；不得进入 I3、reset、Recipe、publication、五工具 serving 或 Test。

唯一根因：当前 ProjectGraphProvider 将 shouldSuppressDefaultProjectContextError 命中的 ProjectContext errors 只累加 suppressedErrorCount 后丢弃；deriveGraphStatus、ProjectContextLiveProbeReceipt.blockingReasons 和 audit graphAndMapTerminalReady 均不检查这些项。真实 MR5 terminal 在 suppressedErrorCount=514、SP=104 时仍 verdict=passed；测试还固化 suppressed>0 + ready。原需求只允许已证明为 expected/advisory/typed N/A 的非 required warning 非阻断，required/confirmed/unclassified suppressed defect 必须阻断。

完成条件：
1. 用一个局部、版本清晰的 Graph suppressed-observation 分类/summary 取代匿名计数。至少守恒 observed suppressed total = typed non-blocking expected/N/A + blocking required/confirmed/unclassified；每类给稳定 code/reason/count，必要时有有界 samples/ref，不复制 Foundation registry或新建诊断平台。
2. 分类必须由现有 request kind、severity/code、路径/适用性及已加载 Foundation/ProjectScope 事实决定，禁止按消息字符串 blanket 放行所有 parser/file-flow/file-symbol/query-unavailable。无法证明 non-required 的行默认为 blocking/unclassified；required 或 confirmed defect 不能从 diagnostics 和 terminal gate 消失。
3. deriveGraphStatus、ProjectContextLiveProbeReceipt 及公开 schema/terminal semantic hash按最小必要范围接入该 summary：terminal receipt 在 blocking suppressed count>0 时 verdict=blocked，并给明确 blocking reason；expected/N/A warning 可保留非阻断但必须计入守恒。
4. audit 必须校验 MR5/SP suppressed conservation、blocking suppressed=0、unclassified=0，并在 runtime JSON 输出分类 counts；不能再用 status=ready + errorDiagnosticCount=0 证明 Graph terminal truth。
5. RED/GREEN 回归至少覆盖：一个 required/query-unavailable 或等价 confirmed error 过去被 suppression 后 passed、修复后 blocked；一个有明确 applicability 证据的 unsupported broad-source warning仍 typed non-blocking；count mutation/删除分类/守恒不等时 audit fail。重新运行真实 MR5/SP，解释现有 514/104 的分类分布；若存在 required/unclassified 项，先修真实 Graph 请求/selector/parser/ownership缺陷再允许通过，不得仅改标签。
6. 交付一个干净 AlembicPlugin commit，并返回 parent/commit/tree、changed files/symbols、RED/GREEN、runtime/audit 路径与 SHA-256、MR5/SP 分类分布、原始命令摘要及剩余风险。验证至少包含 build:check、Graph focused unit/integration、Plugin loaded PC-F focused regression、npm run check、unit/full（已知无关 Alamofire 基线须 parent/HEAD 对照）、smoke/plugin/distribution/runtime-package/startup 现有门禁及 git diff --check。

禁止修改 AlembicCore/Alembic/AlembicAgent/AlembicDashboard/Test/Wakeflow、vendor/gitlink/cache；禁止第二 Graph/ProjectContext collector/store/registry/parser；禁止把所有 suppressed warning 一律改为 error；禁止以审计标签替代真实 request output。完成只触发总控复验，不自行接受或解锁 I3。
- `i3-i7-core-production-contracts-p1` (accepted) — 执行同一 demand、同一 AlembicCore 窗口的 post-PCF NEXT combined package。开工基线必须是干净的 AlembicCore@4a3e8bab3fc7aa05ef7b680593599634422513aa/tree c7941aff9e489b65ce0b80765e26a803bfa75d76，并逐项复验 PCFBaselineReceipt 的 capabilityContract manifest hash sha256:4d3ecdc70227cd3cc613c9df26ca04107a6327aec7eebc5eae3a5856c614f008；不一致即停止，不得重建或降级 PC-F。

唯一目标：实现 Requirement §18.1 的 Core producer contracts，为后续 Agent→Alembic→Plugin 顺序提供真实可执行、可验证、可恢复的单源合同与原语，不进入其他仓库职责。

完成合同：
1. 复用现有 plan/intent/PlanNextAction，生成并封存 26 项 DimensionCatalogSnapshot；实现 ModulePlanningFactsV1、10 类 AnatomyLensCatalogSnapshot、RequiredFactApplicabilityUniverseV1、fact-query catalog、PlanningRoleVocabularyV1、strict config provenance 和纯函数 compileColdStartPlan。编译器必须对 complete module×dimension universe、eligible/excluded、typed reason、cell/lens/schedule 守恒，deferredCells=[]，无 top-N/floor/静默缩范围；缺事实/后端/config/wire 明确 fail closed。
2. 扩展现有 PlanIntent 为 PlanCognitionReceipt/Lineage 与问题 DAG、subject/scale、tool/query、support/counterevidence、priority、stop/escalate、within-cap budget/reserve 合同；实现 ColdStartPlanSelectionV2 与 ColdStartExecutionProjectionV2 的确定性校验/规范化，保留 Plan 策略但不替 LLM 发明项目问题。相同事实/认知在乱序和 fresh process 下 byte-identical；未知 ref、漏 required lens、环、饥饿、scope/cap 越权、first-N 均拒绝。
3. 在现有 ProjectContextRef/Evidence Ledger/AnalysisArtifact 上实现 dimension-free canonical subject FactRecord、direct/derived witness、multiscale HostAgentAnalysisUnit、ObservationPopulation、KnowledgeCluster、Induction/Falsification、purpose-tagged expansion 和 final schedule 的最小 schema/canonicalizer/validator；直接事实跨 view/scale/dimension 唯一，derived 必须 premise witness，schema edge 不得冒充 loaded producer。
4. 实现 §18.1 后续消费者需要的 Core 原语：TypedGateReturn、HypothesisExpressionSet、串行 admission/terminal fate；PrivateCorpusRevisionInit/Handle 的 restricted internal root factory 与跨 revision fail-closed；复用 openAlembicDatabase、Repository/Gateway/KnowledgeService/WriteZone 的 prepared deterministic-ID crash recovery；obligation-aware immutable coverage、existing vector explicit-entry inspect/build、public route/snapshot/strict-marker contracts。不得新建第二 store/DB/graph/proof/Plan/corpus service、通用 data-root override 或把未连接接口称为完成。
5. 所有严格 authoring/gate/coverage 合同遵守 evidence-bounded-no-floor：attempt/candidate 是上限不是数量目标；canonical batch barrier 全批失败而非 first-N；修复≤2；每个 non-pass 只有一个 owner/resume；replacement fixpoint 使用新的 absent-before-create revision root且旧 root 后续 open/read=0；prepared-ID crash replay不产生第二 Recipe且无 public caller ID 注入。
6. 真实落地到 public package/export 与现有内部实现，给出 consumer compatibility probes；不得只交 type/schema/fixture。回填 parent/commit/tree、changed symbols、public API/package hashes、RED/GREEN、运行报告及 SHA-256、两阶段自审与剩余风险，只提交一个干净 Core commit。

验收至少覆盖：npm run build:check、npm run check、完整 Core tests；Plan anatomy/decomposition/tool/priority/budget conservation；catalog 26 与 25/26 drift；universe/lens/fact schedule；fact ID/witness/population/cluster singleton-many-zero；backend missing/truncated；query/counterexample；clean-vs-incremental；serial admission、fate conservation、revision root isolation、Gateway prepared-ID crash、coverage/vector/public-route；public API/package/consumer imports；PC-F full regression与 git diff --check。任何现有无关基线必须给 parent/HEAD 对照，不得豁免。

禁止修改 AlembicAgent/Alembic/AlembicPlugin/AlembicDashboard/Test/Wakeflow、Design、Ghost 真实数据或 vendor/cache；禁止真实 destructive reset、LLM 调用/prompt/SOP、Main strict journal orchestration、Plugin five-tool serving、发布与 Test。完成仅返回 controller 复验，不自行接受、派发下游或声称 I3-I7 全链完成。
- `i3-i7-core-production-contracts-rework1-p1` (accepted) — 对 i3-i7-core-production-contracts-t1 做一次根因级返工，只修复 private corpus revision replacement 的真实 authority bypass，并保留 commit 9805d2ff5bb138ee0e91539a80710ccf8e74c2f0 已交付的全部 post-PCF producer contracts 与 PCFBaselineReceipt。

控制器已用 fresh temp runtime 复现：先 initialize revision-1，再通过公开 openAlembicDatabase({path:first.handle.resolver.databasePath},{workspaceResolver:first.handle.resolver}) 打开一个未注册到 handle 的 bypass runtime；initialize revision-2 并执行 PrivateCorpusRevisionHandleV1.replace 后，bypass runtime 的 SELECT 1 仍成功，而替换后新开才被 ALEMBIC_DATABASE_ROOT_REVOKED 拒绝。原因是 handle 暴露 resolver，replace 只关闭 #runtimes 内已注册实例，root marker 只拦未来 connect。

完成合同：
1. 在现有数据库/PrivateCorpusRevisionHandle 单源边界上收口 authority，保证同一旧 revision root 的全部已打开 runtime（包括替换前经公开 openAlembicDatabase 的未注册实例）在 replace 后不能继续 read/write；替换后的任何 open 也继续 fail closed。不得靠调用者约定、仅隐藏类型、仅补 marker 或只关闭已知 handle runtime 声称完成。
2. 保留 openAlembicDatabase 作为现有公共能力及普通项目/非 private revision 行为；不要新增第二 DB/store/service、全局 data-root override 或跨仓改动。允许的最小方案可以是在现有 DatabaseConnection/open runtime 层建立按 canonical dataRoot 的活动连接登记与同步 revocation，或同等可证明机制，但必须是进程内真实执行边界而非测试专用 hook。
3. `replace` 必须维持先 seal/evidence marker、再终止旧 revision runtime 的可审计语义；若关闭/撤销失败应 fail closed，不能返回 next 后仍留下旧 root 可读写。保持 cross-run、same-revision、same-fixpoint 等既有拒绝语义。
4. 新增回归：预先通过公开数据库入口打开未注册 bypass runtime，replace 后 read 和 write 都失败；registered runtime 同样失败；post-revoke open 失败；next revision 正常；普通非-private root 不被误伤。测试必须直接走公开入口，不得 mock 掉 DatabaseConnection。
5. 复验原任务 focused suites、npm run build:check、npm run check、完整 Core tests、consumer imports、PC-F baseline/hash 与 git diff --check。只提交一个干净 AlembicCore 返工 commit；回填 parent/commit/tree、RED（当前反证）/GREEN 命令原始摘要、changed symbols、故障语义、两阶段自审与剩余风险。

禁止修改其他仓库、Wakeflow/Design/Test/ledger、真实 Ghost 数据、LLM/prompt/SOP、Main orchestration 或 Plugin serving；禁止重写已完成的大合同来规避该缺陷。完成后只返回 controller 复验，不自行接受或派发下游。
- `i3-i7-core-production-contracts-rootcause2-p1` (accepted) — 对 recurringProblem 的 I3-I7 Core producer-contract lineage 做根因二次收敛，不再补 marker/调用方约定。基线为 AlembicCore@868d9070bc8907c5be691ca4024f3ed4aa1da371；保留该提交已正确实现的 canonical-root 活动 connection 登记、marker-before-close、普通 root/next revision 隔离，以及此前全部 post-PCF/PC-F 合同。

新根因证据：`ACTIVE_DATABASE_CONNECTIONS_BY_ROOT` 按 DatabaseConnection 对象登记，但公开 `connect()` 可在同一对象上连续调用；第二次调用覆盖 `this.db`，第一次返回的 native SQLite handle 不再可达于登记对象。replace/revoke 后第二个 handle 关闭，首个 handle 的 `SELECT 1` 仍返回 `{value:1}`。这证明旧 revision in-process read/write=0 仍未成立。

完成合同：
1. 在现有 DatabaseConnection 单源生命周期内建立可证明不变量：一个 DatabaseConnection 同时最多拥有一个活动 native SQLite handle。推荐最小实现是在已有活动 handle 未经 `close()` 释放时，重复 `connect()` 以稳定错误 fail closed；若选择逐 native handle 登记，必须同样简洁并证明不存在不可达 handle。不得新建 store/service/DB manager。
2. `connect()` 的失败路径不能覆盖、遗失或隐式泄漏既有 handle；`close()` 必须保持幂等并清理 root registry。正常 connect→close 后如既有公共语义允许，可再次 connect；明确测试并保持一致。不得破坏 openAlembicDatabase、普通项目或 migration 现有行为。
3. 新增同一 public boundary 回归：对 old revision 的同一 DatabaseConnection 连续 connect 两次时，第二次必须在产生第二个 native handle 前失败（或所有 native handles 均被登记）；随后 replace 后第一个 handle read/write 都失败。继续覆盖不同 DatabaseConnection 的预开 bypass、registered runtime、post-revoke open、next revision、ordinary root。
4. 给出 root-cause-note，明确为什么对象级登记不足、选定生命周期方案为何覆盖所有进程内入口，以及边界只承诺进程内活动 handle；跨进程后续 open 仍由 durable marker 阻止，不声称强杀外部进程已持有的 SQLite handle。
5. 仅修改 AlembicCore 必需源码与测试，单一干净提交。复验 focused、post-PCF、PC-F、build:check、完整 Core tests、consumer/public API、git diff --check；保留既有 ASTChunker doctrine 基线归因。回填 parent/commit/tree、同场景 RED/GREEN、changed symbols、两阶段自审和剩余风险。

禁止修改其他仓库、Wakeflow/Design/Test/ledger、真实 Ghost 数据、LLM/prompt/SOP、Main orchestration、Plugin serving 或发布面；禁止通过隐藏 resolver、删除公共入口、只改测试或继续堆 revocation marker 规避根因。完成后仅返回 controller 复验。
- `i3-i6-agent-plan-analysis-authoring-review-p1` (accepted) — 目标与基线
- 仅修改 AlembicAgent；基线必须为 clean HEAD b3c4998de7264094bb32def4c904efcae768d477 / tree bb5cadee07ac78c4134fc18cd120ecb0f5787c41。
- 本地 @alembic/core 依赖必须实际解析到已验收 Core commit 6b6341f05332a1d15f79c19a0251d41b962b71d1 / tree 84c094ee01052c91df5c9e94a50862dc61652f15；PC-F receipt hash 为 ee67374be793953728d391eee3dc7710380ccb40708ece8a02c9636525959af6。若不一致先停止回填 blocker。
- 这是一个 combined package，必须落在现有真实生产入口；禁止拆成类型、prompt、空 adapter 或未消费模块。

必须完成
1. Plan：扩展现有 PlanAgentRun/PlanIntent/PlanNextAction/PlanCognitionReceipt，生成一次初始计划并最多两次有父链的因果 repair；计划包含 question DAG、anatomy/subject/scale、冻结 capability/query ids、优先级、容量内分配、证据/反证 stop/escalate。移除 strict 路径的 top-N/top20、固定数量、executable selection 与补齐 floor；Plan 不执行工具。
2. 上下文：通过现有白名单端到端传递 manifest/plan/question/lens/fact-query/population/cluster/hypothesis/revision/evidence。扩展 analysisArtifact/ActiveContext 为结构化语义投影，只引用 Core 不可变 Evidence Ledger/PC-F ids；derivedFindingCount 必须为 0，禁止 post-hoc live reread。
3. 策略：在现有 PipelineStrategy 中实现 stage/epoch/gate、bounded expansion、fixpoint、disposition review、typed owner/resume；strict 路径禁止 skipOnFail、degrade-to-success 和未经登记的探索/counterquery。
4. Analyst：真实消费多尺度 population 与 evidence，拥有 cluster membership、归纳/反证/窄化/淘汰 disposition；保留 variants/outliers；提出 exploration/counterquery 时必须先经已验证 port 登记，再允许后续执行；不得发明事实。
5. Producer：只消费已接受且 survived/narrowed 的 hypothesis，输出 typed 0/1/N expression set；0 结果必须有 investigated-empty disposition。Producer 只提案，不得 fact-query、submit、persist、review 或直接调用任何知识写入工具；同一对象 repair 深度最多 2，第三次明确拒绝。
6. 独立评审：把可复用 mining judge 逻辑放到生产模块，由 eval script 调用，而不是脚本私有实现；实现 IndependentValueReviewer 与 InvestigatedEmptyReviewer，检查完整 authored projection、引用有效性、漂移、校准、价值与空结果 rubric，且与 Producer 职责隔离。
7. 全链去 filler/floor：覆盖实际 analyst/producer/exploration/nudge/scan/generate strict 路径；旧行为仅可留在明确的 legacy/non-strict 兼容路径，不能影响本需求链。
8. 接线与兼容：新增对象必须有真实调用者、输入输出、状态变化、失败路径和导出；保留既有 V1/public compatibility，禁止第二套 orchestrator/store/graph。

验证与证据
- 先写/更新针对真实入口的测试，再实现；至少覆盖正常链、0/1/N、多尺度/outlier、反证窄化、enrolled counterquery、fixpoint、两次 repair、第三次拒绝、漂移引用、越权工具、strict skip/degrade、filler/floor 与 legacy compatibility。
- 跑目标测试、npm run build、npm run check、完整 npm test。
- 使用仓库已有确定性 frozen fixture/mock provider 跑 AlembicWorkspace 与 BiliDili 双项目 golden、mining eval 和 judge calibration；记录 provider/model/config/golden hashes、命令、计数与报告路径。不得读取真实 API key 或联网。
- 回填真实 runtime JSON/receipt 样例，证明 Plan→Analyst→Producer→Independent Reviewer 的链路与 Core contract 消费；给出 commit/parent/tree、diff、clean 状态和两阶段自审。

禁止
- 不改 AlembicCore、Alembic、AlembicPlugin、Dashboard、Test、Design、Wakeflow 或 controller state。
- 不实现 Main 的队列/编排、数据库/日志/公有 CAS、Recipe 持久化、MCP serving、真实 LLM/API 调用。
- 不以类型存在、mock-only 未接线、静态 fixture、测试数量或分数替代生产链完成；不得声称整个冷启动需求完成。
- `i3-i7-alembic-main-strict-orchestration-p1` (accepted) — 目标与基线
- 只修改 Alembic；基线必须为 clean HEAD 32578dc3d5fe02eb8c6d24d854066d780ffb668d / tree 945b99963919bb273793c15001a1f845df5964ba。
- 本地依赖必须实际加载已接受 AlembicCore 6b6341f05332a1d15f79c19a0251d41b962b71d1 / tree 84c094ee01052c91df5c9e94a50862dc61652f15、AlembicAgent e2aa051500cc9fc5c43f1085afd5072c5981f26f / tree 459723c2ecf2919f95b2dccbaae6e4e51d46f61b，以及 PC-F receipt ee67374be793953728d391eee3dc7710380ccb40708ece8a02c9636525959af6；任一不一致立即阻塞。
- 这是 Main 一个不可拆分的 strict 生产 combined package。必须落在现有 RecipePipelineFacade、PlanSelectionGate、ColdStartWorkflow、Generate session/runner/finalizer 和现有 Gateway/Repository/Vector/public-route 上；禁止新增平行 orchestrator、第二 journal/store/DB manager 或空接口阶段。

必须完成
1. strict 入口与复原：为批准根实现 authorization→observed pointers→snapshot/restore-probe→exact allowlist reset→blank receipt；strict 模式禁止 fullReset，失败不得前进。保留非 strict/legacy 行为。
2. 单一事实与计划：blank 后只 capture/reopen 一次 certified ProjectContext artifact；Plan 必须调用已接受 Agent Plan cognition，随后用 Core deterministic compiler 生成 required universe、lens、fact-query catalog、baseline schedule 和保留 question/tool/priority/budget 的执行投影。不得再走旧 executable PlanSelection、top-N/floor、二次采集或 raw fallback。
3. 单一可恢复 journal：在现有外层 pipeline/session/finalizer 接入唯一 append-only strict journal，按 Requirement §6 状态顺序记录 receipts、typed failures、owner/resume 和补偿；fresh process 能从精确 durable substate 幂等恢复，generic skip/degrade/completed_with_errors 不得冒充 strict success。
4. 事实到 fixpoint：执行冻结 backend 的 dimension-free direct/derived fact harvest、完整 population 与 schedule expansion；调用 Agent PipelineStrategy 的 Analyst/cluster/induction/enrolled counterquery/falsification/disposition，封印 final schedule 与 analysis fixpoint。未登记 query、缺 backend、partial/error、live reread或 schema-only edge 作为完整事实均 fail closed。
5. Producer 与内容闭环：只从最终 survived/narrowed hypotheses 接收 Agent 0/1/N expression sets；按 canonical serial rows 执行 G1→non-persisting admission→expression disposition或独立G2→journal-authorized deterministic prepared ID persist→refs reconcile→contentReady→hypothesis-expression-set closure。Producer 不得 submit/persist/review；无 filler/quantity proxy；最多两次共同 semantic repair。
6. 私有 revision：每个 fixpoint 分配 confined、absent-before-create 的 private corpus root，使用 Core accepted migration/schema/identity/config/blank init receipt与 existing repository stack。若后续行触发 Analyst re-fixpoint，必须失效并封存旧 root/descendants，关闭句柄，在新物理空 root 从完整最终 eligible set 重放；旧 Recipe 不参与 dedup/coverage/snapshot，旧 root open/read=0。
7. coverage、索引与发布：完成 G3 required cell/lens closure、durability、candidate assembly、ACTIVE、sparse 验证、首个 explicit vector generation、immutable data seal、G4、serving reconciliation、FinalCoverageBinding、candidate five-tool oracle、ServingSnapshotManifest，最后执行 lock-inside-compare 的唯一 public route CAS；public/null/失败恢复必须符合 §§12-17，严禁 query-time 补数据。
8. 接线与隔离：canonical module resolver 注入 Main Gateway；refs/binding/manifest/source vector 守恒；source root 与 private revision root 分离，普通 WorkspaceResolver/global container 不可访问 revision root。IncrementalRescan/legacy readers/hooks逐项标 applicable 或 typed-unreachable，不得成为 strict 旁路。
9. 终态：新增独立 strict completion/finalized 状态，不复用 asyncFill、generic bootstrap completed 或 completed_with_errors；产出可由后续 Plugin 消费的候选/public publication handle 与完整 runtime artifact provenance，但本包不实现五个 MCP serving。

验证与证据
- 先对现有 fullReset、old PlanSelection、fire-and-forget/asyncFill、required-null/fallback、direct persistence 与旧 coverage closure保留真实 RED，再实现 GREEN。
- 至少覆盖顺序/单 capture、snapshot/reset/blank、Plan cognition+compile、fact/population/query/falsification/fixpoint、0/1/N与serial admission、prepared-ID crash、ref/contentReady、semantic re-fixpoint双 private roots、G3/G4、sparse/vector、seal、candidate oracle、manifest/public CAS、fresh-process resume、idempotency、concurrency与故障矩阵。
- 关键 fixture：先持久化 row A、后续 row 触发 re-fixpoint；证明两个 root/DB hash不同但 migrationLedgerSemanticHash相同且 BiliDili 包含 017，旧 A 对新 dedup/coverage/snapshot为零，替换 revision完整重放且无重复 Recipe。
- 运行 npm run build:check、目标 tests、npm run check、完整 npm run test、必要 integration/e2e、git diff --check；现有无关基线必须 parent/HEAD 对照，不能顺手修改。
- 只提交一个干净 Alembic commit；回填 parent/commit/tree、changed symbols、Core/Agent/package/dist/load hashes、strict runtime/journal/fault JSON及 SHA-256、原始命令计数、RED/GREEN、兼容性和两阶段自审。

禁止
- 不改 AlembicCore、AlembicAgent、AlembicPlugin、Dashboard、Design、Test、Wakeflow/controller state、真实 Ghost/data roots或真实 DeepSeek/API。
- 不实现 Plugin 五工具/Host handshake，不启动 Test，不宣称全需求或真实双项目冷启动完成。
- 不用 mock-only、类型存在、静态报告、测试数量、旧 public 数据或成功状态代替主链真实状态变化和持久化证据。
- `i3-i7-core-private-revision-rehydrate-rootcause3-p1` (accepted) — 基线与范围
- 仅修改 AlembicCore；开工必须是 clean HEAD 6b6341f05332a1d15f79c19a0251d41b962b71d1 / tree 84c094ee01052c91df5c9e94a50862dc61652f15，并保持 PCFBaselineReceipt sha256 ee67374be793953728d391eee3dc7710380ccb40708ece8a02c9636525959af6。
- 这是 Main blocker 揭示的 Core NEXT 修复，不重做既有 private-revision 设计：只补“fresh process 对同一 active existing revision 的安全重开”。不得修改 Alembic、AlembicAgent、Plugin、Design、Test 或 Wakeflow。

完成合同
1. 在现有 ProductionPersistenceContracts / PrivateCorpusRevisionResolver / @alembic/core/workspace 公共面增加一个最小 rehydrate/resume existing revision API。名称可按仓库约定，但 Main 必须通过正式 package export 直接消费。
2. 新建初始化仍严格 absent-before-create；resume 只能重开已经初始化且未 revoked 的同一 confined leaf，不能创建目录、换 root、重跑 blank init 或把新 root 当成原 substate。
3. 以已持久化 PrivateCorpusRevisionInitReceiptV1（或其完整等价输入）校验 canonical receipt hash、run/revision/fixpoint、project root/id/scope、data/parent/leaf identity、no-symlink、config、migration bundle/set/semantic hash和 SQLite integrity。缺 root、receipt/hash/identity/config/migration drift、path escape/symlink、revoked root一律 fail closed。
4. 通过同一模块私有 authority 构造并登记 active Handle/runtime，复用现有 openAlembicDatabase、root revocation registry 与 one-connection/one-native-handle 不变量；不公开 authority，不新增第二 store/DB manager/resolver platform。
5. fresh-process 回归：进程 A 初始化同一 revision、写入可识别 durable row/receipt并关闭；进程 B 只凭 approved base resolver + sealed init receipt 重开同一 leaf，证明 dataRoot/DB hash和数据一致、可继续现有 repository stack。另覆盖 tampered receipt、wrong scope/config/migration、missing/revoked leaf、existing-leaf initializer仍拒绝、replace 后旧 root仍不可读、普通 root/新 revision不回归。
6. 更新 public exports/types 与 built-artifact consumer import probe。只提交一个干净 Core commit；回填 parent/commit/tree、changed symbols、RED/GREEN、fresh-process JSON及 SHA-256、public package/dist/load hashes、focused/full tests和两阶段自审。

验证至少包含 focused private-revision/database/public-api tests、npm run build:check、npm run check、完整 npm test、consumer imports、PC-F regression与 git diff --check。不得进入 Main strict journal、Agent facade、Plugin serving、LLM/prompt/SOP、真实 Ghost data或 Test。
- `i3-i6-agent-public-strict-facade-rework1-p1` (accepted) — 基线与范围
- 仅修改 AlembicAgent；开工必须是 clean HEAD e2aa051500cc9fc5c43f1085afd5072c5981f26f / tree 459723c2ecf2919f95b2dccbaae6e4e51d46f61b。相邻已验收 Core 上游为 737ac265dc50b8ccf788c1b9103ead537d94ff06，只读使用，不修改 Core/Main/Plugin/Design/Test/Wakeflow。
- 这是 Main blocker 已复现的最小公共面修复：现有 strict 实现已验收，但合法 package imports 无法访问。不得重写 Plan/Analyst/Producer/reviewer、prompt、provider、runtime 或新增 orchestrator/store。

完成合同
1. 新增三个窄且稳定的 exact package subpaths：@alembic/agent/runs、@alembic/agent/production、@alembic/agent/evaluation。优先用顶层 curated facade 文件，避免把内部 barrel 全量暴露；不得扩大根入口，除非现有边界测试证明唯一合法方案。
2. runs 至少公开 runStrictPlanAgent 及其 RunStrictPlanAgentInput、PlanContextProjectionV1 类型；production 公开 Main 完成 strict orchestration 真正需要的现有 strict analysis projection/expansion、Analyst epoch/fixpoint、Producer lineage/expression-set/repair/gate/stage contracts，不复制实现；evaluation 公开 IndependentValueReviewer、InvestigatedEmptyReviewer 及其输入/决策类型。不要公开 module-private authority、fixture/eval runner 或 provider internals。
3. package.json exports 的 import/types 必须指向构建后真实文件；同步更新 agent-public-api-boundary 的 stable count/list/contract matrix、public signatures 与相应 smoke/fixture。原 12 个公共 subpath 的绑定集合和语义保持不变，新增面只包含批准的 strict contracts。
4. 用构建后的包从 Alembic checkout 真实导入三个新 subpath，证明上述运行时函数/class 非 undefined，并用 TypeScript consumer fixture 证明必要类型可解析；不得使用 src/dist deep import、相对跨仓 import或 test alias。
5. 负向证明 @alembic/agent/src/*、dist/*、三段及更深内部路径仍是 ERR_PACKAGE_PATH_NOT_EXPORTED；现有 legacy root/service/runtime/prompts 等入口继续通过。不得为了 consumer 方便开放 wildcard。
6. 只提交一个干净 AlembicAgent commit；回填 parent/commit/tree、changed exports/symbols、RED/GREEN actual import、built artifact/signature hashes、Alembic consumer probe、focused/full tests与两阶段自审。

验证至少包含 npm run build:check、npm run check、npm run test、lint:public-api-boundary、smoke:public-imports、smoke:public-signatures、lint:core-import-boundary、release package/pack preview 相关现有门禁及 git diff --check。不得进入 Main orchestration、Core rehydrate、LLM 外网/真实 key、Recipe persistence/serving 或 Test。
- `i3-i7-alembic-main-strict-orchestration-rework1-p1` (accepted) — 基线与边界
- 只修改 Alembic；开工必须为 clean HEAD 32578dc3d5fe02eb8c6d24d854066d780ffb668d / tree 945b99963919bb273793c15001a1f845df5964ba。
- 实际本地依赖必须解析到已接受 AlembicCore 737ac265dc50b8ccf788c1b9103ead537d94ff06、AlembicAgent b2888aab463ae996341f9eeaba6d2bf91a6a11c0 与 PC-F receipt ee67374be793953728d391eee3dc7710380ccb40708ece8a02c9636525959af6；不一致立即阻塞。
- 复用原 Main package 的完整目标。本轮只解除已复现的上游公共面阻塞，不缩小、不扩展需求，也不修改 Core/Agent/Plugin/Design/Test/Wakeflow 或真实 Ghost/data roots。

合法上游入口
- Core 只经 @alembic/core 及已接受的精确公共子路径消费，私有 revision fresh-process 恢复必须调用 @alembic/core/workspace 的 rehydratePrivateCorpusRevisionV1，不得 deep import、内联复制或伪造 receipt。
- Agent Plan 只经 @alembic/agent/runs 的 runStrictPlanAgent；analysis/expansion/epoch/fixpoint/Producer lineage/expression-set 只经 @alembic/agent/production；独立 reviewer 只经 @alembic/agent/evaluation。
- Main 向既有 AgentService/profile 注入 StrictProductionRuntimePortV1，让 Agent 内部 stage registry/PipelineStrategy 持有 stage/gate/repair authority；不得 deep import Agent 私有 builder、prompt、fixture/eval runner或重造并行 orchestrator。

必须完成
1. 在现有 RecipePipelineFacade、ColdStartWorkflow、PlanSelectionGate、Generate session/runner/finalizer 接入 authorization→observed pointers→snapshot/restore probe→exact allowlist reset→blank；strict 禁止 fullReset，失败不可前进，legacy 保持兼容。
2. blank 后只 capture/reopen 一次 certified ProjectContext artifact；调用 Agent strict Plan cognition，再由 Core compiler生成 required universe、lens、fact-query catalog、baseline schedule及完整保留 question/tool/priority/budget 的 execution projection。旧 executable PlanSelection、top-N/floor、二次采集、raw/synthetic fallback 为零。
3. 在现有外层 pipeline 增加唯一 append-only strict durable journal，记录确定状态、typed owner/resume 与补偿；fresh process 从精确 durable substate恢复。generic skip/degrade、asyncFill、completed_with_errors 不得成为 strict success。
4. 执行 frozen dimension-free direct/derived facts、完整 population、已登记 expansion/counterquery、Analyst cluster/induction/falsification/disposition并封印 analysis fixpoint；未登记 query、缺 backend、partial/error、live reread或 schema-only edge 均 fail closed。
5. 对 survived/narrowed hypotheses 接收 Agent 0/1/N expression sets，按 canonical serial row 执行 G1→non-persisting admission→disposition review或独立G2→journal-authorized prepared ID persist→refs→contentReady→expression-set closure；Producer 无 submit/persist/review 权限，无 filler/数量 proxy，semantic repair 深度最多2。
6. 每个 fixpoint 使用 confined、absent-before-create private corpus root和同一受支持 Repository/Gateway/KnowledgeService 栈。semantic re-fixpoint 必须封存旧root、fresh-process rehydrate验证后在新物理空root完整重放，旧root后续 open/read=0，旧 Recipe 不参与 dedup/coverage/snapshot。
7. 完成 G3、durability、candidate assembly/ACTIVE、sparse、首个 explicit vector generation、immutable seal、G4、serving reconciliation、FinalCoverageBinding、candidate five-tool oracle、ServingSnapshotManifest，最后 lock-inside-compare 单一 public route CAS；query-time 不补数据。
8. canonical module resolver、refs/binding/source vector/manifest 守恒；source root与private revision root分离；IncrementalRescan/legacy readers/hooks逐项 applicable 或 typed-unreachable。
9. strict 终态独立于 generic bootstrap；本包仅产出后续 Plugin 可消费的 candidate/public handle 和 runtime provenance，不实现 Plugin 五 MCP serving，不启动 Test。

关键验证
- 保留 fullReset、旧 Plan、二次 facts、fire-and-forget、required-null/fallback、direct persistence、旧 coverage closure 的 RED，再证明同链 GREEN。
- 覆盖单capture/顺序、snapshot/reset/blank、Plan+compile、fact/population/query/fixpoint、0/1/N、serial admission、prepared-ID crash、refs、双 private roots、fresh-process rehydrate、G3/G4、sparse/vector、seal、candidate oracle、public CAS、resume/idempotency/concurrency/故障矩阵。
- 关键 fixture：先持久化 row A，再由后续 row 触发 semantic re-fixpoint；两个root/DB hash不同但 migration semantic hash一致且 BiliDili含017，旧A对新dedup/coverage/snapshot为零，完整重放且无重复 Recipe。
- 运行 build:check、目标测试、check、完整 test、必要 integration/e2e、git diff --check；仅一个干净 Alembic commit，回填 parent/commit/tree、依赖/包/dist/load hashes、runtime/journal/fault JSON与SHA-256、命令计数、RED/GREEN、兼容性和两阶段自审。
- 不使用真实 DeepSeek/API key、不触碰真实数据根；如果已接受公共入口仍不足，返回精确 blocker，不得复制上游源码或改相邻仓库。
- `i3-i8-core-tool-neutral-serving-manifest-redesign1-p1` (accepted) — Design revision 97 的用户确认已删除每次 candidate 的 Plugin 五工具 pre-CAS gate；本 NEXT package 只修正该决定在 AlembicCore 公共 production contract 单源中的最后一个上游残留。基线必须是干净的 AlembicCore@737ac265dc50b8ccf788c1b9103ead537d94ff06/tree，并保留全部已接受 PC-F、private-revision、journal/public-route 合同。当前 `ProductionPersistenceContracts.ts#ServingSnapshotManifestV1`、exact input keys 与 tests 仍强制 `candidateOracleHash`，而后续 Alembic Main 被确认只能在 G4→FinalCoverage 后生成消费者中立 `ServingSnapshotValidationReceipt`，不得调用/复制 Plugin MCP 或伪填旧字段。

完成合同：在现有 `ServingSnapshotManifestV1/createServingSnapshotManifestV1` 单源内原位移除 candidate-oracle 语义，改为必填、非空、canonical-hashed 的 `servingSnapshotValidationHash`（若当前未发布 strict-v1 兼容事实允许，优先最小原位修正；不得新增平行 manifest/service/transport）。同步 exact-key 常量、public `@alembic/core/knowledge` 类型/构造器与 focused tests；证明旧 `candidateOracleHash` 输入 fail closed、新 validation hash 被 manifest hash 覆盖、字段遗漏/额外字段/空值失败，PublicKnowledgeRouteV1 仍只引用 serving manifest hash且 CAS recovery semantics 不变。全仓搜索要求 Core source/test/dist-facing contract 中不再存在正向 candidate-oracle manifest authority；不修改 Alembic/AlembicPlugin/AlembicAgent，不实现 validator、MCP handler、candidate reader/handle/path override，不进入 Test。

仓库窗口提交一个新的干净 Core commit，并回填 parent/commit/tree、changed symbols、公共 built-artifact import probe、focused RED/GREEN、`npm run build:check`、相关 contract suite、完整 Core suite 与 `git diff --check`。如果发现真实已发布兼容消费者要求版本化而不能原位修正，停止并返回具体 consumer/artifact 证据，不自行保留双重权威。
- `i3-i7-alembic-main-tool-neutral-publication-redesign1-p1` (accepted) — 同一 demand 的 Alembic Main NEXT package，精确替代 revision 97 已撤销的 per-candidate Plugin 五工具 pre-CAS 方案。执行基线必须是干净的 Alembic@7284c558ffb9e7a659f2c61e9b6a5b21f78be66f，并消费已接受、已构建的本地 AlembicCore@5ad2e0a04d27000a27927acc4de0d83418b2420a；保留既有 PC-F、authorization/reset、single capture、Plan/analysis/production、private revision、G1-G4、vector、journal/recovery 与 public-route primitives，不回退其他已实现主链。

完成合同：只在 Alembic 仓库扩展现有 strict path，不建平行 orchestrator/service/API。将 `StrictCandidateOracleV1` / `runCandidateFiveToolOracle` / `candidateOracleHash` / `CANDIDATE_ORACLE_PASSED` 的正向生产语义删除或明确重命名收窄为消费者中立的 repository/ref/sparse/vector sealed-corpus verification；不得把 CRUD、sparse、vector probes 表述为正式 Search/Graph/Recipe Map/Prime/Guard。现有 `StrictFinalizationRuntime` 生成本地、不可变、canonical-hashed 的 `ServingSnapshotValidationReceiptV1`（Main 内部 contract，不新增 Core/Plugin 公共服务），最少绑定：run/session/snapshot；candidate data、candidate coverage/G3、G4、final coverage；精确 serving-ready Recipe IDs/fingerprints 与 lifecycle/DB/file/ref/coverage conservation；tool-neutral sparse evidence；精确健康 vector generation/manifest；certified project facts/source vector/Plan/fact-query/schedule/fixpoint/expression-set lineage；Core manifest/route schema version；typed verdict/failed predicate。任何 missing/mismatch、`failed|unknown` final disposition、unready/rejected/staging member、vector mismatch 或非 canonical hash均 fail closed，保持 snapshot private。

严格顺序和持久化：journal 单源改为 `G4_READY → SERVING_RECONCILED → FINAL_COVERAGE_BOUND → SERVING_SNAPSHOT_VALIDATED → SERVING_MANIFEST_READY → PUBLIC_CAS_PREPARED（若现有 durable prepared evidence 需要此状态）→ PUBLIC_CAS_COMMITTED → FINALIZED`；`ServingSnapshotManifestV1` 只接收 accepted Core 的 `servingSnapshotValidationHash`，该 hash 必须等于 exact validation receipt hash。正式五工具不在 pre-CAS path。最终激活只允许 post-blank compare-null CAS；保留 exact prepared bytes/readback/recovery，null→one winner，stale/other payload fail closed。若当前 authorization 仍把 pre-reset route hash传入 final CAS，修正 strict path 为 null expectation，不改变 legacy 非 strict 行为。

输出与边界：删除/改名 runtime report 中会被误认为 publication surface 的 `candidateHandle` 和 candidate-oracle 字段；可以保留不可用于解析路径/打开 reader 的审计型 private-corpus evidence。不得新增 `@alembic/plugin`/AlembicPlugin 依赖、EmbeddedToolExecutor、ToolExecutionContext、MCP handler/schema、CandidatePublicationHandle、candidate reader、root/path override、private snapshot resolver、跨仓 oracle transport或 query-time write；`ResidentServiceBoundary.test.ts` 必须继续证明 Main 零 Codex-facing MCP ownership。Plugin、Core、Agent、Test 和真实项目均不修改。

测试要求：先加入 focused RED，覆盖旧字段/旧 state/旧 report surface 不再出现；validation receipt hash绑定、顺序、final-coverage失败、Recipe/ref/sparse/vector mismatch、manifest hash引用；compare-null winner/replay/conflict/crash recovery；no Plugin/MCP/candidate-reader/handle boundary。更新或重命名 `StrictCandidateOracle.test.ts` 使测试名和断言不再宣称五工具。运行 focused finalizer/journal/CAS/boundary/integration tests、`npm run build:check`、`npm run check` 与 `git diff --check`；接受构建必须记录 Alembic/Core/Agent exact dependency commits/hashes。提交一个新的干净 Alembic commit并回填 parent/commit/tree、changed symbols、RED/GREEN、运行命令/结果、无 forbidden symbol/依赖扫描、two-stage self-review和遗留风险。完成本包不等于 I8 或真实 T6 五工具通过；验收后总控才创建 Plugin exact-artifact compatibility NEXT package。
- `i8-alembic-plugin-public-serving-compatibility-redesign1-p1` (accepted) — 执行 I8 AlembicPlugin public-serving compatibility combined package。基线必须为干净的 AlembicPlugin@aba8c511be2a0de812fb8e9976062090fe49998d/tree 692dfa44f2b9b8cead5adde2755cc6396b75a643，并精确加载已接受 AlembicCore@5ad2e0a04d27000a27927acc4de0d83418b2420a 与 Alembic@bee8272f9ef2f959829577d38f27447a492e75e7 的 serving contracts/artifacts；不得猜字段或复制合同。

完成条件：1) Plugin 继续唯一拥有正式 Search、Graph、Recipe Map、Prime、Guard handlers/schemas；以 versioned serving fixtures 贯通普通 public resolver，验证 schema、handler、route 和 vector compatibility。2) route 指向已发布 snapshot 时，四个知识工具只读 CAS 后 pointed snapshot；Recipe Map 读取 exact FinalCoverageBindingReceipt，并把账目守恒与项目/知识覆盖结论分开；vector 只使用 pointed generation。3) Graph 始终 request-scoped live-source、Recipe-free，不读取候选或 pointed Recipe corpus；只允许携带 publication provenance，且 route-null 时仍正常工作。4) route-null/无 public pointer 时四个知识工具必须 typed unavailable/fail closed，不得回退旧库、候选库、默认路径或混合 session；Graph 不受该隔离影响。5) fixture 和负向回归必须覆盖 exact artifact/hash/version mismatch、stale route、route-null、跨 generation 混读、Map coverage binding 不匹配、vector mismatch，并经正式 handler 入口而非复制 handler 逻辑。

强禁止：不得访问 run-private candidate；不得新增 CandidatePublicationHandle、candidate reader/path override/cross-repo oracle transport；不得依赖或导入 Alembic Main，调用 producer/private runtime，或把 Plugin 五工具变成 pre-CAS gate；不得修改 Core/Main/Agent/Test/Wakeflow、vendor/gitlink/cache；不得运行真实项目冷启动或 T5/T6 正式矩阵。本包只关闭 I8 fixture-level public compatibility，真实 MR5/SP/Alembic/BiliDili 五工具矩阵仍由 CAS 后 T6 Test 完成。

验证与交付：先证明现有缺口/RED，再实现最小改动；运行 build/check、focused public resolver/handler/schema/route/vector tests、unit/integration、plugin distribution/runtime-package/smoke 边界门禁和 git diff --check。只提交一个干净 AlembicPlugin commit；回填 parent/commit/tree、changed files/symbols、exact loaded artifact hashes、RED/GREEN、原始命令摘要、两阶段自审、风险与剩余 Test 边界。完成后只返回总控评审，不自行启动 Test。
- `i8-core-strict-publication-marker-contract-rootcause1-p1` (accepted) — 执行 I8 上游 AlembicCore strict-publication-marker contract root-cause package。基线必须为干净的 AlembicCore@5ad2e0a04d27000a27927acc4de0d83418b2420a/tree 9cc8ed0966ad71320c792765b4c0a2a3fd1f869d。Plugin I8 的 blocked 证据已由总控复核：Main 尚未发布 physical public bundle；同时 Core 现有 StrictPublicationMarkerV1 的 projectId/projectScopeId/strictConfigReceiptHash/publicationModeVersion 字段与已确认 requirement 中持久化 marker 的 strict-v1/routeSchemaVersion/projectIdentityHash/migrationBundleHash 语义不一致，且全仓无合法生产 writer。此包只修正共享 marker schema，为下一步 Main writer 和 Plugin resolver 提供同一消费者中立权威。

完成条件：在现有 ProductionPersistenceContracts.ts / @alembic/core/knowledge export 单源内原位收敛 StrictPublicationMarkerV1 与 createStrictPublicationMarkerV1；严格表达 mode='strict-v1'、routeSchemaVersion=1、projectIdentityHash、migrationBundleHash 及已有 Core canonical hash/version 约束，具体是否保留 schemaVersion/markerHash 必须按现有 Core receipt 惯例与 Requirement 原文作最小一致实现，不新增第二 marker 类型。构造器必须 exact-key、非空、canonical-hash、固定枚举/版本并 fail closed；旧字段不得继续作为正向 authority。补 focused tests 覆盖 valid marker、旧字段、缺失/额外字段、空值、错误 mode/route version、非 canonical hash 和语义变化导致 hash 变化。同步公共 built artifact 导出并证明真实 package import 可用。

强边界：Core 只拥有 consumer-neutral schema/creator；不得加入 dataRoot、绝对/相对路径、recipe-publications namespace、文件 IO、resolver、marker writer、CAS、Main/Plugin 依赖或 runtime orchestration；不得修改 Alembic/AlembicPlugin/AlembicAgent/Design/Test/Wakeflow，亦不得把 marker 写入 provider/model config。固定 publication namespace、物理 bundle/marker 写入与 CAS 属于下一 Alembic Main package；正式 public resolver 属于后续 Plugin package。

验证与交付：先以 focused test 证明当前 contract mismatch，再实现最小原位改动；运行 targeted ProductionPersistenceContracts tests、public built-artifact import probe、npm run build:check、完整 Core suite、全仓旧 marker 字段与路径知识 boundary scan、git diff --check。仓库窗口提交一个新的干净 AlembicCore commit；回填 parent/commit/tree、changed files/symbols、RED/GREEN、原始命令摘要、两阶段自审、风险及明确的 Main/Plugin consumer handoff。若发现任何真实已发布消费者依赖旧 marker schema，停止并返回具体 caller/artifact 证据，不自行双写或版本化第二协议。
- `i7-i8-alembic-main-public-serving-bundle-rootcause1-p1` (accepted) — 执行 I7→I8 Alembic Main public-serving physical handoff root-cause combined package。基线必须为干净的 Alembic@bee8272f9ef2f959829577d38f27447a492e75e7/tree c255770449cf4a9c68227f67730861a2db1d3801，并通过 workspace file dependency 精确消费已接受 AlembicCore@d4de7edd6bbfaeb36a01b3cc243ba1617c001f65。总控已接受 Plugin blocker：当前 Main 只把 PublicKnowledgeRoute bytes 写到 caller-authorized `publicRoutePath`，而 exact DB/Recipe/config/vector data、FinalCoverageBindingReceipt、ServingSnapshotValidationReceipt 和 ServingSnapshotManifest 仍留在 private revision/checkpoint；Plugin 因此没有合法 public artifact。本包必须关闭这条真实生产链路，不能只补类型、fixture 或报告。

完成链路：1) strict init/reset 在 approved data root 内安装并读回 Core exact StrictPublicationMarkerV1，固定路径 `.asd/context/recipe-publications/marker.json`；marker 绑定 accepted projectIdentityHash + migrationBundleHash，使用 durable temp-write/file fsync/atomic rename/parent fsync/readback，且 provider/model config 内容与 hash 不变。enrolled strict + marker 缺失/损坏/不匹配 fail closed，不回退 legacy。2) 移除 strict publication 对 caller-selected `publicRoutePath` 的 authority；从 accepted dataRoot 固定推导 `.asd/context/recipe-publications/active.json` 和 `snapshots/<snapshotId>/`，严格验证 snapshotId、realpath/no-symlink/confinement，禁止任意 path override。3) 从 latest sealed PrivateCorpusRevisionHandle 组装真实 serving snapshot：`snapshots/<snapshotId>/data/` 包含 exact checkpointed SQLite、允许的 serving config artifact（遵守 sanitized/no-secret 与生产 config 不变约束）、Recipe files、所选 vector generation/store/manifest 和服务所需 refs/coverage；只复制 content-ready→ACTIVE 后的 exact serving set。所有写入完成后 WAL checkpoint、FK/integrity、逐文件 canonical manifest/hash、写 candidate-data-manifest，再把 data subtree seal；G4 后不得修改 data，seal 后失败使用新 snapshotId。4) 在 snapshot append-only metadata 区持久化并读回 exact FinalCoverageBindingReceipt、ServingSnapshotValidationReceipt、Core ServingSnapshotManifest（`manifest.json`）以及它们引用的 G3/G4/data/vector/source lineage；固定消费者可推导的文件名，Core manifest 本身不得混入 path/private fields。Recipe Map 后续必须能读取 exact final coverage，而不是从 Graph 重建。5) 只有真实 data+metadata bundle 全部存在且哈希、marker、source-vector、reconciliation、vector generation 再验证通过，才在同一 operation lock→route lock 顺序内执行 compare-null CAS；`active.json` 只含 Core PublicKnowledgeRouteV1，无 path/Recipe/ref/coverage/sparse IDs。rename-succeeded/receipt-missing 只按 exact canonical route bytes 恢复；semantic-only equality 冲突。公共 consumer 仅凭 accepted dataRoot + fixed marker/route + snapshotId 即能定位 bundle，不需要 private checkpoint/revision、run evidence 或 Main internal API。

强边界：Main 仍是 strict journal、final reconciliation、serving manifest、physical publication 和 public CAS 唯一 owner；不得新增 AlembicPlugin 依赖、正式 MCP handler/schema 副本、EmbeddedToolExecutor/ToolExecutionContext、CandidatePublicationHandle、candidate/public reader、private snapshot resolver、pre-CAS 五工具或 cross-repo oracle transport；不得把 `runCandidateFiveToolOracle` 冒充五工具，不得新增平台/service；Graph 与 Plugin 正式 resolver 不在本包实现。不得修改 Core/Plugin/Agent/Design/Test/Wakeflow/vendor/gitlink/cache；不得真实运行 MR5/SP/Alembic/BiliDili T5/T6。

验证与完成定义：先用现有集成测试/物理产物证明 RED（route 可写而 public snapshot/marker/receipts 不存在），再在现有 strict orchestrator/finalizer/journal/reset-authority 单链内最小实现。focused tests 必须实际检查 canonical marker、fixed paths、snapshot DB/Recipe/vector/metadata 文件、逐项哈希与 route→snapshot 可解析性；覆盖 marker missing/corrupt/mismatch、route/path traversal与symlink、data/manifest/final-coverage/validation/vector tamper before CAS、route remains null、post-seal mutation rejection/new snapshot、compare-null conflict、rename crash recovery、private checkpoint 删除后 public bundle 仍完整可解析。保留 ResidentServiceBoundary，扫描 Main→Plugin/MCP/candidate reader/path override 为零。运行 Core d4de build与 exact import probe、Main build:check/build、focused unit/integration、完整 Main suite、lint、repo-boundary、shared-asset drift、git diff --check；回填原始 physical artifact tree/JSON/hash probe。仓库窗口提交一个新的干净 Alembic commit，回填 parent/commit/tree、changed files/symbols、RED/GREEN、命令摘要、两阶段自审、风险与 Plugin consumer handoff。完成后只返回总控；下一步由总控验收后重新派发 Plugin I8，不自行启动 Plugin 或 Test。
- `i8-alembic-plugin-public-serving-compatibility-rootcause2-p1` (accepted) — 执行 I8 AlembicPlugin public-serving compatibility root-cause combined package。基线必须为干净的 AlembicPlugin@aba8c511be2a0de812fb8e9976062090fe49998d/tree 692dfa44f2b9b8cead5adde2755cc6396b75a643；通过 workspace file dependency 精确消费已接受 AlembicCore@d4de7edd6bbfaeb36a01b3cc243ba1617c001f65，并以已接受 Alembic Main@5a92ec24c24ba0992515fae884a510dc6c0a61f4 的真实 fixed publication bundle 为唯一 producer contract。旧 I8 blocker 已被 Main 物理产物关闭；不得继续从旧 Main 源码结论出发，也不得复用旧 blocked task。

完成链路：
1) 在现有 ProjectRuntimeContext/EmbeddedToolExecutor 公共链路内增加 ordinary strict publication resolver。只从 accepted dataRoot 推导 .asd/context/recipe-publications/{marker.json,active.json,snapshots/<snapshotId>/...}；验证 Core StrictPublicationMarkerV1、project/scope identity、migration bundle、Core PublicKnowledgeRouteV1 canonical hash/bytes、snapshotId confinement/no-symlink、ServingSnapshotManifest、candidate-data-manifest、FinalCoverageBindingReceipt、ServingSnapshotValidationReceipt 和逐文件 hash。不得接收 caller path/root override。
2) 保持兼容分流准确：marker absent 才允许现有 legacy reader；valid strict marker + active route absent 时 Search/Prime/Recipe Map/Guard 返回 typed knowledge-unavailable，绝不回退 legacy DB/vector，Graph 仍从真实 projectRoot 读取 live source；marker/route/manifest/hash/project/migration 损坏或不匹配 fail closed。ProjectRuntimeContext 公开 bounded publication provenance（mode/route state/session/snapshot/vector/source hashes），不公开绝对 snapshot path/private revision。
3) Search/Prime/Map/Guard 只使用 CAS pointed snapshot 的 exact SQLite、sanitized serving config、Recipe files、selected vector generation/store/manifest。收窄 createReadOnlySearchSnapshot 的 legacy hard-coded vector_index.asvec 假设：strict mode 必须加载 route 指定 generation/hash 并验证 present/expected IDs、model/dimension/corpus binding；不得静默退到 legacy vector 或 keyword-only 后宣称 vector compatibility。所有 public calls 保持物理只读、不产生 WAL/SHM、hit/adoption/guard write。
4) Recipe Map 从 fixed snapshot 的 exact final-coverage.json 读取覆盖结论并提供 bounded summary+continuation；Recipe mount/accounting 与 project coverage 分字段，不从 Graph、coverage_ledger 或 PC-F probe 重建 final coverage。Graph 不读取 DB/Recipe/mount，继续 live-source/Recipe-free，只携带同一 publication provenance 和 source-revision match 状态。
5) 添加可移植、versioned strict-publication-v1 serving fixture，由已接受 Core/Main synthetic physical artifact 生成或字节固定，钉住 marker/route/manifest/data/vector/final-coverage hashes。通过现有 EmbeddedToolExecutor 正式 schemas/handlers 做 I8 compatibility：Search/Prime/Map/Guard 从 pointed fixture 返回同一 session/snapshot/vector provenance，Graph 只返回 source structure；覆盖 marker absent/strict route null/corrupt marker、canonical route drift、DB/Recipe/vector/manifest/final-coverage tamper、cross-generation/cross-project、traversal/symlink、read-only。该 fixture 是兼容性证据，不得声称 MR5/SP/BiliDili 的正式五工具已通过；真实每场景矩阵只在 CAS 后 T6。
6) 对 Plugin 内 reachable public five-tool reader 及其 repository/vector hooks 做入口 inventory；strict marker 生效后禁止任何 public MCP 路径绕过 resolver 读 legacy live DB/vector，且 legacy EventBus/StagingManager/afterPublish 不得进入 public read。只修与五工具 strict serving 直接相连的入口，不新增平台/service 或无关重构。

强边界：Plugin 继续唯一拥有正式 MCP handlers/schemas；不得修改 Core/Main/Agent/Dashboard/Design/Test/Wakeflow/vendor/gitlink/cache，不得导入 Alembic Main，不得增加 CandidatePublicationHandle、candidate/private reader、Main checkpoint/revision access、path override、pre-CAS execution mode或跨仓 transport；不得把 fixture 变成第二套协议。Graph 不因 Recipe 缺失失败，四知识工具不因 live legacy DB 存在而绕过 strict route-null。不得启动真实 MR5/SP/Alembic/BiliDili Test。

验证与完成定义：先在现有正式 EmbeddedToolExecutor/五 executor 上证明 RED（strict marker/bundle 存在仍解析 legacy databasePath/vector，Map 未读取 exact final coverage），再最小修复。运行 exact Core/Main artifact/hash probe、build:check、check、test:unit、targeted public-resolver/five-handler/route-null/vector/Map/Graph tests、integration/full tests、build、smoke:codex-plugin、verify:codex-plugin、verify:plugin-distribution、verify:codex-runtime-package、scope/layer/repo/Core/retired/ring/shared-asset gates，并让既有 order-sensitive vector group 连续成组通过。仓库窗口提交一个新的干净 Plugin commit；回填 parent/commit/tree、changed files/symbols、fixture manifest/hash、五 handler schema/output/provenance矩阵、route-null/negative/raw command evidence、两阶段自审、风险和 T6 handoff。完成后只返回总控，不自行启动 Test。
- `i8-i9-alembic-runtime-config-artifact-load-boundary-p1` (accepted) — 关闭同一需求的 Test 入口硬缺口 ENV-BIND-04 与 RuntimeArtifact load gate。基于当前已接受的 Alembic checkpoint，只在现有 Main/daemon strict 生产入口增加一个最小、脱敏、可哈希的 RuntimeConfigLoadReceiptV1：在任何 target-root mutation 前，按 WorkspaceSettingsStore→process-env/provider-specific override→WorkspaceResolver vector/localEmbedding fallback 的实际优先级，记录必需 key 的 presence/source/N-A reason、effective provider/model/DeepSeek endpoint symbol/reasoning effort、prompt/SOP/budgets、embedding/vector 参数和 config hash，绝不序列化 secret value 或 durable absolute path；明确把 ALEMBIC_AI_REASONING_EFFORT 与 DeepSeekProvider 实际 config.reasoningEffort/ALEMBIC_DEEPSEEK_REASONING_EFFORT 优先级对齐，并对 ALEMBIC_DEEPSEEK_BASE_URL 做显式来源对账。复用现有 authorization/journal/enrollment，把 receipt hash 在 pre-mutation 阶段绑定并 fresh-process 可复核。同步提供 Main-owned 的 RuntimeArtifactManifest load-check 入口：消费 controller 提供的 exact accepted Core/Agent/Main/Plugin artifact manifest，校验实际 package path/version/hash/schema/dependency resolution，检测 stale dist/second copy/incompatible contract 时在 root mutation 前 fail closed；Dashboard 保持 controller trigger 决定的 required 或 not-applicable，不启动 UI。禁止第二配置平台、写/复制密钥、Plugin/MCP 依赖、改动真实 Ghost、执行 Test、扩大 Recipe gate。使用真实 main/daemon entrypoints 写 RED→GREEN 单元/集成/loaded-package/fresh-process 证据，覆盖 source precedence、missing/typed-N-A、hash drift/second copy、no-secret serialization、pre-mutation failure；运行 Alembic focused + full check/test/build/runtime-package gates。由 Alembic 窗口提交一个干净 commit，并回填 parent/commit/tree、changed symbols、RED/GREEN、原始命令摘要、脱敏 receipt/hash 样例、边界扫描和两阶段自审。完成后只回 controller 评审，不启动 Test。
- `i9-recipe-coldstart-real-production-matrix-p1` (accepted) — 执行唯一 Test card：按 MR pristine→MR rebuild→SP pristine→SP rebuild 顺序，使用 exact accepted artifacts 验证 T0–T7、Recipe 质量/覆盖、post-CAS 五工具、故障恢复与重复性；首个有效失败即停并回总控。
- `i9-alembic-main-external-setup-recovery-rootcause1-p1` (accepted) — 修复 TEST-I9-ENTRY-CONTRACT-DISCONNECTED：在现有 Alembic main/daemon strict production 入口增加一个最小、版本化、可哈希的 pre-mutation setup authority port，使 accepted artifact 能在不预创建 pristine dataRoot 的情况下消费 controller-bound external evidence/journal/operation-lock/snapshot/path-plan/pre-reset/restore authority，并为 authorized rebuild 提供 whole-root snapshot、disposable restore probe、失败后的 exact restore/reselection；不得新增第二编排器、Test adapter、Plugin/MCP 依赖或通用任意路径 API。实现必须保持现有 strict Plan→Analyst→Producer→G1/G2/G3/G4→tool-neutral serving→CAS 主链不变。Pristine：authorization/load receipts 与 fsynced StrictRunJournalHeader 位于 target 外，planned absence 通过后才 supported init，snapshot/reset 均 N/A。Rebuild：在 external root-identity lease 下冻结 pre-reset publication/reader/config/marker/baseline，whole-root snapshot 到非重叠 external root，disposable restore equality 后才 exact allowlist reset；任一 pre-CAS/T6 failure 能在 lease 下 restore snapshot 并重选精确 pre-reset reader mode。所有 source/other Ghost/config/credential/target-snapshot-evidence overlap 均 fail closed；不持久化 secret 或用户绝对路径。连接真实 HTTP/job/main/daemon 入口与 fresh-process resume，覆盖 symlink/overlap/hash drift/live-owner/partial snapshot/restore mismatch/crash before-after reset/CAS conflict。完整运行 Alembic targeted + full check/test/build/package gates，产出一个 clean commit；回填 parent/commit/tree、请求/收据 schema、实际调用链、RED→GREEN、原始命令摘要、外部拓扑脱敏收据样例、恢复等价证据、边界扫描和两阶段自审。完成后仅回总控；不得运行真实 Test 或修改任何 Ghost root。
- `i8r-alembic-main-two-stage-quiesced-observation-redesign2-p1` (accepted) — 同一 demand 的 Alembic NEXT combined package。以 clean baseline Alembic@43306622a06e2782381aa1aa257681f1f7aa8781 为父提交，完整 supersede revision 154 暴露的 V1 不可实现 equality；这不是第四次 point-fix，而是用户确认、Design 完整修订、controller revision 156 接受后的第二次且最终 requirement redesign 实现。

唯一目标：
把 strict external setup 收敛为可证明的两阶段顺序：
external authority → existing external root-identity lease → StrictRunJournalHeaderV2 durable/readback/fsync → authenticated graceful quiesce → old PID exit + observable SQLite checkpoint/close → immutable QuiescedPreResetObservationReceiptV1 → exact whole-root snapshot/restore probe → reset/blank；pre-quiesce whole-root 只作 authorization/drift/recovery provenance，绝不能再作为 post-quiesce snapshot/restore expected hash。

必须连接的真实入口和行为：
1. DaemonSupervisor 在 ordinary ready fast-path 前识别 strict execute|recover|complete；strict launch 必须启动/等待新的 strict child 身份，不得把现有 ready daemon 当成功，也不得在 child 建立 external authority/lease/header 前 terminate/restart writer。普通非 strict ready/restart/start/stop 语义不变。
2. 在现有 daemon HTTP/token/shutdown 面上增加一个最窄、hashes-only、run-bound、幂等的 strict-quiesce request/StrictQuiesceAcceptedAckV1；request 不含路径或 secret。校验 daemon token、项目/root identity 和 canonical request hash；相同 request 返回同一 ack，冲突 request fail closed 且 writer 保持运行。accepted ack 仅证明 daemon 接受停写，不冒充 checkpoint 完成。
3. ack 后调用现有 shutdown coordinator。Bootstrap checkpoint(TRUNCATE)/DB close 失败必须对 strict owner 可观测，不能继续吞错。strict child 等待旧 PID 退出、daemon.json/daemon.pid 移除和 post-exit DB/WAL/SHM 稳态，再封印 QuiescedPreResetObservationReceiptV1。
4. receipt 必须绑定 run/authority/header/lease/root identity、pre-quiesce receipt、request/ack或typed not-running/pristine-absent、exact post-quiesce rootTreeHash、checkpoint/integrity/WAL/SHM proof、protected config/marker/public-route hashes和最小 volatile delta。只有源码证明的 daemon.json/daemon.pid 删除可默认合法；daemon log 仅在运行证据证明本次 active stdio append 时精确记录。DB/WAL/SHM、daemon-entrypoint、marker、provider config和未知路径都不能被排除或宽泛 allowlist。
5. snapshot source、restore probe、restoreWholeRootCrashSafely 和 recovery equality 全部改用 post-quiesce receipt/rootTreeHash；snapshot 后不得含 stale PID/state/token。恢复完成后只允许正常 runtime 新建 fresh daemon control identity，再恢复/reselect原 reader-mode binding并验证 DB/files/vector/route/migration/config/marker/baseline-read。
6. journal execute/recover/complete 明确并实现幂等阶段：header 前；request 未接受；accepted/正在 drain；old PID exit/checkpoint 完成但 receipt 未写；receipt 已写、snapshot 未写；snapshot/restore-probe；quarantine/reset；restore；后续 public states。fresh process 只能重放同一 request hash或从已封印 receipt/snapshot继续。timeout/conflict/malformed ack/checkpoint failure/unknown delta 均禁止 SIGKILL 后继续 snapshot/reset。
7. pristine absent 仍走 typed absent receipt，不伪造 shutdown/snapshot；authorized rebuild 使用真实 live-ready daemon 覆盖上述链。不得引入第二套 daemon control、operation lock、journal、snapshot、recovery platform，不得修改 Core/Agent/Plugin/Dashboard，不得把 Plugin 五工具或 Test 带回 pre-CAS。

验收证据：
- 先增加可复现 RED，再 GREEN。至少证明：(a) live-ready strict execute 不被吞；(b) authority→lease→header durable 时间/写入先于 request、control-file 删除和 checkpoint/root mutation；(c) malformed/conflict 不停 daemon；(d) accepted ack 幂等且 request 无路径/secret；(e) checkpoint failure 可观测并阻止 receipt/snapshot；(f) post-quiesce receipt 精确绑定 whole-root/DB/WAL/SHM和最小 delta；(g) snapshot/probe/reset成功且旧 V1 equality 不再执行；(h) 在上述每个 crash stage 由新进程恢复，exact restore 后无 stale control identity；(i) ordinary non-strict daemon ready/restart/start/stop 无回归。
- 运行 focused unit/integration tests、built-entry/live-daemon runtime probes、fresh-process crash matrix、build/typecheck/lint/仓库规则要求的完整回归。原有 26-test baseline 只是回归起点，不是完成证据。
- 两阶段自审：先逐项对 Design §§2.3/8/17/18.3 与 controller revision 156；再审认证/并发/TOCTOU、durability、checkpoint failure、hash authority、恢复幂等、普通兼容性和范围。
- 只提交 Alembic 一个干净、可审查 commit；返回 parent/commit/tree、changed files/symbols、RED/GREEN、原始命令摘要、runtime/fault evidence 路径和内容 hash、逐条契约响应、兼容性及剩余风险。
- 当前不得创建、修改、准备或派发 Test。即使本包通过，也必须由 controller 独立复验、重建并核验 exact artifacts/manifest，之后仍等待用户再次明确启动并配合 Test。
- `i8r-alembic-main-two-stage-quiesced-observation-redesign2-rework1-p1` (accepted) — Alembic 单仓最小 rework，父提交 f281f8ca196d7381554c7f314a823f58ce0767d9。保持已确认的 authority→lease→V2 header→authenticated graceful quiesce→post-quiesce receipt→snapshot/restore 时序和现有 daemon/HTTP/journal/snapshot 平台不变，只闭合 fresh-process 幂等恢复：一是 receipt 已持久化但 QUIESCED_OBSERVED 尚未写入时，新进程必须补齐同一 hash-bound journal 事件后才能进入 SNAPSHOT_COPY_STARTED；二是 not-running progress 已持久化但 daemon.json/daemon.pid 尚未删完时，新进程必须按精确控制文件策略幂等补删、sync、复验，再封印 receipt。为 header 前、request-unaccepted、accepted-draining、old-PID-exited/receipt-missing、receipt-written/event-missing、snapshot/probe、reset/quarantine、restore 各阶段加入可复现的进程重启/fault matrix，证明同一 request/hash/receipt/snapshot authority 延续、未知 delta/checkpoint failure 仍 fail closed、无 SIGKILL 后继续、无 stale control identity。普通非 strict daemon 语义不变；不改 Core/Agent/Plugin/Dashboard；不创建、修改、准备或派发 Test。提交一个新的干净 Alembic commit并返回逐阶段原始运行证据。
- `i8r-alembic-plugin-mcp-meta-transport-rootcause1-p1` (accepted) — 执行 AlembicPlugin MCP clean-output transport root-cause NEXT combined package。基线必须为干净的 AlembicPlugin@d901f6236843a43765297dda500e71aac80fe2c6，并保留此前已接受的 strict public resolver、route-null isolation、pointed snapshot/vector/final-coverage、Graph live-source/Recipe-free 与五工具 handler/schema 成果。本阶段只解决真实 MCP 调用的顶层 _meta 输出契约冲突，不修改 Recipe 生产、冷启动 Plan/FACT/coverage/CAS，也不启动 Test。

已复现根因：EmbeddedToolExecutor 在 clean response 上附加顶层 _meta.alembicPublication；HostMcpServer 随后把该对象交给 serializeMcpToolResult/projectMcpToolOutput，Prime/Work 等严格 output schema 只声明 meta，不接受 _meta，真实传输报 unrecognized_keys。现有 StrictPublicFiveToolCompatibility 只直接调用 executor.execute 并断言 _meta，未经过 HostMcpServer CallToolRequest serialization，因此是假绿。

完成合同：
1. 先增加最小 RED：必须经过 HostMcpServer 注册的 CallToolRequest/serializeMcpToolResult 等价真实 transport seam，稳定复现 _meta 被 strict projector 拒绝；不能只测 EmbeddedToolExecutor。
2. 统一 clean structured payload 与 MCP CallToolResult transport metadata 的归属。publication provenance 必须保留且 schema/SDK 合法，Prime/Work/Search/Graph/Recipe Map/Guard 不得靠静默删除未知字段通过；错误结果也不得重新制造同型冲突。
3. 最小修复现有 attachExecutionContext、output projector/serializer 边界，不增加第二套输出协议、fallback serializer、特殊工具白名单或宽泛 passthrough；保持 clean output 的严格字段校验。
4. GREEN 覆盖：Prime 和 Work 的真实 transport；正式五工具的 ready publication provenance；route-null/corrupt publication error path；CallToolResult structuredContent/content/isError 与 metadata 均可被 MCP schema 接受。既有 EmbeddedToolExecutor 兼容性测试若需调整，必须说明唯一规范表示在哪里。
5. 运行 focused transport/output-contract/AgentPublicTools/StrictPublicFiveToolCompatibility tests、npm run build:check、npm run check、npm run test:unit、必要的 runtime package/distribution/plugin probes 与 git diff --check。提交一个干净 AlembicPlugin commit并回填 parent/commit/tree、changed symbols、RED/GREEN 原始命令摘要、真实 transport 返回样例、runtime package/load证据、两阶段自审和剩余风险。

强边界：不得修改 AlembicCore/Alembic/AlembicAgent/Dashboard/Design/Test/Wakeflow/vendor/gitlink/cache；不得读取 run-private candidate、增加 path override、改变 public CAS/serving semantics、启动 MR/SP/BiliDili/Alembic 真实 Test，或顺手处理本轮审计发现的其他冷启动缺陷。完成只返回 controller 复验。
- `i8r-alembic-plugin-host-neutral-mcp-errors-p1` (accepted) — 在 AlembicPlugin 精确父提交 2a36c9b6c61ec765f54c8660f510c779b2bbc05c 上实现 host-neutral MCP generic-error 优化。真实代码事实：host/results.ts、HostMcpServer.ts、local-tools/output.ts、search-output.ts 与 embedded-executor.ts 的共享路径仍输出 CODEX_MCP_ERROR 或 Codex 专属失败文案，同一 HostMcpServer 同时被 Codex 与 Claude Code 使用，而 errorHandler/error-taxonomy 已有 INTERNAL_ERROR→core.failure.internal-error。实现要求：(1) 新运行时未知工具执行失败统一发出顶层 INTERNAL_ERROR，超时、校验与 STRICT_PUBLICATION_* 等已知码原样保留；(2) failureResult 形成单一显式顶层 code 权威，禁止 data 内竞争 errorCode/覆盖歧义，可选最小签名迁移但不得新增错误平台；(3) Search/local projector 接受新 INTERNAL_ERROR；旧 CODEX_MCP_ERROR 仅作为明确 deprecated compatibility input，读取后归一为 INTERNAL_ERROR且不得重新发出；不得按 message token 推断严格码或宽泛透传；(4) normalized mcpErrorCode 继续为 core.failure.internal-error；(5) 共享可见文案改为 host-neutral“Plugin-owned tool execution failed”；(6) MCP JSON-RPC protocol errors 与 CallToolResult isError 工具执行错误保持分层；(7) 不全局重命名真实 host-specific CODEX_* 码，不改 Core/Main/Agent/Dashboard/Test/Wakeflow/vendor/cache，不改 cold-start/CAS/Recipe 语义，不派发 Test。测试先红后绿：清洁基线证明通用 throw/Claude Code 路径当前泄露 CODEX；真实 SDK Client+InMemoryTransport+HostMcpServer 证明 generic failure 的 isError、合法顶层 _meta、structuredContent/diagnostic INTERNAL_ERROR 与 core.failure.internal-error；严格码精确保留；普通消息含伪 STRICT_PUBLICATION_* 不升级；legacy CODEX_MCP_ERROR 输入归一且不重发；Codex/Claude Code 双宿主输出一致；协议错误分层不退化。静态扫描只允许 CODEX_MCP_ERROR 出现在明确 deprecated decoder/taxonomy alias，且共享文案不再含 Codex。运行 focused 既有 57/57 组与新增测试、ClaudeCodeHostRuntime/WrapHandler/registry adoption，随后 npm run test:unit、npm run check、npm run build:check、npm run build、verify:plugin-distribution、verify:codex-runtime-package、verify:codex-plugin、smoke:codex-plugin。提交一个干净 AlembicPlugin commit，并回填父提交、commit/tree/diff、红绿证据、双宿主 transport 原始断言、静态边界扫描、全套验证、runtime package/hash、两阶段 self-review、遗留风险。
- `i3-i8-core-fact-admission-authority-rootcause4-p1` (accepted) — Cold-start root-cause repair, upstream Core authority. Start only from clean AlembicCore HEAD d4de7edd6bbfaeb36a01b3cc243ba1617c001f65 and read the requirement design sections 11.2, 11.6, 12.4. Implement reusable deterministic contracts and execution authority needed by Main: (1) a strict frozen-fact backend registry/executor over certified frozen inputs, using the existing AST/grammar, ProjectContext/source graph and config-parser capabilities instead of generic regex; every declared loaded backend must be executable or fail closed, witnesses must bind the actual inspected frozen file/revision, and no hidden truncation is allowed; (2) a typed, non-writing strict admitCandidate path with immutable AdmissionReceipt that reuses validation/dedup/similarity/consolidation logic but never writes proposals or final knowledge and fails closed on advisor/backend failure; bind the receipt to candidate content, G1 receipt, private-corpus revision and inspected accepted corpus; (3) strict persistence verification requiring a matching G1 + Admission + G2 fingerprint/receipt chain before deterministic create/recovery; preserve existing non-strict create() compatibility; (4) one canonical strict snapshot-id validator/parser shared by Main and Plugin, accepting the base content-addressed id and the documented repaired UUID-suffix form while rejecting malformed or path-unsafe variants. Add focused unit/integration tests for real backend execution, unavailable-backend failure, no truncation/conservation, admission no-write and fail-closed behavior, receipt mismatch rejection, legacy create compatibility, and snapshot-id valid/invalid variants. Do not edit Alembic, AlembicAgent, AlembicPlugin or Dashboard. Produce one clean AlembicCore commit. Backfill changed files, commit hash, exact commands/output, contract/API names, migration/compatibility notes, and any consumer risks. This package is authorized by the user's explicit decision to skip Design and have the controller perform direct root-cause repair; it is not permission to change the confirmed requirement or weaken gates.
- `i3-i6-agent-iterative-analysis-rootcause2-p1` (accepted) — Cold-start root-cause repair, upstream Agent control loop. Start only from clean AlembicAgent HEAD b2888aab463ae996341f9eeaba6d2bf91a6a11c0 and read the requirement design sections 11.2 and the strict stage/fixpoint rules. Repair the strict production pipeline so Analyst exploration is a real bounded iterative epoch loop rather than one analyze call followed by an immediate fixpoint. Extend the strict runtime/stage contract so each analysis epoch can observe the latest append-only fact population and schedule, return only typed outcomes, and request an analysis_retry when accepted exploration/counterquery obligations were enrolled and executed. In PipelineStrategy, permit only the strict typed retry emitted by the strict fixpoint gate, rerun the analyze stage, enforce an explicit epoch/obligation bound, and fail closed on untyped retry, non-progress, exhausted budget, unenrolled query execution, or any Producer-side tool request. Preserve the existing StrictAnalysisExpansionPortV1 append-only enrollment and validateStrictStageToolCallsV1 authority; do not create a parallel ad-hoc loop in Main and do not weaken Producer tool prohibition. Make prompt/input construction revision-aware so a retried Analyst sees updated facts, terminal outcomes and outstanding obligations. Add focused tests for two-or-more epochs, counterquery discovery, stable fixpoint termination, retry bound exhaustion, non-progress rejection, unenrolled tool failure, and Producer tool denial, plus existing non-strict pipeline compatibility. Do not edit Alembic, AlembicCore, AlembicPlugin or Dashboard. Produce one clean AlembicAgent commit. Backfill changed files, commit hash, exact commands/output, exported contract/API names and the downstream Main integration recipe. This package is authorized by the user's explicit decision to skip Design and have the controller perform direct root-cause repair; it must preserve the confirmed completion definition.
- `i3-i8-alembic-strict-chain-integrity-rootcause4-p1` (accepted) — Cold-start root-cause repair, downstream Main integration. Do not start until the controller has accepted the upstream tasks i3-i8-core-fact-admission-authority-rootcause4-t1 and i3-i6-agent-iterative-analysis-rootcause2-t1 and supplies their commit/API evidence. Baseline before integration is clean Alembic HEAD c84c51952fe119efb89360a08adffa335322b886. Rewire the actual /api/v1/jobs/bootstrap -> RecipePipelineFacade -> GenerateWorkflow -> ColdStartWorkflow -> strict production chain: (1) delete the generic-regex/first-file/256-row harvest shortcut and execute every plan-declared frozen fact backend through the accepted Core executor, preserving full per-fact witnesses, terminal denominator and no hidden truncation; (2) drive the accepted Agent multi-epoch loop so Analyst exploration/counterqueries are enrolled, executed, appended and retried until a real stable fixpoint, never seal the expansion port before Analyst; (3) make reviewStrictProducerResult validate expression-set conservation only and move the production authority to an exact serial private-corpus flow G1 -> Core admitCandidate -> disposition review for merge/duplicate/zero OR G2 for content drafts -> persist only with matching immutable receipts; rerun/version G1 when consolidation changes content; (4) preserve one terminal ledger row for every proposal and mandatory zeroDisposition, including rejected/superseded/merged/duplicate/zero outcomes; merge/duplicate count terminal only when the target representative is final contentReady; (5) construct G3 from the real terminal ledger and actual unresolved/suppressed obligations, never hardcode empty arrays or declare investigated-empty without evidence; (6) keep collision recovery content-addressed and use Core's canonical snapshot-id contract. Replace the false-comfort single injected fixture with targeted runtime/integration scenarios covering multiple files, multiple epochs, real query execution, admission no-write, merge/duplicate target readiness, zero-expression conservation, G3 rejection on residue, receipt mismatch, retry/recovery and a snapshot suffix that the Plugin consumer fixture can resolve. Preserve daemon/HTTP failure propagation and prior non-strict behavior. Do not copy Core or Agent logic into Main, and do not edit sibling repositories. Produce one clean Alembic commit. Backfill changed files, commit hash, exact commands/output, runtime JSON/ledger evidence, actual call-chain evidence and remaining risks. This package is authorized by the user's explicit decision to skip Design and have the controller perform direct root-cause repair; no partial chain or mock-only fixture is acceptable.
- `i8-alembic-plugin-recovery-snapshot-contract-rootcause3-p1` (accepted) — Cold-start root-cause repair, downstream Plugin consumer contract. Do not start until the controller accepts i3-i8-core-fact-admission-authority-rootcause4-t1 and supplies Core's canonical strict snapshot-id API/commit. Baseline before integration is clean AlembicPlugin HEAD 66b661fcba81647968b104224138a99a134401c4. Replace the two divergent base-only regex validators in StrictPublicKnowledgeResolver and AlembicRecipeMapOutput with the accepted Core canonical strict snapshot-id validator/parser (or a single generated/imported consumer contract if direct dependency policy requires it), so Plugin accepts exactly the ids Main is permitted to persist: snapshot-<64 lowercase hex> and the collision-recovery UUID-suffix form, while rejecting uppercase hash, malformed UUID, extra suffixes, separators, traversal and path-unsafe input. Preserve strict path confinement and five-tool behavior. Add focused resolver/schema tests and a cross-repo fixture proving a Main-style repaired UUID-suffix snapshot can be resolved and surfaced through Recipe Map, plus malformed/unsafe rejection and base-id regression coverage. Do not broaden arbitrary snapshot names, do not weaken confinement, do not edit Main/Core/Agent/Dashboard, and produce one clean AlembicPlugin commit. Backfill changed files, commit hash, exact commands/output, the imported/shared contract source, and compatibility risks. This package is authorized by the user's explicit decision to skip Design and have the controller perform direct root-cause repair.
- `i3-i8-alembic-public-lineage-integrity-rootcause5-p1` (accepted) — Main strict lineage root-cause repair. Baseline is clean Alembic commit bd58c342d78596e5e7af3c0535456cb0895ea3f8; consume accepted AlembicCore 898a461385c676dd2cc467bcf8bd58226409a87b and AlembicAgent 846e93d5351545d855489586948d60f1fa5f67b5. Preserve the already verified full facts→multi-epoch→G1/admission/G2/disposition→persist→G3 behavior. Repair the concrete disconnect found by controller review: a real two-epoch/15-obligation run currently reaches PUBLIC_CAS with expansionLedgerHeadHash=hash([]), and finalCodeFactGenerationManifestHash is a new hash of fact IDs rather than analysis.factExecutionManifest.manifestHash. Carry the authoritative expansion-ledger head/final schedule receipt and exact Core CodeFactGeneration manifest identity out of the analysis runtime, persist them in the checkpoint, and bind the same values without recomputation into ServingSnapshotValidation lineage, serving manifest, PublicKnowledgeRoute, runtime report and resume/reconstruction checks. No empty/default lineage may pass after an accepted expansion; base no-expansion runs must use the canonical Core no-expansion ledger identity, not an ad hoc constant. Add focused RED/GREEN assertions for multi-epoch route equality, actual manifest equality, tampered/missing lineage rejection before PUBLIC_CAS_PREPARED, and checkpoint replay divergence. Regenerate raw runtime/checkpoint/public-bundle evidence and include a collision-recovery UUID-suffix physical snapshot for the downstream Plugin consumer; do not claim the synthetic plugin-entry tgz from RuntimeArtifactManifestFixture as exact Plugin artifact evidence. If the helper remains for isolated runtime-load unit testing, label its synthetic boundary explicitly and keep it out of cross-repo acceptance. Run focused tests, the real Facade bootstrap integration, build:check, npm run check, release package guard, diff check and Guard; produce one clean Alembic commit and a point-by-point response to revision 201.
- `i3-i8-alembic-public-lineage-integrity-rootcause5-reissue1-p1` (accepted) — Corrected immutable reissue of Main strict lineage root-cause5 repair after controller dispatch-metadata typo; scope and completion definition are unchanged. Baseline is clean Alembic commit bd58c342d78596e5e7af3c0535456cb0895ea3f8; consume accepted AlembicCore 898a461385c676dd2cc467bcf8bd58226409a87b and the actual accepted AlembicAgent commit 846e93d39c0e34b501ae4a357aa4d373b15de0d0. Preserve the already verified full facts→multi-epoch→G1/admission/G2/disposition→persist→G3 behavior. Repair the concrete disconnect found by controller review: a real two-epoch/15-obligation run currently reaches PUBLIC_CAS with expansionLedgerHeadHash=hash([]), and finalCodeFactGenerationManifestHash is a new hash of fact IDs rather than analysis.factExecutionManifest.manifestHash. Carry the authoritative expansion-ledger head/final schedule receipt and exact Core CodeFactGeneration manifest identity out of the analysis runtime, persist them in the checkpoint, and bind the same values without recomputation into ServingSnapshotValidation lineage, serving manifest, PublicKnowledgeRoute, runtime report and resume/reconstruction checks. No empty/default lineage may pass after an accepted expansion; base no-expansion runs must use the canonical Core no-expansion ledger identity, not an ad hoc constant. Add focused RED/GREEN assertions for multi-epoch route equality, actual manifest equality, tampered/missing lineage rejection before PUBLIC_CAS_PREPARED, and checkpoint replay divergence. Regenerate raw runtime/checkpoint/public-bundle evidence and include a collision-recovery UUID-suffix physical snapshot for the downstream Plugin consumer; do not claim the synthetic plugin-entry tgz from RuntimeArtifactManifestFixture as exact Plugin artifact evidence. If the helper remains for isolated runtime-load unit testing, label its synthetic boundary explicitly and keep it out of cross-repo acceptance. Run focused tests, the real Facade bootstrap integration, build:check, npm run check, release package guard, diff check and Guard; produce one clean Alembic commit and a point-by-point response to controller revision 201. The blocked predecessor made no code changes; this reissue changes only the authoritative Agent commit identity from the nonexistent typo to the accepted object.
- `i8-alembic-plugin-exact-lineage-bundle-revalidation-rootcause5-p1` (accepted) — 执行 I8 下游 exact-artifact 重验，关闭 Main rootcause5 接受后仍未完成的真实 Plugin 消费闭环。基线必须是干净 AlembicPlugin@3e6cb743b92d95dd7baf0eabd916e78e395f1203，并精确消费已接受 AlembicCore@898a461385c676dd2cc467bcf8bd58226409a87b、AlembicAgent@846e93d39c0e34b501ae4a357aa4d373b15de0d0、Alembic Main@8f7e179f11e5c48366e6a26c9d1ce519f1f45ea2 产出的物理 UUID-suffix snapshot：wakeflow-ledger/Alembic/i3-i8-alembic-public-lineage-integrity-rootcause5-reissue1-t1/runtime-evidence/recipe-publications/snapshots/snapshot-1c056d6127d3837c33332ce987a39a5d79000648a5921a8333543fc6a154e31a-<redacted>。禁止使用或接受 Main 的 synthetic plugin-entry fixture/tgz；唯一 producer evidence 是上述物理 public bundle、active route、manifest、lineage、ServingSnapshotValidation 与其逐文件 hashes。

从现有 ProjectRuntimeContext → StrictPublicKnowledgeResolver → EmbeddedToolExecutor/正式 Search、Prime、Recipe Map、Guard、Graph 链验证：1) marker/route/snapshot/manifest/lineage/SQLite/vector/final-coverage 全部从 exact bundle 解析且无 caller path override；2) expansionLedgerHeadHash=sha256:168eaa2540b57e5d4456c18bed9cd5a4f3ea29673830ccb32494b39e6c7481c4、finalExpandedScheduleHash=sha256:a4d9864adea07a0081ee714b8bc9a80e75040267a8f2e9fac6c3c74c29d34845、finalCodeFactGenerationManifestHash=sha256:924b7a2f52f36d02b1c54cc71d91b77474af72cdb687b6707a63343c2e1dfb16 保持同一 provenance，不被 Plugin 丢弃、重算或降级；3) Search/Prime/Map/Guard 只读 pointed snapshot，Graph 保持 live-source/Recipe-free 且携带同一 publication provenance；4) route-null、tampered lineage/manifest/vector/final-coverage、跨项目、traversal/symlink 必须 fail closed，公共读取零写入；5) 实际 runtime/package loaded hashes 对齐接受的 Plugin/Core/Main 组合。

先跑 exact bundle compatibility RED/现状探针：若存在 schema/resolver/handler/runtime 断链，只在 AlembicPlugin 现有链路做最小根因修复，提交一个干净 Plugin commit并复跑全部门禁；若现状已完整兼容，不得为了制造提交而改代码，回填 clean HEAD、无变更 diff 与完整可复核运行证据即可。运行 build:check、check、test:unit、正式五 handler/resolver/route-null/vector/Map/Graph targeted tests、integration/full tests、build、smoke:codex-plugin、verify:codex-plugin、verify:plugin-distribution、verify:codex-runtime-package，以及 scope/layer/repo/Core/retired/ring/shared-asset gates。不得修改 Core/Main/Agent/Dashboard/Design/Test/Wakeflow，不得启动 Test，不得把本包称为 T6 真实场景五工具最终验收。完成后只回传总控。
- `i3-i9-core-semantic-evidence-authority-rootcause6-p1` (accepted) — 同一冷启动需求的 AlembicCore 单一合并根因修复包；基线必须是 clean AlembicCore@898a461385c676dd2cc467bcf8bd58226409a87b。目标不是再加 validator 外壳，而是让现有 Core contract 成为 Agent→Main 生产链实际可消费、不可字符串自证、可崩溃恢复的单一 deterministic authority。

必须完成：
1. FactRecord/CodeFact 权威保留 normalized semantic payload/value、accepted ProjectContext witness/anchor、primary scale 与 derived premise lineage；direct fact 的 canonical subject 复用 accepted ProjectContextRef identity。不得在 Main 侧靠额外 lookup 或字符串补语义。
2. ObservationPopulation 合同与 canonicalizer 必须守恒完整 denominator：accepted/duplicate/excluded/error/inspected-no-pattern、variants、outliers、negative controls、canonical subject/parent refs、continuation/omission。zero result 缺完整 denominator 必须 unknown。
3. KnowledgeCluster 不再要求所有 observation 使用 `${factFamily}:${subject}` 预分配 mechanismKey；允许 Analyst 基于不同 subject 的真实语义证据提出同机制 cluster，同时保留 member/fact/population lineage、variants/exceptions/negative contrasts和稳定 identity。提供跨文件同构、拆分、singleton、错误合并的 RED/GREEN。
4. Counterquery/falsification/zero-hypothesis/cluster discard/semantic merge-split/Producer non-draft disposition 必须绑定 enrolled schedule obligation、真实 FactQueryExecutionReceipt/outputHash/denominator、独立 KnowledgeDispositionReview receipt 与 current fixpoint；任意 non-empty reviewerReceiptId、caller-declared complete、空执行或 self-review 不得通过。
5. reviewer/producer actor identity contract 必须承载实际 provider/model/prompt/run/load identity，供 Main/Agent 对账同一执行模型；不得只比较调用者字符串。InvestigatedEmptyDecision 必须要求非空且完整的 expected obligations、每项 terminal execution/review evidence，空 expected 集绝不 pass。
6. PrivateCorpusRevision rehydrate/checkpoint validators 接收并校验 expected current runId、analysisFixpointHash、revisionId、完整 config/runtime hash；旧 receipt 在新上下文必须 fail closed。把需要的 integrity validators 通过稳定 public subpath 导出，Main 不得 deep import。
7. 将 candidate-attempt canonical batch、serial admission ledger、HypothesisExpressionSet closure、resource-cap conservation 等现有 Core 原语接入统一 public production contract；孤立但无 production consumer 的旧合同必须有显式消费探针，不能以单测存在宣称连通。

验证：先保留 controller 已复现的 RED（跨 subject cluster mismatch、空 counterquery survived、空 investigated-empty pass、旧 revision 新 context rehydrate），再 GREEN；运行 build:check、focused strict suites、完整 npm test、npm run check、public API/layer/consumer-import gates、fresh-process/fault probes及 git diff --check。只提交一个干净 Core commit并返回 parent/commit/tree、changed symbols、真实命令与计数、probe JSON/hash、公共 API compatibility、Main/Agent 接入说明和残余风险。

非目标：不改 AlembicAgent/Alembic/AlembicPlugin/Dashboard/Wakeflow/Design/Test，不调用真实 LLM，不碰 Ghost/data roots，不重做 PC-F store/parser/graph，不新增第二 proof/analysis/recovery platform，不修改需求或数量目标。完成只返回总控复核，不自行接受或派发下游。
- `i3-i9-agent-semantic-evidence-integration-rootcause6-p1` (accepted) — AlembicAgent rootcause6 downstream integration. Start only from clean AlembicAgent@846e93d39c0e34b501ae4a357aa4d373b15de0d0 and exact local AlembicCore@96655b9bbd56f22381ffa7f48a6cd52cabc5a1da. The controller reproduced the real break: Agent build:check fails because createInductionReceiptV1 now requires currentAnalysisFixpointHash and createFalsificationReceiptV1 requires both currentAnalysisFixpointHash and a real KnowledgeDispositionReviewV1; focused strict tests have 10 failures because old population/review shapes still reach the new Core runtime.

Repair the existing Agent strict production path in place; do not create a parallel analyst/producer platform. Consume semantic authority through the stable @alembic/core/production public facade (host-agent-workflows may remain only for contracts it still legitimately owns). Agent cognition may propose cluster/induction/falsification/expression semantics, but every accepted result must be built from Core canonical facts/execution receipts, complete population denominator rows, exact cluster hash, current analysis review context, independently bound KnowledgeDispositionReviewV1 with canonical hashKnowledgeDispositionProposalV1 semantics, and conserved induction→hypothesis→falsification/disposition→producer eligibility. Remove reviewerReceiptId/string-only authority and old caller-declared structural completion from the strict path. Zero-hypothesis, discard, falsification, producer-non-draft and investigated-empty outcomes must carry the exact Core review/consumer contract; no orphan review may appear. Producer 0/1/N output must expose or derive Core HypothesisExpressionSet terminal closure compatible with the later Main candidate-attempt/admission consumer, without granting Agent persistence, admission, G1/G2, private corpus, or CAS authority.

Preserve the accepted bounded multi-epoch fixpoint and causal repair depth semantics, but rebase their identities on the new Core receipts. Update the curated @alembic/agent/production facade and built-artifact consumer probe only as needed; existing non-strict behavior remains compatible. Add RED→GREEN tests for the controller-reproduced type/runtime failures and negative rebinding/missing-chain/orphan cases. Produce a fresh-process public-facade JSON proof that goes through actual Core canonical constructors rather than handcrafted hashes. Run the evidence contract gates and return one TargetResultEnvelope for controller review only.

Hard boundaries: modify only AlembicAgent; do not edit AlembicCore, Alembic Main, AlembicPlugin, Dashboard, Design, Test, Wakeflow, vendor/gitlinks or caches; do not run real LLMs or touch real data roots; do not claim Main/public bundle/five-tool/Test/demand completion. Main remains blocked until this Agent producer is accepted.
- `i3-i9-alembic-semantic-evidence-chain-rootcause6-p1` (accepted) — Alembic Main rootcause6 single combined downstream repair. Start only from clean Alembic@8f7e179f11e5c48366e6a26c9d1ce519f1f45ea2 with exact local AlembicCore@96655b9bbd56f22381ffa7f48a6cd52cabc5a1da and AlembicAgent@3c95549bb82aa011216d6d3be0f41741ba42d5ad. The controller reproduced the real consumer break: npm run build:check has eight errors across StrictAnalysisRuntime, StrictPrivateCorpusRuntime and StrictFinalizationRuntime—old hypothesis reviewerReceiptId, incomplete population/observation shapes, old two-argument integrity validators, missing runtimeReceiptHash in private revision initialization, and obsolete InvestigatedEmpty input.

Repair the existing Main strict production chain in place; do not add a parallel analysis/proof/store/finalizer. Main must consume the exact Core executor receipts and accepted Agent semantic contracts: build complete ObservationPopulation denominator rows with executionReceipts and all accepted/duplicate/excluded/error/inspected-no-pattern/variant/outlier/negative-control/subject-parent fields; preserve Analyst-owned cross-subject cluster proposals instead of caller/string structural completion; bind current analysis review context, canonical KnowledgeDispositionReview receipts and induction→falsification/disposition→producer eligibility; derive context dispositionReviewIds from actual conserved review receipts, never hypothesis string fields.

Connect Producer 0/1/N results to Main's existing serial expression-or-disposition journal loop. Every canonical row must run G1, non-persisting admission, independent disposition review or G2, deterministic private persistence/ref reconciliation/contentReady, then close the accepted Core HypothesisExpressionSet receipt with exact terminal fates and no unresolved row. Main remains the sole owner of admission, G1/G2, private revision, persistence, coverage, finalization and CAS; Agent only supplies cognition/semantic lineage. Investigated-empty must use real executionReceipts, a current-context Core disposition review and nonempty complete obligations. PrivateCorpus initialize/rehydrate/checkpoint/integrity calls must validate exact current runId, fixpoint, revision, configReceiptHash and runtimeReceiptHash; invalidated/stale roots cannot reopen or contribute dedup/coverage.

Update the existing strict finalization and coverage path so the same accepted execution/population/cluster/induction/falsification/review/fixpoint/expression-set identities survive into G3, sealed private corpus, sparse/vector/G4, final coverage, serving snapshot validation, serving manifest and lock-inside-compare public CAS. Preserve the accepted strict state order, external recovery boundary and rootcause5 physical publication behavior. Remove or quarantine old strict fixture shapes only where the real Main consumer no longer accepts them; ordinary non-strict behavior and public package compatibility must not regress.

Add RED→GREEN tests for all controller-reproduced build failures and runtime rebinding/missing-chain/stale-context cases. Produce a fresh-process JSON proof through the actual Main strict entry/facade using the accepted public Core/Agent subpaths; deterministic host fixtures are allowed at LLM/external-data boundaries, but execution/review/population/fixpoint/admission/expression/finalization receipts may not be hand-built substitutes for the path under test. Run the full evidence contract and return one TargetResultEnvelope for controller review only.

Hard boundaries: modify only Alembic. Do not edit Core, Agent, Plugin, Dashboard, Design, Test, Wakeflow, vendor/gitlinks or caches; do not run real LLMs, real daemon quiesce/reset or touch real Ghost/data roots; do not claim Plugin/five-tool/Test/demand completion. If an accepted Core or Agent contract itself is insufficient, stop with the exact producer blocker instead of adding a Main fallback or deep import.
- `i3-i9-alembic-semantic-evidence-chain-rootcause6-rework1-p1` (accepted) — Alembic Main rootcause6 rework1, based only on clean Alembic@8d9eea3 with exact accepted Core@96655b9 and Agent@3c95549. Preserve the predecessor's correct complete population/execution denominator, analysis/fixpoint lineage, private revision context, expression-set closure, G3/G4, physical/public lineage and CAS changes. Repair the controller-proven authority defect: StrictPrivateCorpusRuntime currently fabricates pass KnowledgeDispositionReview receipts for five live merge outcomes without invoking the independent reviewer, and StrictFinalizationRuntime does the same for investigated-empty; the integration fixture hand-builds review receipts and only proves that at least one unrelated G2 review occurred.

Use the existing AgentService and Agent/Core public evaluation/production contracts. For every producer non-draft merge/duplicate terminal and every investigated-empty decision, execute the configured independent reviewer through the real host boundary, require success with no tools, parse a typed verdict and reason/evidence output, then form the Core receipt from the actual producer/reviewer provider, model, prompt, run, invocation, load and response-output hashes. Main may validate and persist the receipt but must never declare verdict=pass or synthesize a reviewer output hash from admission/ready-member structures. Missing, malformed, stale, rebound, reused, non-pass or tool-calling review must fail closed before HypothesisExpressionSet closure/G3.

Refactor the deterministic fixture so it returns reviewer responses at the LLM/external boundary rather than constructing the authority receipt for the seam under test. Track exact reviewer invocation records and assert five distinct merge reviews plus the existing G2 review in the positive Facade run; prove each terminal receipt binds the matching actual response hash and current schedule/fixpoint/execution denominator. Add negative probes for the old shortcut and authority rebindings. Add a positive all-inspected-no-pattern investigated-empty closure only if current accepted Core/Agent contracts can produce it without changing sibling repositories; otherwise return the exact producer-contract blocker rather than fabricating it. Regenerate fresh-process checkpoint/runtime evidence and replace the inaccurate 'independently receipt-backed' claim with raw invocation/receipt conservation evidence. Main-only, no new platform and no Test.
- `i3-i9-core-disposition-review-execution-authority-rootcause7-p1` (accepted) — Rootcause7 upstream repair after Main rework1 false green. KnowledgeDispositionReviewV1 and the current host boundary can be satisfied by a model that only echoes hashes: the receipt/prompt does not carry the frozen semantic evidence, Admission comparison semantics, calibrated rubric/axes, or an independently verifiable actual evaluator execution identity; Main can stamp authorized provider/model/load/run fields after a generic AgentService call. Extend the existing Core strict production authority with a typed semantic disposition-review execution/result contract that conserves the actual evaluator host invocation and decision into review authority, explicitly separates strict workflow run identity from evaluator invocation run identity, and binds the complete reviewable semantics for producer merge/duplicate/non-draft and investigated-empty. Fail closed on self-attestation, missing semantic inputs, identity/load/run/output mismatch, stale/rebound/reused authority and opaque hash-only pass. Preserve all accepted rootcause6 fact/fixpoint/execution/finalization work; Core-only, no LLM implementation, no Main/Agent edits, no Test.
- `i3-i9-core-disposition-review-execution-authority-rootcause7-rework1-p1` (accepted) — Rework Core rootcause7 in place on exact parent 438bc97bfe3010ee23f150bc2f744233ba746033. Close the two controller-proven false-green routes rather than adding more labels: mandatory zero/non-draft must pass the same reviewable G1→non-persisting Admission→independent semantic review chain and consume its terminal evidence; evaluator authority must bind the exact compiled prompt actually sent plus an Agent-owned host execution/load/result record, and evidence rows must join authoritative frozen Evidence Ledger/blob/source/fact coordinates. Reject generic/unrelated response plus post-hoc matching-field stamping, summary-only/rebound evidence, missing zero admission, orphan/replayed execution, and retain correct workflow/evaluator run separation. Core contract/authority/tests only; do not implement provider runtime or edit Agent/Main.
- `i3-i9-core-durable-agent-evidence-trust-root-rootcause8-p1` (accepted) — Repair rootcause8 in Core on exact parent 9d8384c. Preserve the fixed zero/non-draft Admission chain, but replace the publicly mintable, process-local WeakMap host authority and self-built evidence graph with a durable fresh-process-verifiable Agent/evidence trust contract. The @alembic/core/production Main-facing surface must not let an arbitrary caller register a fake adapter returning locally constructed pass JSON; exact prompt/load/invocation/output/decision must be bound to an approved producer attestation/verifier, and Evidence Ledger/blob/ref/fact coordinates must be verified against authoritative frozen store/load evidence. Core contract/authority/public facade/tests only; provider runtime and downstream Agent/Main wiring remain separately owned.
- `i3-i9-agent-durable-semantic-review-attestation-integration-rootcause8-p1` (accepted) — Integrate accepted Core V3 durable semantic-review trust root into the real AlembicAgent runtime. Replace script-only/direct KnowledgeDispositionReview synthesis with an Agent-owned production service that obtains signer/reviewer/evidence-store dependencies only from trusted bootstrap, loads exact frozen Evidence Ledger authority, invokes the independent reviewer/provider with the compiled prompt, and emits durable attestations. Keep Main policy pinning/verification for the later Alembic package.
- `i3-i9-alembic-durable-semantic-review-consumption-rootcause8-p1` (accepted) — Consume accepted Core@d0d33e49 and Agent@5f6e8b1 durable semantic-review V3 authority in the real Main strict cold-start chain. Replace Main's custom prompt/direct KnowledgeDispositionReview construction with an opaque trusted-bootstrap Agent runtime, durably pin the independent public trust policy and signed attestation in the existing strict journal/checkpoint, verify/consume it before terminal closure/persistence/G3/CAS, and prove fresh-process recovery. No second key/reviewer/evidence/journal platform and no Test.
- `i3-i9-agent-production-evidence-ledger-authority-rootcause9-p1` (accepted) — Repair the accepted Agent V3 runtime's remaining production-connectivity gap. Expose an Agent-owned public production authority that creates/reopens the real existing EvidenceLedgerStore at trusted dataRoot/job/session/dimension coordinates, provides a controlled immutable capture facet for Main strict fact evidence and a read-only strict-snapshot/get facet for durable semantic review, and keeps both facets bound to the same persisted JSONL/store identity. Refactor the existing AgentRuntime ledger construction to share this authority instead of introducing a second store. The public runtime path and fresh-process consumer must use only @alembic/agent public exports—no internal EvidenceLedgerStore import, manual fixture-only store, caller-provided fake ledger adapter, or task-supplied signer/policy. Preserve Core@d0d33e49 contracts and Main@1c16269 as read-only downstream context; no sibling edits, no Test, no Design.
- `i3-i9-agent-production-evidence-ledger-authority-rootcause9-rework1-p1` (accepted) — Repair the accepted-shape Agent production Evidence Ledger authority so its new public capture facet validates every runtime draft before sequence allocation or JSONL mutation. The controller reproduced that commit 8284aa7 accepts tool='caller.fake' and range={start:0,end:-1}, writes both to the real JSONL, and only later fails strictSnapshot; eliminate this durable-poisoning path while preserving the same public factory/facets, private EvidenceLedgerStore, redaction, strict snapshot, AgentRuntime sharing, and fresh-process reopen chain. Rejected inputs must have zero durable/in-memory side effects and the next valid capture must use the unconsumed E-n. Agent-only; Core d0d33e49 and Main 1c16269 are read-only context; no Test or Design.
- `i3-i9-agent-production-evidence-ledger-authority-rootcause9-rework2-p1` (accepted) — Close the remaining Agent public Evidence Ledger error-authority spoof: arbitrary caller-thrown Proxy/getter exceptions must never be mistaken for Agent-generated validation reasons. Normalize them to the fixed UNREADABLE_INPUT code before any append while preserving all already-proven zero-mutation, E-1, public factory/facet, fresh-process reopen, V3 attestation, and strict failure behavior. Agent-only; Core/Main/Plugin/Test/Wakeflow are read-only/out of scope.
- `i3-i9-alembic-durable-semantic-review-production-composition-rootcause10-p1` (accepted) — Main rootcause10 production composition: close the still-unimplemented durable semantic-review consumer chain on exact accepted Core d0d33e49 and Agent 2bf05831. Add stable across-process Ed25519 custody and independently pre-approved public trust-policy enrollment on the existing host config/secrets boundary; reopen the real Agent production Evidence Ledger and retained witness authority; replace every strict Main V1/custom-prompt review path with opaque Agent V3 attestation production, durable journal/checkpoint persistence and Core verification before terminal closure/persistence/G3/G4/CAS; prove fresh-process recovery with no reminting. Main-only, no second authority platform, no Test.
- `i3-i9-core-shared-harvest-evidence-authority-rootcause11-p1` (accepted) — Core rootcause11 shared-harvest semantic evidence authority: repair the accepted deterministic review contract so one authenticated physical harvest/file evidence can bind the exact set of distinct same-harvest multiscale execution receipts without duplicating evidence or dropping receipt/scale conservation. Core-only producer contract and public fixture; Agent/Main remain downstream.
- `i3-i9-agent-shared-harvest-v4-integration-rootcause11-p1` (accepted) — Agent rootcause11 downstream integration: connect the accepted Core V4 exact shared-harvest receipt-binding contract to the real AlembicAgent production Evidence Ledger, witness authority, independent reviewer, and key-custody runtime so one physical evidence harvest authorizes the complete two-scale receipt set without duplication or receipt loss. Agent-only; Main remains blocked until this producer lands.
- `i3-i9-alembic-durable-semantic-review-production-composition-rootcause11-p1` (accepted) — Main rootcause11 production composition: resume the previously blocked durable semantic-review chain on the exact accepted Core/Agent V4 contracts. Compose stable across-process key custody, independently pre-approved public trust policy, the real Agent production Evidence Ledger/witness authority and opaque Agent runtime into the existing Main strict orchestration. Preserve one physical source harvest/evidence entry while conserving distinct file/repository obligations and execution receipts in one serialized V4 attestation; durably journal/checkpoint and Core-verify/consume it before terminal closure, persistence, G3/G4 and CAS; prove fresh-process recovery without reminting. Main-only, no second authority platform, no sibling/Test/vendor changes.
- `i3-i9-core-cross-harvest-evidence-authority-rootcause12-p1` (accepted) — AlembicCore I3–I9 root-cause12 combined package。以干净 accepted Core HEAD 8602b9e6dd03a362736118a92cfd4ae24e35661e 为唯一 Core 基线，修复控制器在真实 Main Facade/Generate/ColdStart 链中独立证明的公共 semantic evidence authority 缺口：一个物理 Evidence Ledger occurrence / EvidenceEntry 在完整 final schedule 的 15 个 receipt obligations 中合法覆盖 8 个不同 harvest groups；当前 V4 canonicalizer、expected-execution binding helper 与 durable gateway 却要求该 evidence 的全部 receipt 共享单一 harvestKey、harvestReceiptHash、fileExecutionHash，导致真实 Main 在进入 Agent 前失败。此包不是普通 point-fix，也不得通过缩减 final schedule、复制 EvidenceEntry、改变 evidenceEntryId、拆分不完整 semantic request、跳过 receipt 或放宽防伪来绕过。

必须完成的最小闭环：
1. 在 Core 公共 production contract 中建立一个 evidence authority 对多个 canonical harvest groups 的版本化表达（可在单 authority 内嵌 canonical groups，或等价且更小的公开模型）；每组仍绑定 exact harvestKey/harvestReceiptHash/sourceVectorHash/canonicalSubjectRef，并保留组内 exact fileExecutionHash、witness、blob/path/range 与 receipt membership。一个物理 occurrence/EvidenceEntry/witness 只出现一次作为 authority root，不因 query family、lens、parent 或 analysis scale 被复制。
2. Core canonicalizer/constructor/expected-binding/durable store-load-consume/request validators 必须共同接受同一 evidence root 下的多个合法 harvest groups，且完整 receipt union 与 finalExpandedSchedule receipt universe 精确相等。相同 harvest 下的多 scale receipt 要继续复用一次 physical file/witness；不同 harvest group 可以有各自合法 fileExecutionHash，但不能伪装成同一 harvest。
3. 保持并加强失败关闭：拒绝缺失/重复 receipt、遗漏某个 harvest group、非 canonical group/order、receipt 从其他 session/evidence/source/subject/path/blob/witness 混入、组内 harvest/hash/file execution 不一致、跨组错误复用、tampered authority/load/attestation，以及 partial request 或 authority union 与完整 semantic request 不相等。现有 V4 未授权 mixing 负向语义不能简单删除；应改成区分“同一受信 evidence root 的完整多组集合”与真正跨 authority/source 的混入。
4. 公共 API/production exports 与 fresh-process consumer 必须可由后续 Agent 和 Main 使用；版本迁移需明确，旧 contract 如保留只能兼容旧单组输入，不能成为真实链 fallback 或第二 truth。不得在 Core 包修改 AlembicAgent、Alembic、AlembicPlugin、Dashboard 或 Wakeflow。
5. 正向 fixture 必须至少包含：一个 EvidenceEntry/物理文件；至少两个不同 harvest groups；其中至少一个 harvest group 含两个不同 analysis scales；完整 schedule/request/authority/store-load/attestation-consumption 闭环。输出并断言 evidenceRootCount=1、harvestGroupCount>=2、receipt union/count、各组 receipt/hash/fileExecution membership、physical occurrence/witness de-duplication。另保留对应对抗矩阵。
6. 先在 8602b9e6… 留下真实 RED，证明当前 V4 对该合法形状抛出 SEMANTIC_DISPOSITION_REVIEW_EVIDENCE_EXECUTION_SET_INVALID 或 V3 mismatch；实现 GREEN 后运行 focused、build/check、public exports/boundary、fresh-process 真实 consumer 与完整仓库回归。两阶段自审先逐条回答本包 1–5，再审 authority 完整性、canonicalization、replay/tamper、兼容性、性能与范围。

交付必须是一个干净 AlembicCore commit，返回 parent/commit/tree、changed files/symbols、RED→GREEN 原始命令摘要、可复核 runtime JSON/Markdown evidence、public export/consumer 证据、完整验证、兼容性与残余风险。完成后按 wakeflow-target 记录 TargetResultEnvelope 并 controller-return；不得自行派发 Agent/Main，也不得把本包完成等同于需求完成。后续严格顺序是 Core 被总控接受 → Agent 消费新 contract → Main 真实完整链重连 → 非 Test 全部接受后才允许 Test。
- `i3-i9-agent-cross-harvest-v5-integration-rootcause12-p1` (accepted) — Agent rootcause12 downstream integration: upgrade the accepted V4 production semantic-review runtime to Core V5 cross-harvest authority so the existing production Evidence Ledger loads one physical evidence root once while preserving every canonical harvest group and the complete receipt union. Wire only the real Agent provider/witness/key-custody path, preserve fail-closed durability and explicit compatibility, and prepare the exact public V5 contract for Main; Agent-only, Main remains blocked until this producer is accepted.
- `i3-i9-alembic-durable-semantic-review-production-composition-rootcause12-p1` (accepted) — Resume the blocked Main durable semantic-review production composition on accepted Core V5 and Agent V5. Through the real RecipePipelineFacade/StrictColdStartOrchestrator, consume the complete cross-harvest receipt universe for each physical Evidence Ledger root, persist and recover the serialized V5 attestation plus independently pinned public trust policy, verify/consume through Core before terminal closure, persistence, G3/G4 and CAS, and prove fresh-process recovery without reminting or review replay. Main-only; no receipt selection/regrouping/duplication, V5-to-V4 fallback, second authority platform, sibling/Test/vendor changes.
- `i3-i9-alembic-durable-semantic-review-production-composition-rootcause12-rework1-p1` (accepted) — Repair the accepted Main V5 production composition's trust-root fail-open defects without changing the V5 cross-harvest chain: make initial bootstrap distinguishable from lost/corrupt registry, reject key-present/registry-absent self-authorization, reject group/world-writable policy registry, and reject symlinked or out-of-root custody parent paths. Add controller-reproduced regression coverage and re-prove real Facade/built fresh-process V5 behavior. Main-only; no Core/Agent/Plugin/Test/vendor changes and no fallback or second authority platform.
- `i3-i9-alembic-durable-semantic-review-production-composition-rootcause12-rework2-p1` (accepted) — 修复 Alembic production semantic-review trust enrollment 的完整替换 fail-open：必须可靠区分真实首次安装与已登记后的 key+registry 全丢失/替换，后者 fail closed 且不得自生成、自批准；同时拒绝 group/world-writable custody 路径并保留既有 V5 真实链路。
- `i3-i9-core-pcf-final-artifact-contract-restoration-rootcause13-p1` (accepted) — Repair the final-artifact PC-F contract drift on exact accepted AlembicCore@c0a6633d. The controller's v5 rebuild proved that exactly one frozen row changed: dist/projectContextFoundation.js no longer matches PCFBaselineReceipt because Core@96655b9 added createProjectContextFileRef to the project-context-foundation aggregator, and Alembic@602be240 now consumes that route. In AlembicCore only, expose createProjectContextFileRef from the existing @alembic/core/project-context public facade, remove only the later project-context-foundation re-export, and update the public boundary/tests needed for that deliberate route. After build, the five PCFBaselineReceipt files must recompute exactly to sha256:4d3ecdc70227cd3cc613c9df26ca04107a6327aec7eebc5eae3a5856c614f008; @alembic/core/project-context must import the function through the built package and project-context-foundation must return to its frozen export surface. Preserve all accepted production/V5 cross-harvest semantics and package exports, do not modify Agent/Main/Plugin/Test/Wakeflow, and return one clean Core commit with parent/tree, diff, focused baseline/public-import/V5 regressions, full repository check, Guard, risks and the exact downstream interface for the subsequent Main import migration. It is expected that current Main remains a pending consumer until a separate post-acceptance Alembic package; do not patch it or claim the final artifact gate complete.
- `i3-i9-alembic-pcf-public-route-consumer-migration-rootcause13-p1` (accepted) — Migrate the one real Main consumer after accepted Core producer restoration. On exact Alembic@602be24027a06284d0622e4d0d72fdbcd0831cc8 and accepted AlembicCore@f6e3f1956d67fa1d7a0a3440c2931196d9549d06, change StrictSemanticReviewRuntimeFactory so createProjectContextFileRef comes from the existing @alembic/core/project-context public facade rather than the now-restored frozen @alembic/core/project-context-foundation facade. Keep the legitimate foundation imports on foundation, preserve all accepted durable trust enrollment/recovery and V5 15-receipt/8-group semantics, make no Core/Agent/Plugin/Test/vendor/Wakeflow edits, and return one clean Main commit with exact parent/tree, pre-fix build failure or source proof, built package import proof, focused semantic-review/trust/recovery regressions, full npm run check, Guard, two-stage review, scope and risks. This package does not certify the final artifact gate; the controller must rebuild all exact packages and rerun PC-F/load verification after acceptance.
- `i9-sp-bilidili-dashboard-coldstart-observation-2026-07-30-p1` (accepted) — Observe one user-triggered BiliDili Dashboard cleanup-rebuild cold-start without starting or mutating it; correlate job lifecycle, logs, runtime state, generated Recipe/publication/readback evidence, and the actual production code call chain; stop at first product failure or after terminal-success evidence.
- `i3-i10-core-strict-test-profile-foundation-p1` (accepted) — Phase I3-I10 upstream Core foundation. Read parent AGENTS.md, AlembicCore/AGENTS.md, the active state root, the original cold-start Original Plan/Requirement Design, and `Design/docs/current/recipe-coldstart-strict-test-profile-supplement-requirement-design-2026-07-30.md`. Implement only AlembicCore-owned deterministic/public contracts for the explicit `strict-test-dimension` profile. Required behavior: (1) keep existing `strict-production` compiler, exact 26-dimension catalog, all eligible production cells and `deferredCells=[]` byte/semantic behavior unchanged; do not repurpose `src/shared/testMode.ts`; (2) add additive versioned public contracts/exports for strict-test preflight, preview/recommendation, exactly-one selection confirmation, immutable execution projection, private terminal/report and drift/non-mutation bindings; names may adapt to repository conventions but the profile discriminator and semantics must be explicit, never inferred from env/dimension count; (3) build/validate a full preflight view from a fully compiled strict plan plus caller-supplied runtime/project/config/artifact bindings: preserve project/source/facts/catalog/applicability/query/backend/full-cell universe hashes and surface per-dimension applicable/excluded/unknown + eligible/excluded cell counts; unknown/unsupported/empty/duplicate/missing/extra/facts-lineage mismatch fails closed; (4) derive an exactly-one-dimension execution projection only from a validated preflight + hash-bound confirmation; selected cells must equal all eligible cells for that dimension, no module/file subset, no second dimension, no silent removal; unselected dimensions remain explicitly not executed by the test profile and never become production deferred/excluded/completed; default architecture recommendation is allowed only when applicable with non-empty eligible cells and supported facts/backends, otherwise provide evidence-backed alternatives; (5) define canonical hashes and drift invalidation checks for preflight, confirmation and projection, plus private completion semantics `STRICT_TEST_COMPLETED_PRIVATE` / failure; productionFinalized=false and publicRouteChanged=false are invariant fields; (6) reuse existing private serving/final coverage/G4 neutral contracts by reference/type composition where possible, without adding host orchestration, filesystem, HTTP, Dashboard, Agent runtime, Plugin, cleanup/reset or public CAS behavior to Core; (7) add focused positive/negative tests proving exact catalog and full universe remain frozen, one-dimension projection conservation, invalid selections/drift reject, production contracts/tests remain unchanged, exports resolve. Non-goals: no product data reset/write, no Main/Agent/Dashboard/Plugin edits, no briefing-only fallback, no production shortcut, no fixed BiliDili dimension before runtime preflight. Commit all Core changes in AlembicCore. Validation: targeted new tests plus existing `ColdStartProductionPlanCompiler.test.ts`/relevant plan tests; `npm run build:check`; `npm run test`; `npm run lint`; public API/layer/retired-symbol boundary checks as applicable; `git diff --check`; Alembic Guard. Backfill commit hash, changed files, exact commands/output, exported contract surface, compatibility proof, risks and downstream integration notes for AlembicAgent and Alembic.
- `i3-i10-core-strict-test-profile-foundation-rework1-p1` (accepted) — Repair the Core strict-test-dimension durable validator trust boundary found during controller review. Preserve the additive profile and all strict-production behavior. Make confirmation conserve demand/run and every derived preflight binding; make projection require the exact 26 unique dimension states, the selected state and all selected cells/counts, every unselected zero-cell state, and every immutable preflight/confirmation lineage field; validate private completion contextually against preflight/confirmation/projection and existing serving/final-coverage invariants; make audit reject forged nested terminal evidence. Add regression tests for each controller tamper probe and rerun focused/full/build/boundary checks.
- `i3-i10-core-strict-test-failure-causal-lineage-rootcause2-p1` (accepted) — Repair the root cause in the strict-test failure terminal/audit contract: it is currently stage-blind. Define a deterministic stage-to-authority matrix for every STRICT_TEST_FAILED stage. Early preflight failures must be creatable, validatable, and reportable using only authority that exists at that moment, without requiring later confirmation/projection or a successfully completed preflight receipt when it cannot yet exist. Stages after confirmation/projection must require their exact non-null predecessor hashes and matching contexts; reject null, mismatched, missing, or temporally post-failure substitute receipts. Make failure audit/report composition stage-aware instead of forcing the successful completion context shape. Preserve the five repaired completion tamper gates, exact 26-state/full-lineage validators, strict-production/legacy testMode hashes, public entrypoints, and Core-only boundary. Add controller-equivalent causal regression probes plus full validation.
- `i3-i10-agent-strict-test-execution-integration-p1` (needs-rework) — Implement the exact AlembicAgent producer contract for the strict-test-dimension profile on clean AlembicAgent@a882f61 and accepted AlembicCore@744616b. Add one narrow production adapter that validates Core preflight→confirmation→projection plus the unchanged compiled Plan, binds the existing StrictProductionRuntimePort to exactly the confirmed dimension's full eligible cell set, validates that authority at the existing stage boundary, and emits a canonical Agent-segment receipt conserving Plan/fact execution/Analyst fixpoint/Producer expression/independent-review lineage and one terminal disposition per selected cell. Reuse the existing strict stage graph and AgentService path; do not add a second profile, briefing/orchestrator/store/workspace, env/legacy test trigger, Main persistence/G4/serving, Dashboard, DaemonJob, production button, Plugin, or Test work. The sourceRef is the function-level implementation contract and is authoritative for symbols, invariants, tests, evidence, and boundaries.
- `i3-i10-core-strict-test-automatic-selection-replacement-p1` (accepted) — Replace the superseded manual strict-test confirmation contract with a deterministic Core-owned automatic-selection authority, migrate projection/resume/terminal/failure/audit lineage, and remove the unconsumed confirmation-era public route without changing production strict behavior.
- `i3-i10-agent-strict-test-automatic-selection-integration-p1` (sent) — Replace the stopped manual-confirmation Agent residue with a versioned adapter that consumes accepted Core automatic-selection authority, binds the existing real strict Agent pipeline to the exact selected cell set, and emits a canonical Agent execution receipt without adding Main, UI, daemon, or production behavior.

## Target tasks

- `i1-i2-core-project-context-foundation-t1` -> window `AlembicCore` (accepted)
- `i1-i2-core-project-context-foundation-rework1-t1` -> window `AlembicCore` (accepted)
- `i1-i2-core-project-context-foundation-rootcause2-t1` -> window `AlembicCore` (accepted)
- `i1-i2-core-content-terminal-fence-t1` -> window `AlembicCore` (accepted)
- `i1-i2-core-foundation-authority-v2-t1` -> window `AlembicCore` (accepted)
- `i1-i2-core-foundation-authority-v2-rework1-t1` -> window `AlembicCore` (accepted)
- `i2-1-alembic-main-pcf-adapters-t1` -> window `Alembic` (accepted)
- `i2-1-alembic-main-pcf-adapters-rework1-t1` -> window `Alembic` (accepted)
- `i1-i2-core-session-project-context-update-rootcause3-t1` -> window `AlembicCore` (accepted)
- `i2-1-alembic-main-pcf-adapters-rootcause3-t1` -> window `Alembic` (accepted)
- `i2-2-alembic-plugin-pcf-graph-map-t1` -> window `AlembicPlugin` (accepted)
- `i2-2-alembic-plugin-pcf-graph-map-rework1-t1` -> window `AlembicPlugin` (accepted)
- `i2-2-alembic-plugin-pcf-graph-map-rootcause2-t1` -> window `AlembicPlugin` (accepted)
- `i2-2-alembic-plugin-pcf-graph-map-rootcause3-t1` -> window `AlembicPlugin` (accepted)
- `i3-i7-core-production-contracts-t1` -> window `AlembicCore` (accepted)
- `i3-i7-core-production-contracts-rework1-t1` -> window `AlembicCore` (accepted)
- `i3-i7-core-production-contracts-rootcause2-t1` -> window `AlembicCore` (accepted)
- `i3-i6-agent-plan-analysis-authoring-review-t1` -> window `AlembicAgent` (accepted)
- `i3-i7-alembic-main-strict-orchestration-t1` -> window `Alembic` (accepted)
- `i3-i7-core-private-revision-rehydrate-rootcause3-t1` -> window `AlembicCore` (accepted)
- `i3-i6-agent-public-strict-facade-rework1-t1` -> window `AlembicAgent` (accepted)
- `i3-i7-alembic-main-strict-orchestration-rework1-t1` -> window `Alembic` (accepted)
- `i3-i8-core-tool-neutral-serving-manifest-redesign1-t1` -> window `AlembicCore` (accepted)
- `i3-i7-alembic-main-tool-neutral-publication-redesign1-t1` -> window `Alembic` (accepted)
- `i8-alembic-plugin-public-serving-compatibility-redesign1-t1` -> window `AlembicPlugin` (accepted)
- `i8-core-strict-publication-marker-contract-rootcause1-t1` -> window `AlembicCore` (accepted)
- `i7-i8-alembic-main-public-serving-bundle-rootcause1-t1` -> window `Alembic` (accepted)
- `i8-alembic-plugin-public-serving-compatibility-rootcause2-t1` -> window `AlembicPlugin` (accepted)
- `i8-i9-alembic-runtime-config-artifact-load-boundary-t1` -> window `Alembic` (accepted)
- `i9-recipe-coldstart-real-production-matrix` -> window `Test` (accepted)
- `i9-alembic-main-external-setup-recovery-rootcause1-t1` -> window `Alembic` (accepted)
- `i8r-alembic-main-two-stage-quiesced-observation-redesign2-t1` -> window `Alembic` (accepted)
- `i8r-alembic-main-two-stage-quiesced-observation-redesign2-rework1-t1` -> window `Alembic` (accepted)
- `i8r-alembic-plugin-mcp-meta-transport-rootcause1-t1` -> window `AlembicPlugin` (accepted)
- `i8r-alembic-plugin-host-neutral-mcp-errors-t1` -> window `AlembicPlugin` (accepted)
- `i3-i8-core-fact-admission-authority-rootcause4-t1` -> window `AlembicCore` (accepted)
- `i3-i6-agent-iterative-analysis-rootcause2-t1` -> window `AlembicAgent` (accepted)
- `i3-i8-alembic-strict-chain-integrity-rootcause4-t1` -> window `Alembic` (accepted)
- `i8-alembic-plugin-recovery-snapshot-contract-rootcause3-t1` -> window `AlembicPlugin` (accepted)
- `i3-i8-alembic-public-lineage-integrity-rootcause5-t1` -> window `Alembic` (accepted)
- `i3-i8-alembic-public-lineage-integrity-rootcause5-reissue1-t1` -> window `Alembic` (accepted)
- `i8-alembic-plugin-exact-lineage-bundle-revalidation-rootcause5-t1` -> window `AlembicPlugin` (accepted)
- `i3-i9-core-semantic-evidence-authority-rootcause6-t1` -> window `AlembicCore` (accepted)
- `i3-i9-agent-semantic-evidence-integration-rootcause6-t1` -> window `AlembicAgent` (accepted)
- `i3-i9-alembic-semantic-evidence-chain-rootcause6-t1` -> window `Alembic` (accepted)
- `i3-i9-alembic-semantic-evidence-chain-rootcause6-rework1-t1` -> window `Alembic` (accepted)
- `i3-i9-core-disposition-review-execution-authority-rootcause7-t1` -> window `AlembicCore` (accepted)
- `i3-i9-core-disposition-review-execution-authority-rootcause7-rework1-t1` -> window `AlembicCore` (accepted)
- `i3-i9-core-durable-agent-evidence-trust-root-rootcause8-t1` -> window `AlembicCore` (accepted)
- `i3-i9-agent-durable-semantic-review-attestation-integration-rootcause8-t1` -> window `AlembicAgent` (accepted)
- `i3-i9-alembic-durable-semantic-review-consumption-rootcause8-t1` -> window `Alembic` (accepted)
- `i3-i9-agent-production-evidence-ledger-authority-rootcause9-t1` -> window `AlembicAgent` (accepted)
- `i3-i9-agent-production-evidence-ledger-authority-rootcause9-rework1-t1` -> window `AlembicAgent` (accepted)
- `i3-i9-agent-production-evidence-ledger-authority-rootcause9-rework2-t1` -> window `AlembicAgent` (accepted)
- `i3-i9-alembic-durable-semantic-review-production-composition-rootcause10-t1` -> window `Alembic` (accepted)
- `i3-i9-core-shared-harvest-evidence-authority-rootcause11-t1` -> window `AlembicCore` (accepted)
- `i3-i9-agent-shared-harvest-v4-integration-rootcause11-t1` -> window `AlembicAgent` (accepted)
- `i3-i9-alembic-durable-semantic-review-production-composition-rootcause11-t1` -> window `Alembic` (accepted)
- `i3-i9-core-cross-harvest-evidence-authority-rootcause12-t1` -> window `AlembicCore` (accepted)
- `i3-i9-agent-cross-harvest-v5-integration-rootcause12-t1` -> window `AlembicAgent` (accepted)
- `i3-i9-alembic-durable-semantic-review-production-composition-rootcause12-t1` -> window `Alembic` (accepted)
- `i3-i9-alembic-durable-semantic-review-production-composition-rootcause12-rework1-t1` -> window `Alembic` (accepted)
- `i3-i9-alembic-durable-semantic-review-production-composition-rootcause12-rework2-t1` -> window `Alembic` (accepted)
- `i3-i9-core-pcf-final-artifact-contract-restoration-rootcause13-t1` -> window `AlembicCore` (accepted)
- `i3-i9-alembic-pcf-public-route-consumer-migration-rootcause13-t1` -> window `Alembic` (accepted)
- `i9-sp-bilidili-dashboard-coldstart-observation-2026-07-30` -> window `Test` (accepted)
- `i3-i10-core-strict-test-profile-foundation-t1` -> window `AlembicCore` (accepted)
- `i3-i10-core-strict-test-profile-foundation-rework1-t1` -> window `AlembicCore` (accepted)
- `i3-i10-core-strict-test-failure-causal-lineage-rootcause2-t1` -> window `AlembicCore` (accepted)
- `i3-i10-agent-strict-test-execution-integration-t1` -> window `AlembicAgent` (blocked)
- `i3-i10-core-strict-test-automatic-selection-replacement-t1` -> window `AlembicCore` (accepted)
- `i3-i10-agent-strict-test-automatic-selection-integration-t1` -> window `AlembicAgent` (completed)

## Sub-directories

- [task-packages/](task-packages/)
- [target-results/](target-results/)
- [transition-candidates/](transition-candidates/)
- `intake/` — _(not present)_
- [test-cards/](test-cards/)
- [evidence/](evidence/)
- `focus/` — _(not present)_
