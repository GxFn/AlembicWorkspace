{
  "schemaVersion": 1,
  "taskPackageId": "p1-mainbody-aiscan-gate",
  "demandKey": "alembic-recipe-authoring-guidance-optimization-2026-06-29",
  "summary": "P1.4c CORRECTED (Option A, controller-confirmed) — your prior needs-review was correct: the named GatewayActionRegistry candidate:create/recipe:create are DEAD code (never dispatched), and the controller corrected the oracle. Now gate the REAL live main-body in-process AI create that you found: lib/cli/AiScanService.ts:159 knowledgeService.create(recipe, {userId:'ai-scan'}) — the alembic ai-scan CLI which authors recipes via runScanAgentTask then create+publish, today only Core stage-3. This is the genuine CG-4 in-process gap (section C.7 independent knowledgeService.create). Implement: before the AiScanService.ts:159 knowledgeService.create, run validateAgainst([recipe], {stage:'all', path:'in-process', profile: resolveAuthoringProfile(context), sourceRefResolver: your own in-process fs resolver, dimensionId}) from @alembic/core/knowledge; on violations, do NOT create/publish that recipe — surface the violations (skip-and-report per recipe, consistent with the ai-scan batch flow). Profile: ai-scan without a cold-start session resolves to OPPORTUNISTIC (all content gates plus cheap grounding, NOT the 3-distinct-files floor, NOT session-scope); if an ai-scan run carries dimension/session context it resolves to cold-start (full gate) — let resolveAuthoringProfile decide. PRODUCER-PROMPT (C.4): the ai-scan author is runScanAgentTask -> AgentService.run(scan-extract), the embedded AlembicAgent whose insightProducer was already upgraded to renderGuidance in P1.4b — so producer-prompt-first is most likely ALREADY satisfied; VERIFY the ai-scan extract uses that embedded AlembicAgent producer and note it; only if ai-scan has its OWN separate producer prompt do you upgrade it producer-first. FS RESOLVER: supply your own byte-faithful section C.11 fs sourceRefResolver (consume @alembic/core/knowledge validateAgainst/resolveAuthoringProfile types) — the AlembicAgent gate is not publicly re-exported so you cannot reuse it; this is the sanctioned fallback. Acceptance on node 22: main-body build:check green; a re-point test proving AiScanService now runs validateAgainst before knowledgeService.create (content gates fire; opportunistic drops the floor and session-scope; cold-start keeps them); a currently-passing-corpus regression documenting the bounded new-reject set for ai-scan recipes (expected: only content and cheap-grounding codes, never INSUFFICIENT_EVIDENCE or SESSION_NOT_FOUND under opportunistic); full main-body test green. Discipline: bounded SCOPED tightening (opportunistic lighter bar, not a relaxation; cold-start matches the host); main only, no push or tag or version bump; touch ONLY the main-body; do NOT touch AlembicCore, AlembicPlugin, AlembicAgent, FieldSpec; do NOT gate the dead Gateway actions (Option C skipped — zero live paths); do NOT touch the external HTTP POST /api/v1/knowledge route (Option B is a separate user decision, out of this demand's CG-4 scope). This completes CG-4 for the live in-process AI authoring paths (AlembicAgent done in P1.4b + AiScanService here). Backfill the main-body commit, the AiScanService re-point diff, the currently-passing-corpus new-reject set, the producer-prompt finding (already-covered vs upgraded), and raw build and test output. If the tightening would reject an ai-scan recipe you believe should pass, STOP and return needs-review with the case.",
  "status": "pending",
  "sourceRef": "Controller rework decision (Option A) on tr-p1-mainbody-inprocess-repoint plus the corrected state-root evidence/p0-gate-truth-matrix-2026-06-30.md (main-body in-process entry corrected to AiScanService.ts:159) plus design sections C.7, B-P1.4, 12.3/12.4 plus the shipped Core profile model (8771524, @alembic/core/knowledge)",
  "createdAt": "2026-06-30T01:59:02.122Z",
  "reviewRoute": "rework",
  "targetTasks": [
    {
      "targetTaskId": "p1-mainbody-aiscan-gate",
      "taskPackageId": "p1-mainbody-aiscan-gate",
      "targetWindow": "Alembic",
      "summary": "Option A (controller-confirmed): gate the REAL live in-process AI create AiScanService.ts:159 (not the dead Gateway actions). Before knowledgeService.create, run validateAgainst([recipe], {stage:'all', path:'in-process', profile: resolveAuthoringProfile(ctx), sourceRefResolver, dimensionId}) from @alembic/core/knowledge; on violations skip-and-report that recipe. opportunistic = content + cheap grounding, no 3-file floor, no session-scope. Verify the ai-scan producer is the embedded AlembicAgent insightProducer (already upgraded P1.4b); supply your own C.11 fs resolver (AlembicAgent gate not re-exported). Accept on node 22 with build:check + re-point test + currently-passing-corpus regression (bounded new-reject set) + full test green. Main only, no push; touch only main-body; do NOT gate the dead Gateway actions or the HTTP route. Backfill commit + diff + new-reject set + producer finding. STOP and return needs-review if a should-pass recipe is rejected.",
      "status": "pending",
      "createdAt": "2026-06-30T01:59:02.122Z",
      "reviewRoute": "rework"
    }
  ]
}
