# RW-t2 report — A3 overlay-route delete + C2 version-pin tail (2026-06-13)

Task: alembic-0.3.0-release-wave-rw-t2 (group rw2), AlembicPlugin.
Commits: CC shell **0f002f2** (pushed first, ls-remote verified), parent
**28ed99c** (gitlink → 0f002f2 verified, pushed, ls-remote verified). Codex
shell untouched (c452de3). Publish NOT run (held for user, C1).

## Leg 1 — A3 delete (RC4-style proof set)
- Deleted `lib/http/routes/{evolution,panorama}.ts` + their HttpServer mounts
  + the panorama-only `test/unit/PresentationRoutes.test.ts`.
- Route inventory 11 → 9; neither route in `CODEX_EMBEDDED_RUNTIME_REQUIRED_ROUTES` (8).
- Fresh 5-repo scan (dynamic `import(` + HTTP path literals): zero plugin
  consumers. Dashboard `/panorama` + `/evolution` (relative `/api/v1`) reach
  the MAIN Alembic daemon, contract-mounted there (provider-contracts I22) —
  those twins stay. Full scan in `route-inventory-and-scan.txt`.
- Behavior-neutral: MCP `alembic_panorama` tool + `PanoramaService` untouched;
  only dead HTTP read surfaces gone. Recorded as legacy-register **D3**.
- Git-recoverable (`git rm`; revert restores).

## Leg 2 — C2 version-pin tail
- Exact-version pins kept (D3, NO bump): CC manifest 0.2.0, Codex manifest
  0.2.0, bin `RUNTIME_PACKAGE_SPECIFIER=@gxfn/alembic-runtime@0.2.0`, channel
  specifier `@gxfn/alembic-runtime@0.2.0`.
- Marketplace hosting/naming confirmed: marketplace `gxfn`, plugin `alembic`,
  repo `GxFn/AlembicClaudeCode` → install identity `alembic@gxfn`.
- Negative-cache clear step added to the CC shell install docs (CC4 fix-D,
  docs-only): `/mcp` Reconnect or delete `mcp-needs-auth-cache.json`.

## Leg 3 — proof (unified tree, clean rebuild)
- Suite **2459/2459 (164 files)** — delta −5 panorama-route tests / −1 file
  vs the 2464/165 baseline (the deleted surface's only tests).
- `npm run check` exit 0; `validate --strict` PASS; `verify:codex-plugin` PASS;
  cross-shell drift PASS; smoke `ok:true`.
- DUAL-SHELL PARITY: `diff -rq p5t4-probe rwt2-final` exit 0 — served MCP wire
  surface byte-stable (tools/list + callTool); the deleted overlays never
  touched the MCP surface. Artifacts: `parity-after/`, `parity-baseline-p5t4/`.

## Process note (honest)
A `git stash` inside an exit-1 `&&` chain during smoke diagnosis stranded the
deletion edits mid-task; recovered via `git stash pop` and re-ran the FULL
proof set on the unified tree (deletions + smoke fix + README together) — the
green gates above are all from that single coherent tree, not stitched.

## SN5 residue found + fixed (verification script)
`scripts/smoke-codex-plugin.mjs:114` imported the runtime entry via the split
form `join('dist','lib','codex','mcp','CodexMcpServer.js')` — the SN5
`lib/codex`→`lib/runtime` path-string rewrite only caught slash-bearing string
literals (sibling line 255 was updated), not segmented `join()` args. Fixed
`'codex'`→`'runtime'`. Behavior-neutral, unrelated to A3; surfaced only because
smoke is in this task's proof set. Lesson for later SN waves: sweep
`path.join()` segment-array forms too.
