{
  "schemaVersion": 1,
  "taskPackageId": "dh-1-host-abstraction-unify-delete-generic",
  "demandKey": "alembic-plugin-dual-host-architecture-refactor-2026-06-19",
  "summary": "DH-1 host 抽象统一（RC-1）+ 删 generic-host-agent（RC-5）。AlembicPlugin 基线 afbf25e，直提 main。host identity 由 hostShape 派生（去 RuntimeContext.ts:45 codex 硬默认、:62 expectedPluginHost 派生、加 CLAUDE_CODE_PLUGIN_HOST 常量）；cc manifest:32/33 + bootstrap:100 声明 claude-code；Diagnostics.ts:542 比较走 expectedPluginHost；AGENT_HOSTS 去 generic（6 文件 contract.ts:11/mcp-tools.ts:103/KnowledgeContextStatus.ts:29-36/cross-host-readiness.ts:55-59 + 2 测试）。不建 L3、不做 de-Codex 改名、不动 per-host 产物（DH-2/3/4）。漂移门禁 RED 已解耦（归 DH-4）。完成=identity 派生正确 + generic 删净 + tsc 绿 + 受影响测试绿 + 非强进程不变量 + commit hash。",
  "status": "pending",
  "sourceRef": "Design/docs/current/alembic-plugin-dual-host-architecture-refactor-2026-06-19.md (### DH-1 精确改点 + 已决 1/2/4) + wakeflow-ledger/AlembicPlugin/dh-0-dual-host-precheck-2026-06-19.md (⑤ true L3≈5 files, ~221 surface) + 控制器决策 evt-20260619155155-0009 (drift→DH-4 解耦)",
  "createdAt": "2026-06-19T15:55:30.365Z",
  "targetTasks": [
    {
      "targetTaskId": "DH-1",
      "taskPackageId": "dh-1-host-abstraction-unify-delete-generic",
      "targetWindow": "AlembicPlugin",
      "summary": "DH-1 host 抽象统一 + 删 generic-host-agent（RC-1 + RC-5）。本窗口=AlembicPlugin（仓库 `~/Documents/AlembicWorkspace/AlembicPlugin`，基线 HEAD `afbf25e` clean = daemon-removal + MC-3p 后，纯 MCP 非强进程）。**直提 main，不开分支。**\n\n**前置已澄清（控制器本会话决策，重要）**：DH-0B 已核实 alembic-recipes/structure + recipes-setup/README 漂移门禁 RED 是**真 host-divergent 工具面**（main `alembic_knowledge/confirm_usage/alembic_guard` ↔ plugin `alembic_search/alembic_code_guard/alembic_project_matrix`），无法在不改 main 四工具对外 MCP 语义下转绿。用户决策 B：该漂移**解耦于 DH-1**（归 DH-4 per-host 门禁模型正确转绿），**DH-1 不依赖漂移门禁绿、也不得去碰那 3 项 skill/template 漂移资产**。DH-1 改点（运行时 host 抽象代码）与漂移资产不相交，照常推进。\n\n**范围（仅 RC-1 host 抽象统一 + RC-5 删 generic；不建 L3、不做 de-Codex 改名、不动 per-host 产物分叉——那些分别是 DH-2/DH-3/DH-4）**：\n\n1. **host 身份（RC-1，由 hostShape 派生、消除硬编码 codex）**：\n   - `runtime/RuntimeContext.ts:45` 去 'codex' 硬默认；`:62` `expectedPluginHost` 由 `hostShape` 派生（非恒 codex）。\n   - `PluginRegistry.ts` hostShape(:59-78,:85) 检测已正确 → 把它喂给 `RuntimeContext` 派生 identity，让 `pluginHost` 可用于 per-host 身份。**本阶段只统一 identity 来源，不改四工具行为派发（行为下沉是 DH-2/3）。**\n   - 加 `CLAUDE_CODE_PLUGIN_HOST` 常量（对称现有 `CODEX_PLUGIN_HOST` RuntimeContext.ts:16）+ per-host `expectedPluginHost`。\n2. **manifest（RC-1）**：cc `.claude-plugin/plugin.json:32` 删 `ALEMBIC_CHANNEL_ID=codex`、`:33` `ALEMBIC_PLUGIN_HOST` 改 `claude-code`；**codex `.mcp.json:8` 不变**。\n3. **bootstrap（RC-1）**：cc `bin/alembic-start.mjs:100` 默认改 `claude-code`（现两脚本相同；**codex 脚本不变**）。\n4. **诊断（RC-1）**：`diagnostics/Diagnostics.ts:542` 比较改 `=== expectedPluginHost`（非恒 `CODEX_PLUGIN_HOST`），使 cc 运行时不再自认 codex。\n5. **删 generic-host-agent（RC-5，6 文件，AGENT_HOSTS 收敛 codex+claude-code）**：\n   - `public-tools/contract.ts:11`（`AGENT_HOSTS` 去 `generic-host-agent`）；\n   - `mcp-tools.ts:103`（去 + 合并到 contract import）；\n   - `KnowledgeContextStatus.ts:29-36`（去 generic + 其他未实现 future-host stub）；\n   - `cross-host-readiness.ts:55-59`（随 contract 自动）；\n   - 2 测试 `CrossHostReadiness:35`、`KnowledgeContextContracts:26`（期望改为 codex+claude-code 双宿主）。\n\n**完成定义**：host identity 由 hostShape 派生（cc→claude-code、codex→codex），无硬编码 codex 默认；generic-host-agent 删净（AGENT_HOSTS=codex+claude-code）；cc manifest/bootstrap 声明 claude-code；Diagnostics 比较走 expectedPluginHost；`tsc --noEmit` exit 0；受影响测试（含上述 2 个）绿；**pre-existing 39 vitest 沙箱失败文件是 DH-0 已记基线、非 DH-1 回归**（须明确区分、不得扩面去修）；纯 MCP 非强进程不变量不回退/不引入常驻；四工具对外 MCP 语义不变；未建 L3、未做 de-Codex 改名、未动 per-host 产物分叉、AlembicCore 不动。\n\n**边界/不变量**：只动上述 RC-1/RC-5 文件；不进 DH-2（建 L3）/DH-3（cc adapter + 52 de-Codex 改名）/DH-4（per-host 产物 + 漂移门禁模型）；不改 codex 侧 manifest/.mcp.json:8 与 codex bootstrap；不改四工具语义；不下沉 Core；不依赖/不强改漂移门禁绿，也不碰 3 项漂移 skill/template 资产（漂移归 DH-4）。先读本仓 `CLAUDE.md`/`AGENTS.md` 并遵循更严规则；用本仓既有 stack/scripts/Node 版本。\n\n**回 TargetResultEnvelope**：每改点 file:line + diff；`tsc --noEmit` 与受影响测试原始输出（明确区分 pre-existing 39 沙箱基线 vs DH-1 新增）；AlembicPlugin main commit hash（直提 main）；hostShape→identity 派生验证证据（cc 运行时自认 claude-code，如 RuntimeContext/Diagnostics 单测或探针输出）；任何 needs-controller/needs-user-decision/blocked。",
      "status": "pending",
      "createdAt": "2026-06-19T15:55:30.365Z"
    }
  ]
}
