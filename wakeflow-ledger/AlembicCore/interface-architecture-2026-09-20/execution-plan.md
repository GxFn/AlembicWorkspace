# 接口接线层架构研究

Gate conclusion：当前用户请求是理解架构、联网和本地参考研究并思考整理方案。最低闭环是当前源码/实际消费者 → 接线分类与差异 → 主源资料/Tencent代码对照 → 具体迁移顺序与验证边界。本轮以只读研究和方案交付为主，不把设计建议当成已授权的破坏性接口删除。没有当前阻塞。

基线：Core4a0b277、Plugin e0deb9a；主仓当前提交将在inventory记录。旧Wakeflow需求为无关planned条目；不更改总控状态/派发。

1. AST盘点package exports、宿主Core导入、纯转发与有行为适配。
2. 追踪向量/只读检索和知识写入两条真实闭环，核对端口职责/生命周期/事务与验证边界。
3. 阅读本地TencentDB-Agent-Memory真实接口与消费链，并查阅官方/原作者架构与package边界资料。
4. 产出分层/接线图、合并保留矩阵、按优先级落点和验收条件，明确事实、推断与建议。
5. 只读边界检查和artifact自检；不运行无改动的全量测试，不创建新的产品commit。保护已有规则文件与coverage/index.ts。
