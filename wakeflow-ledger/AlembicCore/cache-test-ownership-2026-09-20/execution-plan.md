# 缓存与测试归属审查执行计划

日期：2026-09-20。Core 基线 c1f6d77，Plugin 基线 11f8d47。用户“确认 继续”承接上一批明确提出的跨仓测试归属/重复用例与缓存生命周期审查。

Gate conclusion：命令与授权明确；最小闭环为逐文件/实际消费者扫描、覆盖映射、基线、受控复现、有限整合、验证与提交。没有当前阻塞。当前 Wakeflow 活跃条目为无关旧 planned demand；本任务不创建派发或更改总控状态。

1. Core 与宿主 HNSW 大测试逐 describe 对照；确认独有行为及真实包/宿主消费后，保留 Core 权威行为覆盖，整理重复接入测试。四个 Core 边界测试不删除。
2. 缓存实现/公开出口/真实 HTTP 消费及关闭链读取；先证明问题再改。保留 TTL 秒、过期严格小于边界、null miss 和既有单例/公共接口兼容。
3. 代码行为修复先 RED，再最小修复；纯测试搬移以基线/断言对应表验证，不能将降低计数本身作为成果。
4. Core build/check 与必要宿主 type/test；Core 先提交，外层只做本次用户确认的测试归属/必要消费接入。归档逐文件范围、审查证据、提交与剩余风险。

保护：三个仓库原有 AGENTS.md/CLAUDE.md 和 Core 未跟踪 coverage/index.ts；dist 仅构建。无 vendor、发布、推送、依赖更新或外部模型网络操作。
