# 冷启动链路机制重设计——原始诉求（Original Plan）

- 日期：2026-07-04
- 提出人：用户（Dashboard 真机冷启动低产排查后的直接指示）
- 状态：已确认为设计输入（本文件仅忠实记录诉求，不做设计展开）

## 触发事件

2026-07-04 真机 ecf32806（AlembicWorkspace 空间）Dashboard 冷启动（DeepSeek V4-pro，job `bootstrap_mr5ckv5p_211536ea`）：architecture 8 产 6 拒、ts-js-module 15 产 14 拒，最终只入库 2/1。排查确认根因＝模型在 RECORD 阶段凭记忆写证据时捏造 file:line 引用（`Alembic/lib/types/agent.ts` 等路径从未存在；plan 树里存的是正确的 `agent.d.ts`），anti-fab 提交门禁按设计正确拦截；四层机制叠加放大（RECORD/produce 工具面锁死、传输带重建丢原始证据、配额压力、录入零校验）。

## 用户诉求原文要点

1. 以下四项点评修复可以执行：
   - ① note_finding 时就地做引用存在性校验（把检测前移到还有 code 工具、来得及修正的阶段）；
   - ② producer submit 拒绝返回附真实候选路径提示；
   - ③ producer 输入截断策略优先保 verbatim 片段；
   - ④ 提示词强化"引用必须逐字来自工具返回"。
2. 但不满足于点状修复：**希望基于现状做完整链路的机制上的优化**。
3. 对压缩传递、早期信息丢失问题：**做全新的临时文件系统**——从真实项目先分析挖掘，同步记录到文件里，整个链路里随时可以查看这个非压缩的文件。
4. **更全面地整体设计冷启动链路**，深入思考现在存在哪些问题，**统一从机制设计层面做架构来避免问题，而不是小修小补**。

## 边界备注

- 本诉求由用户在控制器窗口直推（用户直推线）；本轮交付物＝设计文档，未授权派发与实现。
- 提交门禁不放松是排查阶段已明确的共识前提。
