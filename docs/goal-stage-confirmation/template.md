# 需求目标与分阶段确认模板

状态：长期模板
维护窗口：AlembicWorkspace

本模板用于用户提出较大任务目标后、正式派发执行窗口前。总控窗口必须先按本模板创建任务级“最终目标 + 分阶段确认”文档，等待用户确认后再新建或激活具体 wave 执行计划。

本模板必须基于 `docs/requirement-designs/<需求名>/` 下的原始计划书和需求设计文档填写；缺任一项，不进入目标阶段确认。

本模板只确认完整功能模块。若需求目标、用户场景、输入输出、状态变化、生产方、消费方、验证方式或完成定义不明确，必须停在确认问题，不得进入执行派发。不得把抽象接口、空 provider、空 adapter 或无真实调用方的代码连接当作阶段目标。

## 用户原始目标

记录用户这次发布的原始目标、限制和补充口径。不要把长期产品路线代替用户当前任务目标。

```text
<保留用户目标的关键原话或准确转述>
```

## 总控理解

用总控自己的话确认这次要完成什么：

- 目标：
- 关键约束：
- 不能偏离的边界：
- 当前不确定点：

## 前置需求设计

- 原始计划书：
- 需求设计文档：
- 代码实现依赖调研：
- 调研结论：
- 当前已有能力：
- 主要缺口：
- 功能闭环：
- 生产方 / 消费方：
- 本确认文档对需求设计文档的调整：

## 最终完成定义

本次任务达到什么状态才算完成：

- 用户场景完成：
- 功能 / 边界完成：
- 输入输出和状态变化完成：
- 跨仓库消费完成：
- 删除 / 保留完成：
- 文档和证据完成：
- 验证完成：

## 非目标

明确本次不做什么，避免窗口自行扩张：

- 不做：
- 不做抽象占位：
- 暂不删除：
- 暂不发布：
- 不涉及窗口：

## 影响范围

最终覆盖窗口：

| 窗口 / 状态 | 任务 |
| --- | --- |
| `Alembic`<br>状态 |  |
| `AlembicCore`<br>状态 |  |
| `AlembicAgent`<br>状态 |  |
| `AlembicDashboard`<br>状态 |  |
| `AlembicPlugin`<br>状态 |  |
| `BiliDili`<br>状态 |  |

补充说明：

- 新发现的关联仓库 / vendor / artifact：
- 当前不纳入原因：

## 依赖链判断

先写 producer / consumer，再决定谁可以派发：

| 上游产出 | 生产窗口 | 消费窗口 | 派发判断 |
| --- | --- | --- | --- |
|  |  |  |  |

原则：

- 上游 contract / API / artifact / schema / evidence 未完成前，下游只能是 `阻塞` 或 `观察中`。
- 不允许下游窗口猜字段、复制临时 contract 或提前做 fallback。

## 阶段计划

| 阶段 | 目标 | 前置条件 | 完成标准 | 当前可派发窗口 | 不派发窗口 |
| --- | --- | --- | --- | --- | --- |
| 1 |  |  |  |  |  |
| 2 |  |  |  |  |  |
| 3 |  |  |  |  |  |

## 当前阶段判断

- 当前阶段：
- 为什么先做这一阶段：
- 为什么不先做其它阶段：
- 本阶段形成的功能闭环：
- 确认后第一波可启动窗口：
- 等待窗口：
- 确认后是否需要新建 wave 执行计划：

## 验证策略

- 每波最低验证：
- 阶段完成验证：
- 稳定面统一验收：
- 真实项目 smoke 触发条件：

## 风险与确认问题

- 风险：
- 需要用户确认：
- 不明确时禁止派发：
- 若用户未确认，当前派发状态：

## 窗口分派

确认前必须保持不派发。确认后，再把本节或后续 wave 文档改成真实分派表。

| 窗口 / 状态 | 任务 |
| --- | --- |
| `Alembic`<br>暂停 | 等待用户确认本任务目标与阶段计划。 |
| `AlembicCore`<br>暂停 | 等待用户确认本任务目标与阶段计划。 |
| `AlembicAgent`<br>暂停 | 等待用户确认本任务目标与阶段计划。 |
| `AlembicDashboard`<br>暂停 | 等待用户确认本任务目标与阶段计划。 |
| `AlembicPlugin`<br>暂停 | 等待用户确认本任务目标与阶段计划。 |
| `BiliDili`<br>暂停 | 等待用户确认本任务目标与阶段计划。 |

## 可复制提示词

发送给：无

```text
等待用户确认任务级最终目标与阶段计划；当前不派发。
```

不发送给：`Alembic`、`AlembicCore`、`AlembicAgent`、`AlembicDashboard`、`AlembicPlugin`、`BiliDili`。

## 回填区

### 用户确认

- 状态：未确认
- 确认时间：
- 用户调整：

### 确认后第一波

- 启动文档：
- 发送窗口：
- 阻塞窗口：
- 观察窗口：
- index 当前计划是否已切到 wave 执行计划：
